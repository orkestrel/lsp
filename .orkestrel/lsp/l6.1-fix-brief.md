# Unit L6.1 — the L6 fix round in the lsp tree

Role and engine: `implementer` route on GPT-5.6 Sol, working in `/home/user/lsp` under
`workspace-write` as its sole writer. You perform this assignment directly and spawn nothing. You
are the fix unit for the L6 audit findings that live in this tree; the reconciliation at
`.orkestrel/lsp/l6-audit-reconciliation.md` rules each finding and this brief carries them. Read
that file, the reviewer verdict `l6-audit-reviewer-verdict.md`, and the repository's `AGENTS.md`
with `.claude/rules/typescript.md` and `.claude/rules/writing.md` before editing.

## Standing tree state, named so you do not stop on it

The tree is on `main` with the L6 round HELD UNCOMMITTED across `src/core/types.ts`,
`src/core/factories.ts`, `src/core/LSPClient.ts`, `guides/lsp.md`, and the test files. Never
revert, stash, or reset anything; your edits land on top and commit with the round. The sandbox
denies network and spawning grandchildren; every command you need runs in-process.

## The work — each item carries one audit finding

1. **Delete the unreachable cancellation branch (reviewer F1, ruled for deletion).**
   `#timeoutRequest` at `src/core/LSPClient.ts:556-564` ends with
   `if (!this.#settle(id, timeout, true)) return`, then
   `if (this.#lifecycle.phase !== 'ready') return`, then `this.#cancelRequest(id)`. The phase
   guard can never pass: only signal-less requests bind this handler, and the only signal-less
   callers are initialize during `starting` and shutdown during `destroying` (the reconciliation
   records the corroborated enumeration). Remove the guard and the cancel call, and settle the
   remaining shape minimally — a bare `this.#settle(id, timeout, true)` needs no dead result
   check. The retained rows at `tests/src/core/LSPClient.test.ts:255` and `:1419` pin the
   absence of `$/cancelRequest` on lifecycle timeout and must stay green unchanged. If any test
   row reddens, stop and report.
2. **The `open` `@param` line (reviewer F2).** `src/core/types.ts:308` reads "@param options -
   The cancellation signal that bounds the diagnostics wait." Replace with: "@param options - The
   options carrying the cancellation signal that bounds the diagnostics wait."
3. **The factory signal term (reviewer F3).** `src/core/factories.ts:7` names an unqualified
   "abort signal". Adopt the guide row's term: "The transport, workspace, lifecycle timeout,
   client abort, and initial event hooks."
4. **The transport-close scope sentence (reviewer F4, ruled for rewording).** The sentence in
   `src/core/types.ts:270-271` and its twin in `guides/lsp.md:17-19` restrict transport-close
   settlement to destroy time, while `#closeTransport` also runs from `#releaseGeneration` on a
   handshake failure. Reword both, identically, so the destroy-time restriction stays on the exit
   write and leaves the transport-close clause: "the initialize and shutdown requests, the
   destroy-time exit write, and transport-close settlement". Touch no other sentence.
5. **The successor L6-B record (reviewer claim 5).** Write
   `.orkestrel/lsp/l6-b-client-report-r2.md`: a header naming it the successor to § What changed
   of `l6-b-client-report.md`, then one row per conversion — the old title, the new title, the
   ruling clause of the adopted contract that made the old assertion false, and the coverage the
   conversion retired. The three conversions, from the round diff:
   `bounds a push diagnostic publication deadline` → `bounds a push diagnostic publication with
   its call signal`; `times out one request and sends its cancel notification` → `aborts one
   request and sends its cancel notification`; `detaches a drained publication deadline before
   the next generation` → `detaches a drained publication signal before the next generation`.
   The middle conversion retired the only row reaching the `#timeoutRequest` cancellation branch;
   state that this unit deletes the branch on the reconciliation's ruling, so the retired
   coverage covers machinery that no longer exists.

## Scope

Owned files: `src/core/LSPClient.ts`, `src/core/types.ts`, `src/core/factories.ts`,
`guides/lsp.md`, and the new `.orkestrel/lsp/l6-b-client-report-r2.md`. Report-only: the test
files and every other record. Off-limits: everything else. No commit, no push, no
`git checkout`/`restore`/`stash`/`reset`/`clean`, no installs, no tree-wide `format` or
`lint --fix` (scoped `oxfmt --write` over owned files only), no `any`, no assertion, no
suppression.

## Output

Your final answer, returned as your last message AND written to
`/home/user/lsp/tmp/codex/l6.1-fix-last-report.md`: the exact edits per finding; the gate readings
listed under acceptance with exit codes; `git diff --stat` and `git status --short`; observations
outside scope, each named against the unit that owns it. No process diary.

## Deviation contract

A conflict with a ruled finding stops the unit: report expected, found, exact evidence, done or
not done, and at most one short hypothesis. Named stop conditions: any test row reddens; the
deletion requires touching more than `#timeoutRequest`; a reworded sentence contradicts the code.
Sentence flow around the prescribed wordings is yours to decide, record, and carry on from.

## Acceptance criteria

Ordered cheap-first.

1. `git status --short` shows changes only in the owned files beyond the standing held entries.
2. Scoped `oxfmt --check` and `oxlint --deny-warnings` over the owned TypeScript and guide files
   exit 0.
3. `npm run check` exits 0 tree-wide.
4. `npm run test:src:core -- tests/src/core/LSPClient.test.ts` exits 0 with no row changed.

## Review evidence

The Orchestrator captures the actual diff and status after you exit and routes the re-check to
the `reviewer` lane (Opus 5), the engine that did not write this unit.
