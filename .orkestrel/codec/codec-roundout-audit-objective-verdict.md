**1. BROKEN** — `README.md` never states the `computeBytes` divergence the claim locates there. TSDoc on `measureUTF8` and `guides/codec.md` both say `computeBytes` counts the three replacement bytes `TextEncoder` writes for a lone surrogate, and `measureUTF8` answers `undefined`; `scaffold` `computeBytes` does walk unpaired surrogates as width 3. `README.md` carries the surface row and the encode-side sound-triple sentence, and it does not mention `computeBytes` or that split. The encode-side triple and the 1/2/3/4 walk are not the break: the orchestrator’s 30,000-round probe against `TextEncoder` on well-formed text, the width-defect mutation control, and the source walk (`isWellFormed`, then `charCodeAt`, no `encodeUTF8` call, no output buffer) held. Attack that `isWellFormed` is a forbidden delegation failed: the family law forbids asking the encoder, not asking ECMA-262 for well-formedness, and `encodeUTF8` uses the same door. Smallest fix: copy the guide’s `computeBytes` sentence into `README.md` beside the sound-triple paragraph.

**2. CONFIRMED** — Attack on (a): name CESU-8 (`ED A0 80 ED B0 80`) and a 3-byte overlong wrapped as `41 E0 80 80 41` as still unwitnessed. Those buffers are not in the two new sweeps, and they do not falsify the adopted close: the embedded-pair sweep (`41 b1 b2 41`) is what reaches a width or continuation defect after a consumed byte, which the two-byte-as-whole-buffer walk never presents; the per-position sweep over `UTF8_BOUNDARIES` encodings is what reaches a 4-byte overlong and an encoded surrogate one lead byte from U+D7FF (`ED 9F BF`). Writer mutation control (surrogate refusal removed reddens the per-position sweep; continuation check removed reddens the embedded-pair sweep) binds those instruments. Attack on (b): U+D7FF / U+E000 absent from the populations — they sit in `TEXT_CHARACTERS` and in `UTF8_BOUNDARIES`, so `TEXTS` and the mutation sweep carry them. Attack on (c): no reach assertion, and `longest > 4` only on admitted canonicals — `carries admitted and refused mutants on every face` exists; construction substitutes from `MUTANT_CHARACTERS` (foreigners included) into canonical encodings of prefixes up to 24 bytes, so a refused text longer than four characters exists on the §4, §5, and hex faces. The 117 / 37 figures are not in the suite; the assertion is mixed admit/refuse plus longest mutant past 4, which is the reach claim. Attack on (d): `WINDOWS_1252_INDEX` copied from `WINDOWS_1252_HIGH` — INDEX lives in `tests/setup.ts`, values are characters with Unicode-name comments, source values are code points; the test walks HIGH entries against INDEX and INDEX keys against HIGH, pins length `0x20 - WINDOWS_1252_UNDEFINED.length`, and forbids undefined slots as INDEX keys.

**3. CONFIRMED** — Attack: a new fence comment disagrees with the barrel, or a flagged consequence is an unnamed defect. Hex, charset, and measure fences in `guides/codec.md` `## Patterns` are transcribed in `tests/guides.test.ts` `flagship fences` and match (`'ab'` / `undefined` uppercase hex, `'\ud800'` / `0xC0 0x80` / BOM kept, Latin-1 `0x80` → `'\u0080'`, Windows-1252 `0x81` refused, odd UTF-16LE length, `measureUTF8` widths 2/2/3/4 and lone-surrogate `undefined`). `setup.ts` importing `@src/core` is sound: every Vitest project already loads that setup file, and the mutant bases are this package’s own canonical encodings, so they have to come from `encodeBase64` / `encodeBase64URL` / `encodeHex`. The relative `WINDOWS_1252_HIGH` import in `tests/src/core/helpers.test.ts` is sound: that table is in the guides `INTERNAL` list and is not on the barrel, so the second reading cannot go through `@src/core`. The `package.json` line is sound: `description` already names the measure family and the shipped charsets; `keywords` name those families; `exports` is `.` plus `./package.json`.

**4. CONFIRMED** — Attack: a barrel symbol missing from guide or README, `dist` missing `measureUTF8`, dead tables, or `measureUTF8` half-added. `src/core/index.ts` re-exports validators and helpers only; `dist/src/core/index.js` exports that same function set, including `measureUTF8` with the same walk; guide and README surface tables name those functions and keep `BASE64_ALPHABET`, `BASE64_LOOKUP`, `HEX_ALPHABET`, `HEX_LOOKUP`, and `WINDOWS_1252_HIGH` off the barrel (`INTERNAL` in `tests/guides.test.ts`). `MEASURE_MUTANTS`, `UTF8_BOUNDARIES`, `UTF8_MEASURES`, and `WINDOWS_1252_INDEX` are consumed by the law, oracle, and index tests. The brief’s export census is not a hole in that surface.

VERDICT: FAIL — 1 broken, 0 unresolved, 0 not-evidenced, 0 findings outside the claims
