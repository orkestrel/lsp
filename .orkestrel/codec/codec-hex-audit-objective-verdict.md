**1. CONFIRMED.** `decodeHex` returns `undefined` unless `text.length % 2 === 0` and every `charAt` pair hits `HEX_LOOKUP` (`0-9a-f` only). Empty is admitted (`Uint8Array(0)`). Failed attacks: `''`; `'abc'` (odd, before lookup); `'AB'` / `'aB'` (`B` absent); `'١١'` (Arabic-Indic digits absent); `'0xab'` (`x` absent); `'a '` / `'ab\n'` (lookup miss or odd length). `encodeHex` emits only `HEX_ALPHABET`, so `decodeHex(encodeHex(bytes))` deep-equals `bytes` with no length branch; admitted text re-encodes to itself because lookup and alphabet agree. Guide states the §8 uppercase table and the lowercase departure, and names the canonical-form reason (`'AB'` re-encodes as `'ab'`).

**2. CONFIRMED.** `measureBase64` repeats the §4 checks (`length % 4`, `endsWith` padding, `BASE64_LOOKUP`, unused-bit masks `0x0f` / `0x03`) and never calls `decodeBase64`; it allocates no `Uint8Array`. Failed shared-structure attacks: last-group skips are entailed by `endsWith('==')` / `endsWith('=')`, so a non-pad character cannot be skipped; `'AA==AAAA'` still looks up `=` in a non-final group; `'A==='` still looks up the second `=`; the return `(text.length / 4) * 3 - padding` is the decoder's allocation size. A shared unused-bit mask that still refuses `'aa=='` cannot ship: `MEMBERSHIP` pins `'AI=='` / `'AB=='` / `'AC=='` / `'AAB='` on the decoder, and a measure-only mask drift disagrees on those texts inside `SWEEP`. Canonical encodings are pinned to the encoder's input length, not to `decodeBase64(text)?.length`. Established agreement on the 120,000-text probe stands.

**3. CONFIRMED.** Failed attacks: nested functions in the four additions (none); `isHex` off `validators.ts` (it asks `decodeHex`); `HEX_ALPHABET` / `HEX_LOOKUP` on the barrel (`index.ts` star-exports only `validators` and `helpers`; `INTERNAL` names both); options, error type, `any` / `as` / `!`; TSDoc missing `@param` / `@returns` / `@example` / `@remarks`; `{verb}{Noun}` / `is*` forms; non-ES `Buffer` / `atob` / regex. Totality matches the charter: `encodeHex` cannot fail on `Uint8Array`; `decodeHex` / `measureBase64` return `undefined` on a refused string; `isHex` is total on `unknown`.

**4. BROKEN.** The four named catches hold: uppercase `HEX_LOOKUP` entries vs `HEX_MEMBERSHIP` `'AB'` / `'aB'` and the uppercase sweep; measure off-by-one vs `MEASURES` and the octet-prefix length pin; `HEX_OCTETS` (`byte.toString(16).padStart(2, '0')`) vs `encodeHex` / `decodeHex`; `INTERNAL` vs a barrel row for `HEX_ALPHABET`. The suite's hex round-trip membership rule is byte length 0, 1, and 2 only (`''`, every octet, every pair, oracle of one octet, `HEX_SWEEP` texts of at most four characters). A defect that fires only at length ≥ 3 — `encodeHex` inserting a separator, uppercasing a tail, or truncating after two bytes — does not redden. Smallest fix: in the existing whole-buffer test, add `expect(decodeHex(encodeHex(OCTETS))).toStrictEqual(OCTETS)`, and round-trip `OCTETS.slice(0, 3)` beside the §4/§5 residue loop.

**5. BROKEN.** Two changed sentences are false. In `guides/codec.md` § The laws: “The suite drives it across the same sweep population the canonical-form law uses, so the two walks agree on every text or the sweep reddens.” Canonical-form now uses `SWEEP` and `HEX_SWEEP`; measure uses `SWEEP`, `MEMBERSHIP`, `MEASURES`, and octet-prefix encodings, not `HEX_SWEEP`, and a finite sweep does not redden on every string. The Tests bullet already names the real measure population — copy that wording into the laws paragraph. In § Tests: “The round-trip law runs the whole octet space in one buffer, every padding residue, every single byte, and every byte pair, on the §4, §5, and §8 faces alike.” §8 has empty, every single byte, and every pair; it does not run `OCTETS` as one buffer and has no padding residue. Split §8 off that list. Hex doors, lowercase-vs-RFC-table, membership (hex shipped; `measureBase64` shipped; §5/§8 measures waiting), non-goals, producers (`bytesToHex`, mcp digest hex, `digest('hex')`; `toHexLower` is number-to-hex, not a byte coding), escaped `\|` in the signature column, and the wave-1 `'aa=='` / `'aQ=='` sentences remain true.

**6. CONFIRMED.** Failed attacks: a one-member `measure*` family as asymmetry (the membership bar is the creation gate; §5/§8 measures are named as waiting); dead `HEX_*` data (alphabet, lookup, sweep, membership, and oracle all have readers); dist/README/guide surface drift (`dist/src/core/index.js` exports `encodeHex`, `decodeHex`, `isHex`, `measureBase64` and does not export the hex tables). No hex guide fences: `findUnexampled` treats TSDoc `@example` as enough, and that `@example` exists. `charAt` on the measure walk allocates no output buffer; it matches the §4 encoder's table walk.

VERDICT: FAIL — 2 broken, 0 unresolved, 0 not-evidenced, 0 findings outside the claims
