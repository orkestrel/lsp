1. **CONFIRMED** — Failed attack: lone continuation mid-stream, 0xF5–0xFF leads, and width-threshold code points slip past `decodeUTF8` / `encodeUTF8`, or a leading BOM is stripped. `decodeUTF8` sets width 0 for 0x80–0xBF and for every lead outside 0x00–0x7F / 0xC2–0xDF / 0xE0–0xEF / 0xF0–0xF4, so `41 80 41` and 0xF5–0xFF refuse; overlong (`width === 3 && point < 0x800`, `width === 4 && point < 0x10000`), encoded-surrogate, `point > 0x10FFFF`, truncation, and non-continuation checks match RFC 3629. `encodeUTF8` refuses exactly `!text.isWellFormed()`. U+FEFF encodes to `EF BB BF` and `FF FE` and decodes back on the UTF-8 and UTF-16LE faces. The two-byte oracle equality sweep, `UTF8_BOUNDARIES`, and the orchestrator BOM / round-trip runs are the evidence that held.

2. **CONFIRMED** — Failed attack: `WINDOWS_1252_HIGH` only mirrors the platform oracle and disagrees with published CP1252. The 27 entries match the Microsoft/Unicode mapping (`0x80→U+20AC` … `0x9F→U+0178`); the omitted keys are the published undefined slots `0x81, 0x8D, 0x8F, 0x90, 0x9D`. Identity bands are `byte < 0x80 || byte >= 0xA0`. The defined mapping is injective (251 unique decode outputs). `encodeWindows1252` refuses every U+0080–U+009F because no table value lands in that range. Circularity of the WHATWG sweep does not survive that independent table check.

3. **CONFIRMED** — Failed attack: Latin-1 decode is partial, encode admits a code unit past 0xFF, or `isLatin1` is not encode-side. `decodeLatin1` is identity on every byte. `encodeLatin1` returns `undefined` iff any `charCodeAt` is `> 0xFF`. `isLatin1` asks that encoder, which is the guard-side ruling.

4. **CONFIRMED** — Failed attack: odd length, unpaired lead (mid or final), unpaired trail, astral pairs, or leading `FF FE` mishandled. `decodeUTF16LE` refuses odd length, a trail with no lead, a lead with no following unit, and a lead followed by a non-trail; an astral pair consumes four bytes; `FF FE` is U+FEFF. `encodeUTF16LE` uses `isWellFormed`. `UTF16_REFUSALS`, the two-byte oracle sweep, and `TEXTS` (U+10000, U+FEFF) are the pins.

5. **CONFIRMED** — Failed attack: the `ArrayBuffer.isView` gate is not a real length-trap, or a fleet caller legitimately passes a proxied view. `ArrayBuffer.isView` reads `[[ViewedArrayBuffer]]`; a revoked proxy and a proxy wrapping a `Uint8Array` lack that slot, so the guard returns false without `instanceof` (which throws on revoked) and without reading `length`. A `length` trap on a wrapping proxy would otherwise walk a decoder past the target’s bytes. Fleet `src/` contains no `new Proxy` of a `Uint8Array`; wrapping proxies are test hostiles only.

6. **CONFIRMED** — Failed attack: `measureBase64URL`’s composition over `measureBase64` breaks the sound triple, or the guide sentences “walks the grammar itself” / “cannot ask” become false. The §5 preamble is the same substitution `decodeBase64URL` uses, then `measureBase64`, never a decoder. That substitution is the §5 grammar. “Cannot ask” in the families section means cannot ask a decoder (“a measure that decodes has measured nothing”). The law sweeps and the orchestrator’s 120,000-text run hold the triple.

7. **CONFIRMED** — Failed attack: families still universalize “encode cannot fail”; a WHATWG note, the BOM stance, or the later list is wrong; some other guide/README sentence is false. Families now scopes “cannot fail” to RFC 4648 `encode*` and names the charset refusals. The three WHATWG notes match the probed oracles in `tests/setup.ts`. The BOM stance is the round-trip law. The later list is `measureUTF8` waiting on scaffold; `computeBytes` counts a lone surrogate as three bytes (the `TextEncoder` replacement width) while `encodeUTF8` refuses. README surface rows match the guide. No false sentence in those documents.

8. **CONFIRMED** — Failed attack: the unit-A measure off-by-ones and the unit-B classes (overlong, slot 0x81, odd length, U+0100) are unpinned, or the oracles are unrestricted. Pins: `UTF8_REFUSALS`, `WINDOWS_1252_UNDEFINED`, `UTF16_REFUSALS`, `LATIN1_REFUSALS` (`Ā`), sound-triple sweeps. Oracles: `ignoreBOM: true`; no `latin1` label; windows-1252 compared only on defined bytes. Suites cannot witness: a 3- or 4-byte UTF-8 defect that is not a named refusal and does not fit in two bytes (example: `41 80 41`); U+D7FF and U+E000, absent from `TEXTS`; a refused measure text longer than the length-4 sweep that is not a canonical octet-prefix encoding; a shared transcription error between `WINDOWS_1252_HIGH` and the WHATWG oracle.

9. **BROKEN** — `package.json` `"description"` still reads “RFC 4648 Base64 and base64url, pure ES, total, and canonical in both directions.” That unscoped “total” is the universal the families section retired: `encodeUTF8`, `encodeLatin1`, `encodeWindows1252`, and `encodeUTF16LE` are partial. Keywords still omit the charset faces. The README that ships beside this manifest documents those faces. Smallest fix: rewrite `description` and `keywords` to the shipped families and drop or scope `total`. Not broken: dist matches source; README matches the guide; the 27-entry reverse scan is the single-table constraint, not a mapping defect; Patterns remaining Base64 fences is allowed because `findUnexampled` is satisfied by TSDoc `@example`.

VERDICT: FAIL — 1 broken, 0 unresolved, 0 not-evidenced, 0 findings outside the claims
