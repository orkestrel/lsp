1. **CONFIRMED.** Attack that failed: grepped `encodeBase64`, `decodeBase64`, `encodeBase64Url`, and `decodeBase64Url` across the server tree’s `src`, tests, guides, and `package.json`. No definition remains. The barrel is `export * from './helpers.js'` with no codec re-export. The only remaining Base64 names are `@orkestrel/codec`’s `encodeBase64URL` / `decodeBase64URL` in `signToken` / `verifyToken` / `decodeTokenPayload` and the exp-tamper test. The helper table in `guides/server.md` goes `clearCookie` → `signToken`; those four rows are gone and no adjacent helper row left with them. Out-of-tree importers of the deleted helpers are unknowable from these trees.

2. **UNRESOLVED.** `decodeBase64URL` refuses whitespace, `+`, `/`, `=`, and unused trailing bits. The writer’s probe only names whitespace that `atob` stripped on the signature segment. Settle with `git -C C:\Users\mikes\WebstormProjects\server show 33b39c7:src/server/helpers.ts` (the retired `decodeBase64Url`) then `verifyToken` on one `signToken('client', { secret: 'shh' })` token after each of: last signature character replaced by an unused-bit sibling; `=` appended to the signature; every `-` in the signature replaced by `+`; a tab spliced into the signature. The `ONLY` clause holds only if old and new disagree on the whitespace/tab class and agree on the other three.

3. **CONFIRMED.** Attack that failed: an uncontained decode throw, or a `catch` that swallows a failure that should surface. Signature decode is `decodeBase64URL` then `=== undefined` before the `try` (`helpers.ts` `verifyToken`). Payload decode is the same shape in `decodeTokenPayload`. The remaining `catch` in `verifyToken` wraps `importKey` / `verify`; the remaining `catch` in `decodeTokenPayload` wraps `JSON.parse`. `decodeBase64URL` does not throw. `verifyToken` / `decodeTokenPayload` stay total.

4. **CONFIRMED.** Attack that failed: a payload where `isStandardBase64` + `atob` and `decodeBase64` disagree outside unused bits. `isStandardBase64` is `/^(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?$/`. `decodeBase64` accepts exactly canonical §4. Whitespace inside the payload (`=?base64?QQ ==?=`), mid-string `=` (`AA=A`), and length residues (`Q`, `QQ`, `QQ=`) fail the regex and fail `decodeBase64`. The only regex-accept / codec-refuse class is unused trailing bits (`QR==` vs `QQ==`), which is the named grammar change. OWS trim, marker start/end, unmarked literal return, and fatal-UTF-8 `TextDecoder` are still the same code paths; `/w==` still dies at UTF-8, not at the codec. `encodeSentinel` spells `encodeBase64(new TextEncoder().encode(value))`, the unique §4 spelling `btoa` of those same UTF-8 bytes emitted; the encode tests still pin `Y2Fmw6k=`, `ICBwYWRkZWQgIA==`, `dHdvCmxpbmVz`.

5. **CONFIRMED.** Attack that failed: a remaining production call outside the JSON Schema `byte` validators. Production calls are `isMCPBlobResource` (`resource['blob']`) and image/audio `content['data']` in `validators.ts`. `decodeSentinel` does not import or call it. `helpers.ts` only mentions it in TSDoc as a wider separate rule. `validators.ts` was not rewritten.

6. **CONFIRMED.** Attack that failed: a remaining `base64` / `Base64` / `sentinel` / `token` sentence in `guides/mcp.md` or `guides/server.md` that the shipped code contradicts. The mcp sentinel paragraph names `decodeBase64`, the `QR==` / `QQ==` pair, and `isStandardBase64` as JSON Schema `byte` not governing the payload; the header blockquote and clause 9 list `@orkestrel/codec`. The server helper table no longer has the four rows; `signToken` / `verifyToken` / `decodeTokenPayload` rows and the token fence still match the token functions.

7. **UNRESOLVED.** The server manifest and the mcp manifest pin `file:tmp/tarballs/orkestrel-codec-0.0.1.tgz`; each `.gitignore` ignores `tmp`. Settle the file-list and vendored conjuncts with `git -C C:\Users\mikes\WebstormProjects\server show --name-only --format= 59a8e74` and the same for `aaecb8c`, plus `git -C C:\Users\mikes\WebstormProjects\mcp show --name-only --format= 9986b0f`, against the briefs’ owned files plus `package.json` / `package-lock.json` and the vendored paths. Those `git show` outputs were not in the supplied evidence.

8. **CONFIRMED.** Attack that failed: a tarball path in tests, a `tmp/` sentence in `guides/server.md` / `guides/mcp.md`, a leftover `encodeBase64Url` / `decodeBase64Url` / `encodeBase64` / `decodeBase64` in the server package, or a dead codec import. Substituting `^0.0.1` for the tarball range leaves no such pin. `mcp/guides/server.md` still lists `encodeBase64Url` / `decodeBase64Url`; that file tracks installed `@orkestrel/server` `^0.0.16`, not the codec re-pin.

Findings fitting no claim: none.

`VERDICT: FAIL — 0 broken, 2 unresolved, 0 not-evidenced, 0 findings outside the claims`
