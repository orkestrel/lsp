1. **CONFIRMED.** Attack that failed: a 3-byte packing error the 1-byte and 2-byte residues would miss. `encodeBase64` / `decodeBase64` walk independent 3-byte / 4-char groups; encode of one byte zeros the low nibble of the second sextet (`second & 0x0f`) and encode of two bytes zeros the low 2 bits of the third (`third & 0x03`), so encode never emits a form its decoder refuses. Hand-check `[1,2,3] → 'AQID' → [1,2,3]`. The writer’s 256-octet buffer, every byte, every byte-pair, and the Orchestrator’s 20 000 round-trips on the `0524d8a` dist cover the `tail === 0` path; empty is `''` ↔ `Uint8Array []` on both faces.

2. **CONFIRMED.** Attack that failed: `'AAB='` (one-pad unused bits, the class the Orchestrator did not sweep). `B = 1`, `tail === 1`, `third & 0x03 !== 0` refuses; `'AAB'` on the url face substitutes to the same `'AAB='` and dies there. `'AAa='` / `'AA+='` / `'AA/='` are the same class (`26, 62, 63 & 3 ≠ 0`). Length `% 4 === 1` dies at `text.length % 4 !== 0` (`'A'`, `'AAAAA'`). Whitespace and every non-alphabet code point miss `BASE64_LOOKUP`. `endsWith('==')` / `endsWith('=')` on a `% 4 === 0` length forces last-group `xx==` or `xxx=`; a mid-string `=` is looked up and is not in the table (`'AA=A'`, `'===='`). `BASE64_LOOKUP` is a 64-entry bijection with `BASE64_ALPHABET`, so no second spelling of a sextet exists. The Orchestrator’s 8192 two-pad groups plus the writer’s byte-pair sweep close the unused-bit image of encode; the `third & 0x03` line is the same complete check for one-pad.

3. **CONFIRMED.** Attack that failed: boxed `String`, `Proxy`, hostile getters, `Uint8Array`, functions. `is*` is `typeof value === 'string' && decode*(value) !== undefined` — iff is tautological on strings and total-false on every non-string; `typeof` is `'object'` for a box or proxy, so hostile getters never run. Decode on a string does not throw (lookup miss returns `undefined`; `Uint8Array` length is `(n/4)*3 - padding ≥ 0`). Orchestrator: hostile guard values all false.

4. **CONFIRMED.** Attack that failed: forbidden tokens and a hidden import graph in the built artifacts. `dist/src/core/index.js` and `index.cjs` (built from `0524d8a` before the dist reading) contain no `atob` / `btoa` / `Buffer` / `TextEncoder` / `TextDecoder` / `node:` / `@orkestrel/` and no `import` / `require`. Both are single files; ESM `export { decodeBase64, decodeBase64URL, encodeBase64, encodeBase64URL, isBase64, isBase64URL }`, CJS assigns those six. A module cycle needs an import edge; these artifacts have none.

5. **CONFIRMED.** Attack that failed: mixed-alphabet smuggling and the `'AA'` / `'AA=='` split. `'AQ-/'` substitutes to canonical `'AQ+/'` but is refused by `includes('/')` before substitution; `'AQ+_'` / `'+/+/ '` / `'aGk='` die on `+` / `/` / `=`. `'AA'` is url-admitted (`remainder === 2` → `'AA=='` → `[0]`, re-encodes `'AA'`) and standard-refused; `'AA=='` is the reverse. `'AAB'` (url one-pad unused bits) becomes `'AAB='` and is refused. Residue 1 (`'A'`) becomes `'A==='` and is refused. The pre-check plus unique pad restoration (`length % 4` ∈ `{0,2,3}`) accepts exactly `encodeBase64URL`’s image.

6. **UNRESOLVED.** The RFC-transcription tests compare `encode*([value << 2]).charAt(0)` and `decode*(\`${RFC[value]}A==\`)` against independently written `RFC_STANDARD` / `RFC_URL`, so a swapped entry among the 64 would fail those tests. The two named mutation pins exist in source (`MEMBERSHIP` row `'aa=='`; guides `'documents every barrel export'`). Their fail counts (`3 failed | 33 passed`, `1 failed | 21 passed`) are writer-reported and not in the Orchestrator’s established list. Settle with those two mutations restored after the run: drop `if (tail === 2 && (second & 0x0f) !== 0) return undefined` and run `npm run test:src`; drop the `isBase64URL` surface row in `guides/codec.md` and run `npm run test:guides`. Instrument limit visible from reading (does not settle the counts): adding `'*': 0` to `BASE64_LOOKUP` would keep the suite green (`SWEEP_CHARACTERS` and the RFC literals omit `*`) while `decodeBase64('*AAA')` would return `Uint8Array([0, 0, 0])`.

7. **BROKEN.** `guides/codec.md` The laws: “missing or excess padding, a length off the four-character group boundary” as doors the canonical-form law rules out on **each** face. Failing input: `decodeBase64URL('aGk')` has length 3 (off the 4-char boundary, “missing” padding) and is defined — the same guide’s url fence states `'aGk'`. Smallest fix: qualify those doors as §4, and state §5’s spelling as unpadded url with residue 1 and any `=` / `+` / `/` refused.

8. **BROKEN.** (a) holds: `constants.ts` must export; architecture.md says the policy sweep does not decide barrel membership; intern + `INTERNAL` is the stranded-export shape. (c) holds: one walk, no second table, claim 5’s pre-check. (b) fails: `VECTORS`, `MEMBERSHIP`, `SWEEP` live in `tests/src/core/helpers.test.ts`. `.claude/rules/tests.md` places data tables in a setup file at any size. The writer’s reason (a new setup project / `package.json` script the brief forbade) is false: `tests/setup.ts` already exists and is the Vitest `setupFiles` entry. Smallest fix: move those tables into `tests/setup.ts` and import them from the test file.

9. **BROKEN.** `isBase64` and `isBase64URL` are total `Guard<string>` (`(value: unknown) => value is string`) in `helpers.ts`. `.claude/rules/architecture.md` kind table and `.claude/rules/patterns.md` place that shape in `validators.ts`; architecture.md’s repair is “wrong file, right name → move it” (`isVacant` stays in helpers only because it is a predicate, not a `Guard<T>`). Fleet parallel: `mcp/src/core/validators.ts` `isStandardBase64`. Names (`Base64URL` keeps `URL`), `{verb}{Noun}` / `is*`, no nested functions, TSDoc (`Encodes` / `Decodes` / `Checks whether`, `@param` / `@returns` / `@example` / `@remarks`), and absent `types.ts` (no package-owned public type) all held. Smallest fix: `src/core/validators.ts` with the two guards importing the decoders; barrel `export * from './validators.js'`.

10. **BROKEN.** `README.md` is still the scaffold stub (“The @orkestrel/codec package.”). `package.json` `files` ships `README.md` as the stranger’s front door; the real charter lives only in `guides/codec.md`, which the tarball does not include. TSDoc on the six exports is not a README. Smallest fix: replace the stub with the package description, the six exports, and a pointer at the repository guide. Throughput is not an acceptance blocker. `test:distribution` remains the verifier’s packed-tree run and is not this finding.

Findings fitting no claim: none.

`VERDICT: FAIL — 4 broken, 1 unresolved, 0 not-evidenced, 0 findings outside the claims`
