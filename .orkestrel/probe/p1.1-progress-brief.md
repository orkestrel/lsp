# Unit P1.1 — the lint stage restores its gauge before stage-owned cleanup

Role and engine: `implementer`, Claude Opus 5, native subagent working in `/home/user/probe` as
its sole writer. You perform this assignment directly and spawn nothing. The routing deviation is
on the record in `.orkestrel/lsp/l6-audit-reconciliation.md`: this objective unit runs natively
because its proof spawns the real language-server grandchild the bench sandbox denies.

Read before editing: the probe repository's `AGENTS.md`, its `.claude/rules/typescript.md` and
`.claude/rules/tests.md` files, the finding this unit closes — the MEDIUM finding in
`/home/user/lsp/.orkestrel/lsp/l6-audit-analyst-verdict.md` — and the ruling that carries it,
item 7 of `/home/user/lsp/.orkestrel/lsp/l6-audit-reconciliation.md`.

## Standing tree state, named so you do not stop on it

The probe tree is a held round and is DIRTY by design: the P1 adoption, the tarball swap
(`package.json:97` with its lockfile), the fixture capability patch, and the landed L6-E rewire.
Never revert, stash, or reset anything. The round commits as one commit after the Orchestrator's
gate chain. The installed `@orkestrel/lsp` tarball carries the ruled `open(document, { signal })`
contract.

## The finding, with the measured facts of 2026-08-26

`LintStage.#document` raises `this.#progress += 1` at admission
(`src/server/stages/LintStage.ts:197`) and never lowers it: the `finally` block awaits the
stage-owned `await client.close(uri).catch(...)` cleanup (`:213-218`) with the gauge still
raised. `Probe.#expiry` classifies an expiry as claimant-owned whenever `stage.progress` exceeds
its snapshot (`src/server/Probe.ts:588-592`). So a coordinator expiry landing while diagnostics
have settled and only the close write is pending reads claimant-owned work in flight and blames
the claimant for the instrument's own cleanup — against the public contract at
`src/server/types.ts:135-142`, which requires the gauge returned to its pre-inspection reading
before stage-owned awaited work starts. `RuntimeStage` is the named precedent that does this
before eviction and cleanup. The gauge also serves as the `didOpen` version
(`version: this.#progress` at `:203`), so restoring it needs the version decoupled first.

## Objective

An expiry during the lint stage's close cleanup classifies instrument-owned, the gauge returns to
its pre-inspection reading before that cleanup is awaited, and the document version stays
monotonic — proven red-first against the real language server.

## The work

1. **Split the version from the gauge.** Add a private monotonic revision counter for the
   `didOpen` `version` field, so the version no longer reads the gauge. Keep the counter's
   growth monotonic across every inspection of the stage's lifetime.
2. **Restore the gauge before the stage-owned cleanup.** In `#document`, lower `#progress` back
   to its pre-admission reading exactly once on every path — success, abort, and translation —
   BEFORE the `finally` block's `client.close` await starts. The raise-at-admission comment's
   promise stays true; extend it with the restore, per the contract's own wording.
3. **The rows, red-first.** In `tests/src/server/stages/LintStage.test.ts` (and
   `tests/src/server/Probe.test.ts` only if the stage-level seam cannot express the proof):
   - the gauge reads level with its pre-inspection value once the diagnostics wait has settled
     and while the close cleanup is still pending — red under the standing code, green after;
   - the version passed to `didOpen` increases across consecutive inspections;
   - the existing rows stay green — the coordinator refusal rows, the caller-bound rows, and the
     boot-expiry row must not change.
   Name each test for what it proves. Record the red and green readings with commands and exit
   codes. Use the real fixture or the real Oxlint server as the existing rows do; no mock, no
   fake clock.
4. **Prose.** Touch `guides/probe.md` only where this change makes a sentence false, and add only
   what the `guides` parity project obliges.

## Scope

Owned files: `src/server/stages/LintStage.ts`, `tests/src/server/stages/LintStage.test.ts`,
`tests/src/server/Probe.test.ts` (rows only — the fixture capability patch stays), and named
sentences in `guides/probe.md`. Report-only: `src/server/Probe.ts`, `src/server/types.ts`,
`src/server/helpers.ts`, `package.json`, `package-lock.json`, `node_modules/@orkestrel/lsp/`.
Off-limits: everything else. No commit, no push, no
`git checkout`/`restore`/`stash`/`reset`/`clean`, no installs, no tree-wide `format` or
`lint --fix` (scoped `oxfmt --write` over owned files only).

## Output

Your final report, written to `/home/user/lsp/tmp/units/p1.1-progress-report.md` and returned as
your final message: what changed per file with the gauge-restore point named; the red-first
records with commands and exit codes; scoped gate readings (`oxfmt --check`,
`oxlint --deny-warnings` over owned files, `npm run check` tree-wide, the acceptance run
`npx vitest run --project src:server tests/src/server/Probe.test.ts
tests/src/server/stages/LintStage.test.ts`, and the unfiltered
`npx vitest run --project src:server` as an observation with your own reading); `git diff --stat`
and `git status --short`; observations outside scope, each named against the unit that owns it.

## Deviation contract

A conflict with the ruled repair stops the unit: report expected, found, exact evidence, done or
not done, and at most one short hypothesis. Named stop conditions: the restore point cannot
precede the close await without changing `Probe.ts`; a coordinator or caller-bound row reddens
and its cause is not this change; the version split alters any wire assertion an existing row
pins. Counter naming, row titles, and the restore's exact placement inside `#document` are yours
to decide, record, and carry on from. Timing reds under concurrent load are reported with your
reading and carried to the Orchestrator's solo re-run, never diagnosed inside the unit.

## Acceptance criteria

Ordered cheap-first.

1. `git status --short` shows changes only in the owned files beyond the standing entries.
2. Scoped `oxfmt --check` and `oxlint --deny-warnings` over the owned files exit 0.
3. `npm run check` exits 0 tree-wide.
4. The acceptance run exits 0 with the red-first rows restored green and no existing row changed
   beyond what the report records with its reason.

## Review evidence

The Orchestrator captures the actual diff and status after you exit and routes the re-check to
the `analyst` lane (GPT-5.6 Sol), the engine that did not write this unit.
