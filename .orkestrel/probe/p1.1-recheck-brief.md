# Lane assignment — you hold ONE lane

You are the objective audit lane (`analyst`, GPT-5.6 Sol) for the P1.1 fix-round re-check
in `/home/user/probe`. You hold this one lane only. Do the work yourself, directly, in this
session. Do not simulate any other lane, and do not reconcile — the Orchestrator
reconciles. Your sandbox is read-only: you audit, you never edit, and you never accept.

# Re-check — the P1.1 gauge restore in the probe tree

The subject repository is `/home/user/probe` on the `claude/lsp-spec-audit-est33d` branch.
The whole working tree is a HELD UNCOMMITTED round: the P1 lsp adoption, the L6-E
deadline-signal rewire, and the P1.1 fix all sit uncommitted together. P1.1 wrote exactly
these files: `src/server/stages/LintStage.ts`, `tests/src/server/stages/LintStage.test.ts`,
and one sentence in `guides/probe.md`. Every other modified file in `git status` belongs to
the standing round, not to this unit.

P1.1 was written by Claude Opus 5 (native), repairing the MEDIUM finding your engine's
prior lane raised in the L6 round audit. You are the fix-round auditor because your engine
did not write the fix.

## Read first

- `/home/user/probe/AGENTS.md`, then the rules `.claude/rules/typescript.md`,
  `.claude/rules/tests.md`, and `.claude/rules/writing.md` in that repository. The
  Falsification law in `.claude/rules/quality.md` governs your verdict shape.
- The finding under repair: the MEDIUM finding in
  `/home/user/lsp/.orkestrel/lsp/l6-audit-analyst-verdict.md` — `LintStage.#document`
  raised `#progress` at admission and never restored it before the `finally` block's
  stage-owned `await client.close(uri)`, so `Probe.#expiry` could classify an expiry
  against a stage whose inspection had already settled; and the `didOpen` `version` read
  the gauge, coupling a wire field to an occupancy count.
- The unit record: `/home/user/lsp/.orkestrel/probe/p1.1-progress-brief.md` (the
  instruction), `p1.1-progress-report.md` (the writer's report with its red-first table),
  `p1.1-post-diff.txt` (the full held-tree diff, 2123 lines, captured 2026-08-26 after the
  unit), and `p1.1-post-status.txt` beside it.
- The live tree itself — read the current sources directly; the held tree is the subject.

## Orchestrator-supplied host evidence

Your sandbox cannot run the suite; these runs are the Orchestrator's, taken 2026-08-26 on
the host after the unit exited and after the `@orkestrel/lsp` tarball was rebuilt from that
package's `main` at `94cc205` and reinstalled into this consumer:

- `npx vitest run --project src:server tests/src/server/Probe.test.ts
  tests/src/server/stages/LintStage.test.ts` — exit 0, `Test Files 2 passed (2)`,
  `Tests 55 passed (55)`, duration 159.49s.
- `npx vitest run --project src:server` — exit 0, `Test Files 7 passed (7)`,
  `Tests 168 passed (168)`, duration 173.99s.

The writer's own red-first readings are in its report; treat them as the writer's claims,
not as settled fact — the diff and the sources are what you rule from.

## The claims, numbered and falsifiable — rule on each with evidence

1. `LintStage.#document` lowers `#progress` as the first statement of its `finally` block,
   before `#documents.delete(uri)` and before the stage-owned `await client.close(uri)`,
   and each settlement path — the diagnostics return, the caller-bound abandonment, and
   the translated fault — restores the gauge exactly once. No path restores twice and no
   path skips the restore.
2. The `didOpen` `version` field reads a monotonic `#revision` raised at admission, and the
   gauge no longer reaches the wire. Consecutive inspections put strictly rising versions
   on the wire.
3. With the restore in place, `Probe.#expiry`'s claimant classification
   (`stage.progress > snapshot`) no longer attributes an expiry to a stage whose
   inspection has settled while its close cleanup is pending — the scenario the MEDIUM
   finding named. The repair needed no edit to `src/server/Probe.ts` beyond what the
   standing round already carries, and the diff shows none by this unit.
4. The added rows bind to the defect: the pending-close row constructs a genuinely
   unsettled `didClose` write through the `stall` fixture rather than a timing guess, and
   the version row asserts rising wire versions. Each row's title states the behavior it
   proves, and the fixture, the helpers, and the constants the unit added contain no mock,
   fake, spy, module replacement, or fake clock.
5. The writer's red-first record is coherent: the pending-close row fails against the
   standing code for the reason the finding predicts, the version row's red is honestly
   recorded against the restore-only intermediate state with the reason the report states,
   and the green readings match the rows as written.
6. The sentence added to `guides/probe.md` is true of the shipped code and survives the
   writing rules, and nothing else in the guide became false through this unit's changes.
7. The unit stayed in scope: no file outside `src/server/stages/LintStage.ts`,
   `tests/src/server/stages/LintStage.test.ts`, and `guides/probe.md` changed by this
   unit, judged against the unit report and the recorded diffs.

## Output

Return, as your final message, a verdict block per claim — CONFIRMED TRUE, CONFIRMED
FALSE, or UNPROVABLE with the exact evidence read (file and line, or record section) —
followed by any finding outside the claims with its severity and owner, and ONE terminal
line: `VERDICT: PASS` when every claim confirms true, otherwise `VERDICT: FAIL`. No
process diary.
