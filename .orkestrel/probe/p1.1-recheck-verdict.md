1. **CONFIRMED TRUE**

`LintStage.#document` raises the gauge at [LintStage.ts](/home/user/probe/src/server/stages/LintStage.ts:204). Its `try` covers the diagnostics return at line 216, caller-bound abandonment at line 218, and fault translation at line 219. The `finally` block lowers the gauge first at line 221, deletes the URI at line 222, and awaits `client.close(uri)` at line 225. JavaScript runs this single `finally` block exactly once for every path that passes admission.

2. **CONFIRMED TRUE**

Admission raises the separate `#revision` field at [LintStage.ts](/home/user/probe/src/server/stages/LintStage.ts:205), and `didOpen.version` reads it at line 211. The `#revision` field starts at `0` and never falls at lines 69–72. The `#progress` field no longer reaches the document sent to `client.open`. The fixture records the received wire value at [LintStage.test.ts](/home/user/probe/tests/src/server/stages/LintStage.test.ts:113), and the consecutive-inspection row asserts the later value is greater at lines 534–551.

3. **CONFIRMED TRUE**

`Probe.#expiry` compares `stage.progress` with its admission snapshot at [Probe.ts](/home/user/probe/src/server/Probe.ts:578), assigning `claimant` only when the gauge is higher at line 589. During pending close cleanup, `LintStage` has already restored the gauge at [LintStage.ts](/home/user/probe/src/server/stages/LintStage.ts:221). The live `Probe.ts` behavior therefore classifies that window as `instrument`. The P1.1 report states that `Probe.ts` was report-only and unchanged by this unit in `p1.1-progress-report.md`, “Shared-file patches,” lines 148–153.

4. **CONFIRMED TRUE**

The `stall` fixture reads only the initialization frame and then stops consuming standard input at [LintStage.test.ts](/home/user/probe/tests/src/server/stages/LintStage.test.ts:65). It publishes diagnostics independently at lines 82–85. The pipe instrument verifies that the first write settles while the close-sized following write remains pending at lines 268–309. The regression row observes admission, waits for publication, confirms the inspection remains unsettled, and reads the restored gauge at lines 472–525. Because the restored gauge proves `client.open` left its `try`, the remaining awaited operation is `client.close` at [LintStage.ts](/home/user/probe/src/server/stages/LintStage.ts:225). The version row reads recorded protocol values at [LintStage.test.ts](/home/user/probe/tests/src/server/stages/LintStage.test.ts:549). Its title states the rising-wire-version behavior at line 534. The added fixture, constants, and helpers contain no mock, behavioral fake, spy, module replacement, or fake clock.

5. **CONFIRMED TRUE**

The red record is coherent with the code transitions in `p1.1-progress-report.md`, “Red-first records,” lines 48–60. Under the standing implementation, the absent decrement leaves the pending-close reading at `1`, matching the recorded `expected 1 to be +0` failure. Under the restore-only state, each inspection raises the restored gauge from `0` to `1`, so gauge-derived versions repeat and match the recorded `expected 1 to be greater than 1` failure. With `#revision` supplying the wire value, the supplied host acceptance run reports exit `0`, `Test Files 2 passed (2)`, and `Tests 55 passed (55)`; the server-project run reports exit `0`, `Test Files 7 passed (7)`, and `Tests 168 passed (168)`.

6. **CONFIRMED TRUE**

The added sentence at [probe.md](/home/user/probe/guides/probe.md:144) says the lint-stage test fills the pipe, holds `didClose`, and observes a level gauge. The fixture and regression row establish those facts at [LintStage.test.ts](/home/user/probe/tests/src/server/stages/LintStage.test.ts:472). The surrounding guide still distinguishes this stage-seam proof from the unexecuted composed `Probe` expiry proof at [probe.md](/home/user/probe/guides/probe.md:147). The added sentence contains none of the prohibited writing substitutions.

7. **CONFIRMED TRUE**

The unit report names only `guides/probe.md`, `src/server/stages/LintStage.ts`, and `tests/src/server/stages/LintStage.test.ts` as unit-written files in `p1.1-progress-report.md`, “Tree state,” lines 101–120. `p1.1-post-status.txt`, lines 1–10, matches the held standing-round population supplied in the brief. The captured diff places the P1.1 guide sentence at `p1.1-post-diff.txt:23–25`, the source repair at lines 428–431 and 666–698, and the fixture and regression additions at lines 1074–1077, 1100–1139, and 1347–1434. No unit record assigns a P1.1 edit outside the allowed files.

**Findings outside the claims**

None.

VERDICT: PASS