# Audit brief: h2.2-prose (Claude Opus 5 implementer) — objective lane

## Role and engine

You are the `analyst` route: GPT-5.6 Sol, objective correctness and constraint audit. You did not
write this unit. Attempt refutation of every claim below; do not confirm by default.

## Objective

Rule PASS or FAIL on the numbered claims below, per the `orkestrel-falsify` skill's verdict shape.
This is an audit of a held, uncommitted working tree in the current checkout — you read it directly.

## Context

- Read, in order: `AGENTS.md`, `.claude/rules/quality.md` (especially Falsification),
  `.claude/rules/typescript.md`, `.claude/rules/writing.md`, `.agents/skills/orkestrel-falsify/SKILL.md`
  and its `references/brief.md` and `references/reconcile.md` if present in this checkout, and
  `guides/markdown.md`.
- The subject is the `h2.2-prose` unit, a Claude Opus 5 native implementer, not written by you.
- The working tree (this checkout, `/home/user/markdown`) is HELD uncommitted at commit `d310eea`
  on branch `claude/lsp-spec-audit-est33d`, with working changes in exactly three files:
  `guides/markdown.md`, `src/core/types.ts`, `src/core/Markdown.ts`. The last is a one-hunk
  `@returns` TSDoc patch the unit returned for serial integration, applied verbatim by the
  Orchestrator.
- Your sandbox is `read-only`. You verify by reading the held sources against the diff and the
  supplied executed readings below. You spawn nothing and edit nothing.

### Evidence slice (read these; paths outside this checkout, absolute)

- `/home/user/lsp/.orkestrel/markdown/h2.2-prose-brief.md` — the unit's charter.
- `/home/user/lsp/.orkestrel/markdown/h2.2-prose-report.md` — the unit's report: the full `git diff`,
  the `git status --short` output, and executed probe readings Q1 through Q7 behind every ruling.
- `/home/user/lsp/.orkestrel/markdown/h2-audit-reconciliation.md` — rulings R1 and R5, and the
  Amendment dated 2026-08-26 fixing the derivation resolution order for a `MarkdownDerivation`'s
  region: own region, else the direct input's region, else `undefined` — never a further edge.
- The held sources in this checkout: `guides/markdown.md`, `src/core/types.ts`,
  `src/core/Markdown.ts`.

### Supplied executed reading (Orchestrator-run, do not re-derive; cite verbatim if you use it)

Instrument: a `tmp/probe` Vitest test driving the real `projectSpan` through the `@src/core` barrel,
run 2026-08-26, exit 0, swept after the run. The rival readings `{"start":5,"end":5}` and
`{"start":1,"end":1}` would have printed differently had either been the true boundary rule.

```text
segments [{offset 0, start 0, end 1}, {offset 1, start 5, end 5}, {offset 1, start 9, end 10}]
projectSpan 1..1 -> {"start":9,"end":9}
projectSpan 1..2 -> {"start":9,"end":10}
projectSpan 0..2 -> {"start":0,"end":10}
```

This is supplied evidence for the report's flagged claim about the `MarkdownSegment` boundary rule
under cascading zero-extent segments. You did not run this instrument and cannot re-run one (your
sandbox is read-only); treat it as a measurement, not as something to reproduce yourself. Where a
claim rests on it, cite it as the evidence; where a claim rests on source reading instead, label
your conclusion a derivation rather than a measurement.

## Unknowns

None specified. If you find a claim's evidence insufficient to decide, return `UNRESOLVED` naming
what would settle it — do not guess.

## Scope

- Read-only. No owned, shared, or off-limits files to edit — you edit nothing.
- Allowed: reading every file named above, plus any file in this checkout your reading requires.
- Off-limits: writing, running a probe, running git, or spawning any process.

## Execution

Read this brief and the evidence it names, then rule. Perform the audit directly inside your own
CLI; spawn nothing. Your final message must be exactly the verdict this brief specifies — the
numbered verdicts, any findings fitting no claim, and the single terminal `VERDICT:` line. No
process diary, no summary of what you read.

## Deviation contract

A missing evidence file, an authority this brief cites that does not exist in this checkout, or a
held-tree state that does not match this brief's description (a fourth modified file, a diff that
does not match the report's) stops the audit on that point: report it as a deviation with the exact
mismatch, and still rule on every claim your available evidence can decide.

## Numbered falsifiable claims

1. Every corrected sentence in `src/core/types.ts` — covering `MarkdownSpan`, `MarkdownSegment`,
   `MarkdownSource`, `MarkdownParseResult`, `MarkdownDerivation`, and `MarkdownInterface.span` — is
   true of the shipped code in this held tree.
2. Every corrected sentence in `guides/markdown.md` that the report enumerates as changed, including
   the two sites the report names outside its original population at `:192` and `:229`, is true of
   the shipped code.
3. The report's overturning of the h2.1 reviewer's claim-5 ruling is correct: the separate-inputs
   clause of the old `guides/markdown.md:316-319` was false by the report's Q5b reading, and the
   corrected sentence states the fixed resolution order from the Amendment (own region, else the
   direct input's region, else `undefined`, never a further edge).
4. The added TSDoc openers in this tree satisfy the third-person-verb rule in
   `.claude/rules/typescript.md`.
5. The integrated `src/core/Markdown.ts` hunk matches the report's exact patch byte-for-byte in
   content, and its corrected sentence is true of the shipped code.
6. Scope honesty: the diff in the report matches this held tree, and `git status --short` here
   shows exactly the three named files and no others.
7. The added prose in `guides/markdown.md` passes the substitution table and the count-ban sweep of
   `.claude/rules/writing.md` — name the pattern and the population your sweep covered, including a
   clean result.

## Output

Exactly the `orkestrel-falsify` verdict shape: one numbered verdict per claim above (value from
`CONFIRMED` / `BROKEN` / `UNRESOLVED` / `NOT-EVIDENCED`, each with its required evidence), any
findings fitting no claim substantiated to the `BROKEN` standard, and one terminal line:

```text
VERDICT: PASS — <m> of <m> confirmed, no findings outside the claims
```

or

```text
VERDICT: FAIL — <n> broken, <u> unresolved, <e> not-evidenced, <x> findings outside the claims
```

## Acceptance criteria

- Every claim above receives exactly one verdict value with evidence.
- The terminal line is present, matches one of the two forms, and its counts match the numbered
  verdicts plus any findings outside the claims.
- No process diary.

## Review evidence

This dispatch supplies: the actual diff and actual `git status` (via the report and this checkout,
per the code-change row), and the executed probe reading above as corroborating measurement for the
flagged boundary-rule claim.
