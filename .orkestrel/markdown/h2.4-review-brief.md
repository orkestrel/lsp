# Unit brief: h2.4-review — the fix-round review of h2.4-mechanical

## Role and engine

`reviewer` — Opus 5, native subagent, read-only (Read, Grep, Glob; no shell, no Write).
You perform the assignment directly and spawn nothing. You are the fix-round auditor whose
engine did not write the unit.

## Objective

Rule on each numbered claim about the h2.4-mechanical commit (markdown `3710f65`) with
evidence, attempting refutation rather than confirmation. Your lane carries the subjective
half — documentation fidelity, derivation shape, guide voice — beside the mechanical rows.

## Context

- Subject tree: `/home/user/markdown`, committed at `3710f65` on
  `claude/lsp-spec-audit-est33d`.
- The unit's brief and report: `/home/user/lsp/.orkestrel/markdown/h2.4-mechanical-brief.md`
  and `h2.4-mechanical-report.md`. The rulings it executes:
  `/home/user/lsp/.orkestrel/markdown/h2-audit-reconciliation.md` § R5 and § R6.
- Supplied executed evidence (label readings from it as supplied): the commit's full diff
  at `/home/user/lsp/tmp/units/h2.4-review-evidence-diff.txt`. The Orchestrator's gate run
  after the writer exited, 2026-08-26: scoped format exit 0 with the guide included, scoped
  lint exit 0, `npm run check` exit 0, `src:core` `602 passed (602)`, `test:guides`
  `18 passed (18)`, `grep -c 'limit' src/core/parsers.ts` reporting 0, `tmp/probe/` empty.
- Rules: `/home/user/markdown/AGENTS.md`; `/home/user/markdown/.claude/rules/` —
  `typescript.md` (TSDoc), `names.md`, `writing.md`.

## Claims

1. `splitTableRow` in `src/core/helpers.ts` derives its result from `splitTableSources`
   and re-implements none of the escaped-pipe splitting rule; its TSDoc still states its
   own contract truthfully; and no other helper retains a duplicate of the rule.
2. The `end` rename in `parseBlocks` and `collectList` (`src/core/parsers.ts`) is complete
   — declaration, internal reads, TSDoc — and the `guides/markdown.md` rows documenting the
   parameter describe `end` accurately in the guide's register.
3. The TSDoc ported onto `scanLink` and `scanEmphasis` is faithful to what their
   `locateLink` and `locateEmphasis` twins keep — description, `@example`, and the `depth`
   degrade note — adapted correctly to each leaf's actual signature and return shape, and
   the `scanInlineSource` `@example` readings are consistent with what the source returns.
4. The `## Methods` table region of `guides/markdown.md` changed in padding only — the cell
   text is unchanged.
5. Every added or changed prose line in the diff obeys the writing rules and reads in the
   file's established voice.

## Execution

Attempt refutation of each claim against the current tree and the supplied diff. A claim
you cannot break is CONFIRMED with the evidence that convinced you; a broken claim is
BROKEN with the exact site and the smallest correct fix.

## Output

Return the verdict as your final message — per-claim rulings with `file:line` evidence,
then one terminal line in the form `VERDICT: PASS` or `VERDICT: FAIL — <broken claims>`.
The Orchestrator persists it; write no file.
