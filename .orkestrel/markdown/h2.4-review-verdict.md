# h2.4-review verdict — persisted by the Orchestrator

The `reviewer` role (Opus 5, native, read-only) returned this verdict as its final message
on 2026-08-26; the read-only lane writes no file, so the Orchestrator persisted it
verbatim. Subject: markdown commit `3710f65` (the h2.4-mechanical fix unit), against the
brief `h2.4-review-brief.md` and the supplied diff evidence.

---

Lane held: subjective (design fit, documentation fidelity, derivation shape, guide voice),
as the fix-round auditor whose engine did not write h2.4-mechanical. No shell in this lane,
so every ruling rests on the tree, the supplied diff, and the supplied gate readings,
labeled as supplied where leaned on.

## 1. `splitTableRow` derives from `splitTableSources`, keeps no duplicate rule, TSDoc truthful — CONFIRMED

- `src/core/helpers.ts:447-449` is the whole body:
  `return splitTableSources({ text: row, segments: [] }).map((cell) => cell.text)`. No
  character loop survives.
- The escaped-pipe rule has one site. A sweep for the literal `'|'` across `src/core`
  returns `helpers.ts:470`, `:474`, `:481` (inside `splitTableSources`), plus
  `helpers.ts:1094` and `validators.ts:195`, which are `includes('|')` table detection
  rather than splitting.
- The TSDoc's retained contract still holds under the derived body: `splitTableSources`
  trims through `trimSource` (`helpers.ts:464`, `:262-266`, equivalent to the removed
  `row.trim()`), turns `\|` into a literal `|` (`:470-479`), and drops a leading or
  trailing cell whose trimmed text is empty (`:489-495`) — the sentences at `:434-437`.
- The added sentence at `:436-437` names the owner truthfully and opens in third person.
- Refutation attempted and failed: the derived form takes a `MarkdownSource` with no
  segments, and `sliceSource` (`:157`) and `joinSources` (`:174-199`) compute text
  independently of segments, so the string result cannot diverge on the segment path.

## 2. The `end` rename is complete, and the guide rows describe it accurately — CONFIRMED

- Parameter, reads, and TSDoc: `parsers.ts:38`, `:50`, `:96`, `:144`, `:158`;
  `helpers.ts:1120`, `:1133`, `:1242`.
- A case-insensitive `limit` sweep over `src/core` returns nothing in `parsers.ts`,
  matching the supplied `grep -c` reading of 0. The same sweep over `tests/` and the
  package's `*.md` files returns no hit in the boundary sense.
- Guide rows: `guides/markdown.md:64` states "`end` retains an unclosed fence's source
  end", which `parsers.ts:96` does exactly; `:100` states `collectList` "propagates its
  continuation sources and source end through `parseBlocks`", which `helpers.ts:1242-1245`
  does. Both rows keep the table's register.
- Correction to the claim's own text, not to the work: `collectList` lives in
  `src/core/helpers.ts:1128`, not in `parsers.ts` as the claim's parenthetical says. The
  rename landed in the right file.

## 3. The ported scan-leaf TSDoc is faithful and correctly adapted — CONFIRMED

- `scanLink` (`helpers.ts:727-745`) carries `locateLink`'s shape rule verbatim from
  `:667-670`, adapted to the leaf's return; the degrade clause matches the twin.
- `scanEmphasis` (`:835-854`) carries `locateEmphasis`'s rule from `:761-766`, the
  flanking simplification included.
- The `depth` notes are true of the code, not just of the twin: `scanLink` forwards
  `depth + 1` at `:756`, `scanEmphasis` at `:868`, and `scanInlineSource:943-949` emits
  the window as one literal text node at `MAX_DEPTH`; the `{@link MAX_DEPTH}` import sits
  at `:30`.
- The fences are consistent with what the source returns: `scanLink('[text](url)', 0, 11)`
  and `scanEmphasis('*em*', 0, 4)` match the construction sites member for member, and
  `scanInlineSource`'s fence passes a source whose segment span matches its text, with the
  pending text node emitted before the emphasis node.
- The abbreviated emphasis member follows the file's own established form at `:893` and
  `:1125`.

## 4. The `## Methods` region changed in padding only — CONFIRMED

- The hunk at `guides/markdown.md:184` is balanced (16 lines to 16 lines).
- Each minus line against its plus line: the header and every method row are byte-identical
  in cell text and differ only in trailing pad width; the separator row differs only in
  dash count; the `span` row is unchanged context.
- The supplied scoped format run at exit 0 with the guide included corroborates the region
  now matches the formatter's own output.

## 5. Added and changed prose obeys the writing rules and the file's voice — CONFIRMED

- Population: every line the diff adds in the two source files and the guide. Pattern: the
  substitution table case-insensitively, plus an unspaced-em-dash check and the
  third-person-opener rule.
- No substitution-table term appears in an authored line. The only `via` hits in the two
  source files are `helpers.ts:2669` and `parsers.ts:193`, both outside every hunk.
- Openers are third person: "Derives" (`:436`), "Scans" (`:727`, `:835`).
- `both` at `:728` and `:837` is ported verbatim from the twins and each sentence names its
  members, which the writing law permits.

## Findings outside the claims

- **`itemLimit` keeps the boundary synonym alive inside the function R6 named.**
  `helpers.ts:1242` declares it and `:1245` passes it as the `end` argument. Right looks
  like: rename to `itemEnd` at both lines; a function-local, no published surface moves.
- **`scanEmphasis`'s description carries a second dash its sibling resolves with a comma.**
  `helpers.ts:839` reads `{@link locateEmphasis} - and returns`, against `scanLink`'s
  `, and returns` at `:729-730`. Right looks like: replace ` - and returns` with
  `, and returns`.
- **Pre-existing `via` survives in the guide and in source, with no carrier**:
  `guides/markdown.md:158`, `:194`, `:227`, `:453`, `src/core/helpers.ts:2669`,
  `src/core/parsers.ts:193`. The `:194` row appears in the diff only because the padding
  pass re-emitted the line byte-identical. Record against the vocabulary sweep that owns
  the guide.
- **`splitTableRow`'s TSDoc opener stays imperative** (`:434` "Split one GFM table row"),
  as does `delimiterToAlignments` at `:500` — a file-wide pre-existing condition rather
  than drift this commit introduced.

## Referral to the objective lane

- Runtime equivalence of the derived `splitTableRow` on inputs the unit's probe list does
  not name: a row ending in a lone backslash (`'a|b\\'`) and a row whose interior cell is
  whitespace-only (`'|a|  |b|'`). Reasoned equal, not executed — this lane has no shell.

VERDICT: PASS
