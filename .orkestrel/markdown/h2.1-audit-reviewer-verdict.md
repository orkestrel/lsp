# Audit verdict — unit `h2.1-derive`

Lane held: **subjective** (design fit, shape, vocabulary, prose truth), Claude Opus 5, native, read-only, cross-engine against Sol's fix. No execution tool; every behavioral reading is the Orchestrator's, from `/home/user/lsp/.orkestrel/markdown/h2.1-audit-evidence.md`. Source read at `d310eea` in `/home/user/markdown`.

## 1. The implementation is exactly the stated rule — CONFIRMED

`/home/user/markdown/src/core/Markdown.ts:205-222` resolves each output with one own-span read and at most one derivation hop; the `visited` set and the `while` loop are gone, so no cycle is reachable and no second edge is followed.

Attacks that failed:

- **A remaining multi-hop path elsewhere in the file.** I read all of `Markdown.ts`. `#derive` is the only span-resolution path; `span` (`:88-91`) reads `#spans` directly and the constructor (`:55-64`) copies the parse map. Nothing else touches provenance.
- **A recorded edge that reintroduces a hop.** Every `derivations.set` site in `rewriteDocument` is `helpers.ts:3041` (`rebuilt → current`), `:3075` (`accepted → undefined`), and `:3076` (`accepted → current`). `current` is always drawn from the stack, which is seeded only from the operation's input tree (`:2864`, `:2904-2907`), so every recorded value is an input-tree node or `undefined`. A rebuilt node is pushed to `values` (`:3078`), never to `stack`, so it can never be a `current` and therefore never a direct input. An input-tree node's span is already fully resolved in the source handle's map, so a second hop can carry no information the first cannot.
- **A second consumer of the map.** `rewriteDocument` has exactly one in-package caller, `Markdown.map` at `:143`.

Bounding observation: the new resolution is the first two iterations of the removed loop, so its answers are a strict subset of the old ones. It can remove an answer; it cannot invent one.

## 2. The counterexample resolves as ruled, through the public API — CONFIRMED

The row at `/home/user/markdown/tests/src/core/Markdown.test.ts:265-294` reproduces R2's recipe verbatim — parse `a\n\nb\n\nc`, map the first and second paragraphs to one shared identity, then chain a map replacing that identity and moving it onto the third — and drives it through `Markdown`, `map`, and `span` imported from the `@src/core` barrel (`:1-13`).

Attacks that failed:

- **Assertion re-derives the source's own answer.** It does not. It asserts fixed literals (`toBeUndefined`, `{ start: 6, end: 7 }`) and pins output identity with `toBe` at `:289-291`, so it would fail for any other resolution the source could produce.
- **The row is insensitive.** Tracing the pre-fix walk over this construction: `replacement`'s own span is absent, its direct input `shared` has no span in `merged`, and `derivations.get(shared)` is the third paragraph, whose span is `{6,7}` — exactly the `{ start: 6, end: 7 }` received in the revert control's failure message. The evidence record's revert reddened this row and only it plus the abutment row (`Tests 2 failed | 345 passed (347)`).
- **The precondition is doing the work.** `merged.span(shared)` at `:279` is `undefined` under both the old and the new code, so the red came from `chained.span(replacement)` at `:292` — the assertion the ruling names.
- **The construction misses the conflated-role seam.** It does not: `shared` holds both roles in one operation, as `derivations` records `replacement → shared` and `shared → third` simultaneously.

## 3. No refusal was widened into a regression by the `projectSpan` change — CONFIRMED

The added guard at `/home/user/markdown/src/core/helpers.ts:237` sits inside the `from === to` branch, so it cannot reach the `start`/`end` accumulation a nonzero-width span uses.

Attacks that failed, each traced against the documented ascending-`offset` segment contract at `src/core/types.ts:92-97`:

- **A nonzero-width span starting at a boundary.** For the row's own source, `projectSpan(source, 1, 2)` skips the zero-width branch entirely and still returns `{ start: 5, end: 6 }` through `:242-245`. Unchanged.
- **A zero-width position interior to a segment.** `limit <= next.offset` always, so `from === next.offset` forces `from === limit`; an interior position has `from < limit` and never trips the guard.
- **A skip that falls off the end into `undefined`.** The skipped position equals the next segment's `offset`, and that segment accepts every `from` at its own offset, so the loop always resolves rather than reaching the `undefined` fallback. A zero-derived-extent successor cascades the skip and terminates at the last segment through the `next === undefined` arm.
- **A silently changed parse-path answer.** `joinSources` records its separator run as `{ start: left.end, end: right.start }` (`helpers.ts:190`), so the earlier segment's `end` already equals the later segment's `start` at every contiguous junction — the old and new answers coincide there. Only a genuinely discontinuous abutment moves, which is R3's ruling.
- **A `sliceSource` caller.** `sliceSource` (`:127-158`) carries its own boundary arithmetic and never calls `projectSpan`; the diff does not touch it. Its callers are unaffected by construction rather than by test.

The Orchestrator's `Tests 592 passed (592)` bounds the standing suite; the preceding cases are the ones it does not pin.

## 4. The writer's flagged claim holds — CONFIRMED, with the search bound stated

I could not construct the refutation and I can say why the space is closed rather than merely unsearched.

A same-operation edge `D → E` exists only where `rewriteDocument` recorded `D` as an output, which happens only at `:3041` or `:3073-3076`. The guard at `:3073` requires `accepted !== current`, so an identity re-emitted at its own slot records no edge at all. Therefore any `D` carrying an edge is an identity holding a **second** role as an output elsewhere, and `E` is that other slot's input — never a further source for the subject node's role. Where `D` has no span in the source handle, `D`'s input role genuinely has no region, and taking `E`'s region is precisely the R2 crossing.

The one shape that looked promising and did not refute: a rebuilt parent `R` (`derivations.set(R, P)` at `:3041`) re-emitted by the handler at a later slot. That path reaches `:3074`, finds a conflicting entry, and overwrites `R`'s edge with `undefined`. Both the pre-fix and post-fix code then answer `undefined` for `R`, so the loss is `rewriteDocument`'s round-landed ambiguity rule rather than anything this unit changed, and the answer is conservative rather than wrong.

Search bound: I enumerated the recording sites in `rewriteDocument` and the reachability of each through `Markdown.map`, the only in-package caller. I did not attempt a foreign consumer walking the exported `MarkdownDerivation` map itself, which is outside this unit and is R1's prose territory.

## 5. The self-declared prose verdict holds — CONFIRMED

Read against the shipped behavior:

- `src/core/types.ts:416` — "a node mapped to an input node takes that input's span." Under the shipped rule this is true whenever the input has a span, and where the input has none the output takes none. The pre-fix code was strictly worse here: it took a span from beyond that input. The change moves the sentence toward truth, not away from it. Its remaining imprecision — the own-span precedence — is R1's enumerated item at `h2-audit-reconciliation.md:41-42` and predates this unit.
- `src/core/types.ts:417-418` — "mapped to `undefined` … has no span." Matches `:217` exactly.
- `src/core/types.ts:419-420` — the absent-entry sentence. Already ruled false by R1 for synthetic descendants; unchanged by this unit and carried by `h2.2-prose`.
- `guides/markdown.md:316-319` — "an output derived from one input node takes that input's region, and an output derived from separate inputs takes none." This sentence describes the direct-input rule and nothing wider; the pre-fix code was the party that contradicted it.
- `src/core/Markdown.ts:201-204`, the rewritten private comment. It states the shipped rule and its reason, uses the `//` form the TypeScript rule requires of a private method, and explains why rather than restating the code.

Attack that failed: the class TSDoc at `src/core/Markdown.ts:24-31` says "a one-source replacement takes the region of the node it replaced," and `chained.span(replacement)` is now `undefined`. That is not a falsification — `replacement` replaced `shared`, whose region is absent, so it takes an absent region, and the same paragraph already declares that an output built from separate sources reports `undefined`. No sentence in an owned file promises a resolution the rule now refuses.

## 6. The held round survived intact — CONFIRMED

Comparing `h2.1-derive-diff.txt` against `h2-round-diff.txt`:

- The only deletions anywhere in the unit's diff are the old `#derive` comment and walk in `src/core/Markdown.ts`. Both are round-landed lines (`h2-round-diff.txt:467-483`), and replacing them is exactly what R2 instructs.
- No test file hunk removes or loosens an assertion; both test files receive additions only, appended after existing rows.
- The round's `projectSpan` suite (`h2-round-diff.txt:2513-2544`) survives untouched at `tests/src/core/helpers.test.ts:230-273`, with the new row inserted between existing ones.
- The round's provenance rows at `tests/src/core/Markdown.test.ts:220-263` — including "gives a one-source replacement its source region" and "resolves a moved original to its own region" — stand unedited and pass, which is what keeps the fix from over-correcting into "own region or nothing."

## Findings outside the claims

None substantiated.

## Referral to the Orchestrator

The fix-round exit criterion in `/home/user/lsp/.orkestrel/markdown/h2-audit-reconciliation.md:136-138` reads "the derivation chain answers only with the subject node's own region or `undefined`." Taken literally, that forbids a one-source replacement from inheriting its input's region — which the guide fixes as contract at `guides/markdown.md:276-277` and `:318`, and which the round-landed row at `tests/src/core/Markdown.test.ts:228-240` pins as `{ start: 9, end: 13 }`. The shipped rule follows R2's ruling body instead (`:56-63`: "the resolution must not cross that boundary," with the exact rule delegated to the writer), and I judge the ruling body correct and the criterion sentence loose.

This matters beyond bookkeeping because `h2.2-prose` and `h2.3-fences` write and then execute prose from this charter. A prose unit reading the literal criterion would document "own region or none" and falsify a shipped, tested behavior. Correct the criterion sentence to name the direct-input rule before `h2.2-prose` launches. This is the Orchestrator's document, not the unit's work, so I return it as a referral rather than a verdict.

VERDICT: PASS — 6 of 6 confirmed, no findings outside the claims
