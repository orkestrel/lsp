# Unit brief: h2.5-gates — the H2 round's authoritative gate sweep

## Role and engine

`verifier` — Sonnet, native subagent (Read, Grep, Glob, Bash; no edit or write tools). You
perform the assignment directly, spawn nothing, and fix nothing.

## Objective

Run the authoritative tree-wide gate chain in `/home/user/markdown` and report exit-code
truth.

## Context

- Subject tree: `/home/user/markdown`, committed clean at `3710f65` on
  `claude/lsp-spec-audit-est33d`. Confirm `git status --short` is empty before running and
  report any dirt as a finding, not something to clean.
- The chain is the acceptance gate from `/home/user/markdown/AGENTS.md`: the non-mutating
  variants, in order. Run each command bare — no pipeline stage after a gate command; capture
  its exit code directly.

## Execution

Run, in order, from `/home/user/markdown`:

```text
npm run format:check
npm run lint:check
npm run check
npm run build
npm test
```

Continue through the chain even when a gate reds, so the report carries every reading.

## Output

Write nothing to the tree. Return as your final message: each command with its exit code,
the summary line of every test project (files and tests counts), and for any nonzero exit
the exact failing excerpt verbatim. End with one line:
`GATES: GREEN` when every exit is 0, else `GATES: RED — <commands>`.
