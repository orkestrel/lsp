# Unit brief: h2.3.2-prose — the subjective lane's voice repairs

## Role and engine

`implementer` — Opus 5, native subagent. You write in `/home/user/markdown`, the sole
writer in that checkout, from the clean committed baseline `bda6e1e` on branch
`claude/lsp-spec-audit-est33d`. You perform the assignment directly yourself and spawn
nothing. Documentation-voice work is this unit's whole substance.

## Objective

Close every finding the h2 round's subjective review broke: the register-breaking bold
lead, the plain-language failures, and the "came from"/"produced from" vocabulary split.
The guide and the TSDoc read in one voice with the corrected semantics unchanged.

## Context

- Read before editing: `/home/user/markdown/AGENTS.md` § Writing;
  `/home/user/markdown/.claude/rules/writing.md`; `.claude/rules/typescript.md` § the
  TSDoc rules; `.claude/rules/documentation.md`; no skill (explicit none).
- The findings come from the subjective review verdict at
  `/home/user/lsp/.orkestrel/markdown/h2-round2-review-verdict.md`. Each deliverable
  below restates its finding; read the verdict for the full reasoning. Change no
  semantics: every corrected sentence keeps the rule the round measured, and the pinning
  rows in `tests/src/core/` are off-limits and must stay green unchanged.
- Verify each site's current text before editing it; a site whose text differs from the
  finding's quote is a deviation, not a guess.

## Scope

- Owned: `guides/markdown.md`, `src/core/types.ts` (TSDoc text only), and
  `src/core/Markdown.ts` (TSDoc and comment text only).
- Off-limits: every declaration and runtime line in the owned source files, all of
  `tests/`, and everything else. A change that moves a runtime token is a deviation.

## Deliverables

1. **The bold lead at `guides/markdown.md:282`.** Replace the seven-word subject whose
   relative clause collides with the verb (`A node this handle holds no region for
   reports ...`) with a bare-noun subject carrying the condition after the verb, in the
   sibling bullets' register — the review's proposed shape: `**A node with no region in
   this handle reports \`undefined\`** — a foreign node, ...` with the dash tail
   unchanged. Keep the corrected semantics exactly.
2. **The stranded copula at `guides/markdown.md:229`.** The aside `— as a separator
   joined between two abutting regions is —` strands its copula against an antecedent
   whose subject is a boundary. Rewrite in the split shape the round already wrote at
   `src/core/types.ts:108-114`: state the `undefined` rule and its separator example so
   the elided verb agrees, then state the bridging rule as its own sentence. Keep every
   measured value and rule.
3. **The noun-pile.** At `guides/markdown.md:271-272`, `src/core/types.ts:48`, and
   `src/core/types.ts:522`, replace `the characters normalization removed` with
   `the characters that normalization removed` (keep the sentence otherwise).
4. **The `value` verb.** At `guides/markdown.md:278` and `src/core/types.ts:49`, replace
   the code token used as an English verb (`the node values \`a\nb\``) with the
   property-named form — the review's proposed shape: `the text node's \`value\` is
   \`a\nb\` while its region is \`{ start: 0, end: 4 }\`` — adapted to each site's
   surrounding sentence.
5. **One term for the node-to-source relation.** Replace `came from` with the round's
   adopted `produced from` at `src/core/Markdown.ts:24`, `src/core/Markdown.ts:75-76`,
   `guides/markdown.md:251`, and the surface-table rows `guides/markdown.md:20` and
   `:46`, adapting each sentence's grammar minimally. After the edit, a case-insensitive
   search for `came from` over `guides/markdown.md`, `src/core/types.ts`, and
   `src/core/Markdown.ts` reports no hit on the node-to-source relation; report any
   remaining hit in another sense with its ruling.

## Execution

You perform the assignment directly and spawn nothing. Validate scoped:

```text
npx oxfmt --config .oxfmtrc.json --check guides/markdown.md src/core/types.ts src/core/Markdown.ts
npm run check
npx vitest run --config vite.config.ts --no-cache --reporter=dot --project src:core
npm run test:guides
```

## Deviation contract

Stop and report — expected, found, exact evidence — when a site's current text differs
from the finding's quote, when a fix cannot avoid moving a runtime token, or when the
suite reds. Exact wording inside a deliverable's stated shape is yours to decide and
record.

## Acceptance criteria, cheap-first

1. Scoped format check exit 0; `npm run check` exit 0.
2. Case-insensitive `came from` search over the owned files: no hit in the
   node-to-source sense.
3. `src:core` exits 0 at `602 passed (602)`, unchanged; `test:guides` exits 0.
4. A writing-rules sweep over your changed lines passes, pattern and population named.

## Output

Write the report to `/home/user/lsp/tmp/units/h2.3.2-prose-report.md`: each deliverable
with its before and after text, the sweep's pattern and population, the gate readings
with exit codes, and the actual `git status --short` and `git diff --stat` output. Your
final message is a short summary naming the report path.
