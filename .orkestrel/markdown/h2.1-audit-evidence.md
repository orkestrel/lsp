# h2.1 audit evidence — Orchestrator-produced, 2026-08-26

The `reviewer` lane cannot execute, so the Orchestrator ran every behavioral probe and records the
results here. The executed instrument is `h2.1-audit-instrument.sh` beside this file; the isolated
diff it produced is `h2.1-derive-diff.txt`. Every command ran in `/home/user/markdown`, clean at
commit `d310eea` on the `claude/lsp-spec-audit-est33d` branch, after `npm ci` exit 0 in this
container.

## Scope honesty

The pre-h2.1 tree was reconstructed from the checkpoint's parent commit `f77063c` plus the retained
round diff `h2-round-diff.txt`, which applied cleanly (`git apply --whitespace=nowarn`, exit 0). A
file-by-file comparison of that reconstruction against the committed `d310eea` tree over every
`git ls-files` path reports differences in exactly these files:

```text
src/core/Markdown.ts
src/core/helpers.ts
tests/src/core/Markdown.test.ts
tests/src/core/helpers.test.ts
```

These are the unit's owned files. The unified diff over them is `h2.1-derive-diff.txt`.

## Green runs at the committed head

| Command | Reading |
| --- | --- |
| `npx vitest run --project src:core tests/src/core/Markdown.test.ts -t 'does not cross an input identity into its separate output derivation'` | Exit 0; `Tests 1 passed \| 55 skipped (56)` |
| `npx vitest run --project src:core tests/src/core/helpers.test.ts -t 'projects a zero-width abutment through the later segment'` | Exit 0; `Tests 1 passed \| 290 skipped (291)` |
| `npx vitest run --project src:core` | Exit 0; `Tests 592 passed (592)` |

## Revert-proof controls

The pre-h2.1 `src/core/Markdown.ts` and `src/core/helpers.ts` were copied over the working tree
while the committed h2.1 test files stayed in place, and the same commands re-ran:

| Command | Reading under the pre-fix sources |
| --- | --- |
| the derivation row, same command | Exit 1; `Tests 1 failed \| 55 skipped (56)` |
| the abutment row, same command | Exit 1; `Tests 1 failed \| 290 skipped (291)` |
| `npx vitest run --project src:core tests/src/core/Markdown.test.ts tests/src/core/helpers.test.ts` | Exit 1; `Tests 2 failed \| 345 passed (347)` — the failing rows are exactly `does not cross an input identity into its separate output derivation` and `projects a zero-width abutment through the later segment` |

The revert reddened exactly the rows that name the defects and nothing else, so the harness stayed
valid under the revert and the counts are evidence. The failure messages match the writer's
red-first table: `{ start: 6, end: 7 }` received where `undefined` was expected, and
`{ start: 1, end: 1 }` received where `{ start: 5, end: 5 }` was expected.

## Restore proof

`git checkout -- src/core/Markdown.ts src/core/helpers.ts` restored the committed sources;
`git status --porcelain` printed nothing afterward, and the derivation row re-ran green
(exit 0; `Tests 1 passed | 55 skipped (56)`). The `592 passed` full-project run in the green table
ran after this restore.
