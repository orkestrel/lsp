# Unit h2.2.1-prose — successor to h2.2-prose, closing the audit's broken claims

## Role and engine

You are the `implementer` lane, Claude Opus 5, native, writing directly in
`/home/user/markdown`. Do the work yourself and spawn nothing. You are the only writer in that
tree for the unit's duration. You do not commit, push, or run `git checkout`, `git restore`,
`git stash`, `git reset`, or `git clean`. Your corrections ride into the `h2.3-fences` Sol
`analyst` audit; you do not accept your own work.

## This is a successor brief

The `h2.2-prose` unit (brief and report under `/home/user/lsp/.orkestrel/markdown/`) landed and
its Sol `analyst` audit returned FAIL on three claims
(`/home/user/lsp/.orkestrel/markdown/h2.2-audit-analyst-verdict.md`, reconciled in
`h2.2-audit-verdict.md` beside it). This unit carries every broken finding. Read those files
first, then `/home/user/markdown/.claude/rules/typescript.md` and
`.claude/rules/writing.md`. No skill is named.

## Standing conditions (verified 2026-08-26)

- `/home/user/markdown` is HELD uncommitted at commit `d310eea` on the
  `claude/lsp-spec-audit-est33d` branch with working changes in exactly `guides/markdown.md`,
  `src/core/types.ts`, and `src/core/Markdown.ts` — the accepted `h2.1-derive` and `h2.2-prose`
  work plus the integrated `@returns` hunk. You edit on top of those changes and never revert
  them.
- Dependencies are installed. The `probe` Vitest project collects `tmp/probe/**/*.test.ts`; a
  probe you write there is deleted before you return.

## The measurements every correction is written against

The Orchestrator ran both instruments through the real `projectSpan` via the `@src/core` barrel
(`tmp/probe` Vitest tests, exit 0, swept after each run). Cite these readings; do not re-derive
them, though you can re-run an equivalent probe if a wording decision needs a further reading.

The cascade reading — zero-extent runs at one derived offset:

```text
segments [{offset 0, start 0, end 1}, {offset 1, start 5, end 5}, {offset 1, start 9, end 10}]
projectSpan 1..1 -> {"start":9,"end":9}
projectSpan 1..2 -> {"start":9,"end":10}
projectSpan 0..2 -> {"start":0,"end":10}
```

The bridge reading — a range spanning an uncovered interior separator, over the abutting-join
source `{ text: 'a\nb', segments: [{offset 0, start 0, end 1}, {offset 2, start 1, end 2}] }`:

```text
projectSpan 0..3 -> {"start":0,"end":2}
projectSpan 1..2 -> undefined
projectSpan 2..3 -> {"start":1,"end":2}
```

## The corrections, one per broken finding

1. **The `MarkdownSegment` remark's zero-width bullet** (`src/core/types.ts`, the bullet
   reading "a zero-width `p` that coincides with the next segment's `offset` resolves through
   that LATER segment"). The cascade reading falsifies "that LATER segment": resolution
   answers through the LAST run whose `offset` equals `p`, skipping every zero-extent run at
   that offset, and a discontinuous abutment therefore still reports the later run's `start`.
   State that rule; verify your final wording against `projectSpan` in `src/core/helpers.ts`
   and both readings.
2. **The `MarkdownDerivation` remark's closing branch** (`src/core/types.ts`, the sentence "A
   node mapped to `undefined`, a node whose direct input has no span, and a node with no entry
   at all each report `undefined`"). Read alone the sentence is false: the audited Q5b reading
   shows a node mapped to `undefined` reporting `{0,2}` because own-region resolution wins
   first. Carry the qualifier inline — those cases report `undefined` where the output identity
   holds no region of its own — so the sentence stays true out of context.
3. **The guide's coverage sentence** (`guides/markdown.md`, the parse-pipeline paragraph
   ending "reports `undefined` where they do not, as they do not for a separator joined
   between two abutting regions"). The bridge reading falsifies the universal form: `projectSpan`
   resolves the range's two boundaries, reports `undefined` when a boundary lands in an
   uncovered position, and BRIDGES an uncovered interior when both boundaries resolve. State
   the boundary rule. Align the `MarkdownSource` remark's clause in `src/core/types.ts`
   ("reports `undefined` for a range that needs it") with the same rule so the two sites agree,
   and re-read the guide's Source provenance section for any other sentence your correction
   makes false — the search bound is that section plus the parse-pipeline paragraph.
4. **The `Markdown` class TSDoc** (`src/core/Markdown.ts`, the class-level remark stating "A
   node with no single source region - a joined text run, a synthesized paragraph, an output
   built from separate sources - reports `undefined`"). The audited Q4, Q5a, and Q5b readings
   falsify it: a parse-joined run reports the enclosing region, a synthesized paragraph the
   rewrite named an input for reports that input's region, and a reused spanned identity keeps
   its own region. Rewrite the clause to the fixed order the corrected `MarkdownInterface.span`
   remarks in `src/core/types.ts` state — own region, else the direct input's region, else
   `undefined` — in the class TSDoc's own voice. Touch nothing else in that file; the
   `@returns` hunk is already integrated.

## Scope

- Owned: `src/core/types.ts` (the `MarkdownSegment`, `MarkdownDerivation`, and
  `MarkdownSource` remarks only), `guides/markdown.md` (the coverage sentence and any
  provenance-section sentence within the named search bound), `src/core/Markdown.ts` (the
  class-level TSDoc block only), and `tmp/probe/` for a probe you delete before returning.
- Off-limits: every other file, every type shape, every signature, every line of runtime code.
  A correction that cannot land as prose stops the unit.
- Allowed tools: read, edit, and the acceptance-criteria commands.

## Deviation contract

A correction that would require a code change, a measurement that contradicts one of the
supplied readings, or a sentence outside the named search bound your correction makes false,
stops the unit: report expected, found, exact evidence, done or not done, and at most one short
hypothesis. Wording and sentence placement are yours to decide and record.

## Acceptance criteria, cheap-first, each command and output recorded

1. `npx oxfmt --config .oxfmtrc.json --check src/core/types.ts src/core/Markdown.ts` exits 0.
2. `npm run check` exits 0.
3. `npx vitest run --project src:core` exits 0 with `592 passed (592)` unchanged.
4. `npm run test:guides` exits 0 with `18 passed` unchanged.
5. The writing-rules sweep over your added lines
   (`git diff -U0 -- src/core/types.ts src/core/Markdown.ts guides/markdown.md`, added lines
   only, against the substitution table and the count ban) reports clean or every hit ruled
   permitted, with the pattern named.

## Review evidence

This is a documentation change inside held source: your report carries the actual `git diff`
scoped to the files you touched this unit (against the held state you started from, which you
record as `git stash` is forbidden — capture the pre-edit content of each owned region in your
report instead) and the actual `git status --short` output.

## Output

Write your report to `/home/user/lsp/tmp/units/h2.2.1-prose-report.md`: each corrected sentence
before and after, the readings each rests on, the search-bound result for the guide sweep, the
gate readings with exit codes, the full-tree `git status --short`, and any claim you flag for
the analyst. No process diary.
