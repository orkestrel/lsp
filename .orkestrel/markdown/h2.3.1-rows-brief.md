# Unit brief: h2.3.1-rows — the round-two audit's test-row closures

## Role and engine

`builder` — Sonnet, native subagent. You write in `/home/user/markdown`, the sole writer
in that checkout, from the clean committed baseline `e5876ff` on branch
`claude/lsp-spec-audit-est33d`. You perform the assignment directly yourself and spawn
nothing. Every deliverable below is fully specified; you exercise no design judgment.

## Objective

Close the h2 round-two objective audit's broken findings: the cascade rule gains a
discriminating `projectSpan` row, the bridging clause gains its executed rows, the
`scanInlineSource` block moves to its mirror file, and the trailing-space region rule
gains its executed row. `src:core` exits green from the `599 passed (599)` baseline.

## Context

- Read before editing: `/home/user/markdown/AGENTS.md`; `/home/user/markdown/.claude/rules/tests.md`
  and `.claude/rules/writing.md`; no skill (explicit none); the guide section the rows
  execute, `guides/markdown.md` § Source provenance and the `projectSpan` prose near line
  229.
- The findings you close come from the round-two objective audit verdict at
  `/home/user/lsp/.orkestrel/markdown/h2-round2-audit-verdict-objective.md`. Each
  deliverable below restates its finding fully; read the verdict for the evidence behind
  it, not for extra work.
- This unit is allocated ownership of `tests/src/core/helpers.test.ts` by the round's
  re-baseline — the carrier the audit found unallocated.
- Host: Linux, dependencies installed, scoped vitest runs finish in seconds.

## Scope

- Owned: `tests/src/core/helpers.test.ts`, `tests/src/core/Markdown.test.ts`,
  `tests/src/core/parsers.test.ts`.
- Off-limits: everything else — all of `src/`, `guides/`, `tests/setup.ts`,
  `tests/guides.test.ts`. A row whose executed reading contradicts its expected value
  below is a deviation, never a reason to touch `src/`.

## Deliverables, each fully specified

1. **The cascade discriminating row**, in `tests/src/core/helpers.test.ts` inside the
   `projectSpan` coverage: assert that
   `projectSpan({ text: 'ab', segments: [{ offset: 0, start: 0, end: 1 }, { offset: 1, start: 5, end: 5 }, { offset: 1, start: 9, end: 10 }] }, 1, 1)`
   equals `{ start: 9, end: 9 }`. Name the row for the behavior: a zero-width position
   shared by several segments resolves through the last segment at that offset, whatever
   the earlier segment's extent. Discrimination control, run and then removed before you
   finish: change the expectation to `{ start: 5, end: 5 }` (the superseded reading),
   observe the row fail, restore the `{ start: 9, end: 9 }` expectation, observe it pass.
   Record both readings in your report.
2. **The bridging rows**, in `tests/src/core/helpers.test.ts` beside the other
   `projectSpan` rows, over the source
   `{ text: 'a\nb', segments: [{ offset: 0, start: 0, end: 1 }, { offset: 2, start: 1, end: 2 }] }`:
   `projectSpan(source, 0, 3)` equals `{ start: 0, end: 2 }` (both boundaries resolve, so
   the projection bridges the uncovered interior); `projectSpan(source, 1, 2)` is
   `undefined` (a boundary in the uncovered interior resolves nothing); and
   `projectSpan(source, 2, 3)` equals `{ start: 1, end: 2 }`. One row or adjacent rows,
   named for the bridging behavior.
3. **The `scanInlineSource` move.** Delete the
   `describe('scanInlineSource — coordinates inside one line', …)` block at
   `tests/src/core/parsers.test.ts:966-975` together with its leading comment, and delete
   the `scanInlineSource` value import and the `MarkdownNode`/`MarkdownSpan` type import
   from that file where nothing else uses them. Re-add the block verbatim inside
   `tests/src/core/helpers.test.ts`, beside the existing
   `describe('scanInlineSource', …)` block near line 713, keeping the row name and
   assertions unchanged and adding whatever imports that file lacks.
4. **The trailing-space region row**, in `tests/src/core/Markdown.test.ts` under
   `describe('Markdown — span', …)`: for `new Markdown('a \nb')`, assert that the first
   text node values `'a\nb'`, that `span` on it reports `{ start: 0, end: 4 }`, and that
   `'a \nb'.slice(0, 4)` under that region reproduces `'a \nb'` — the trimmed space
   included, per the fence at `guides/markdown.md:277-279`.

## Execution

You perform the assignment directly and spawn nothing. Run each new or moved row as you
land it with a scoped vitest command over its file, then close with the scoped set:

```text
npx oxfmt --config .oxfmtrc.json --check tests/src/core/helpers.test.ts tests/src/core/Markdown.test.ts tests/src/core/parsers.test.ts
npx --no-install oxlint --config .oxlintrc.json --deny-warnings tests/src/core/helpers.test.ts tests/src/core/Markdown.test.ts tests/src/core/parsers.test.ts
npm run check
npx vitest run --config vite.config.ts --no-cache --reporter=dot --project src:core
```

## Deviation contract

Stop and report — expected, found, exact evidence, done or not done, one short
hypothesis at most — when any specified expectation reads differently when executed, when
a deletion in deliverable 3 breaks a row you do not own, or when the scoped suite reds
outside your rows. Where a specified row collides with an existing row name or an
ancillary placement question arises, decide it, record it, and carry on.

## Acceptance criteria, cheap-first

1. The scoped format and lint commands exit 0.
2. `npm run check` exits 0.
3. `src:core` exits 0 with every deliverable's row collected and named, from the
   `599 passed (599)` baseline (the move keeps the row count; the additions raise it).
4. `grep -c 'scanInlineSource' tests/src/core/parsers.test.ts` reports 0.
5. A writing-rules sweep over your added lines passes, pattern and population named.

## Output

Write the report to `/home/user/lsp/tmp/units/h2.3.1-rows-report.md`: each deliverable
with its executed reading, the discrimination control's red and green readings, the gate
outputs with exit codes, the sweep's pattern and population, and the actual
`git status --short` and `git diff --stat` output. Your final message is a short summary
naming the report path.
