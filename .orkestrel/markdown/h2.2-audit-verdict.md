# h2.2-prose audit — reconciliation and acceptance record, 2026-08-26

The objective lane ran: Sol `analyst` (bench, read-only, journal `tmp/codex/h2.2-audit.jsonl`,
session `01a03ded-7d86-7d52-93f1-e5657714feb7`), verdict retained verbatim as
`h2.2-audit-analyst-verdict.md` with its brief as `h2.2-audit-brief.md`. The subjective lane did
not run: the writer's engine is Opus, the auditor an engine that did not write it, and the
subject is prose correctness against measurements rather than shape. The `checker` did not run:
no criterion is mechanical beyond what the lane itself verified.

Its terminal line: `VERDICT: FAIL — 3 broken, 0 unresolved, 0 not-evidenced, 1 findings outside
the claims`.

## The rulings

- **Claim 1 — BROKEN stands, on the Orchestrator's own cascade measurement.** The corrected
  `MarkdownSegment` bullet says a zero-width position resolves through "that LATER segment";
  the supplied reading (`segments [{0,0,1},{1,5,5},{1,9,10}]`, `projectSpan 1..1 -> {9,9}`)
  shows the resolution skips the zero-extent successor and answers through the LAST segment
  whose `offset` equals the position. The `MarkdownDerivation` closing sentence ("A node mapped
  to `undefined` … report[s] `undefined`") also stands broken read alone: the Q5b reading shows
  a node mapped to `undefined` reporting `{0,2}` because own-region resolution wins first; the
  sentence needs that qualifier inline. Carrier: `h2.2.1-prose`.
- **Claim 2 — BROKEN stands, upgraded from derivation to measurement.** The lane labeled its
  conclusion a derivation per its brief; the Orchestrator's bridge probe settles it
  (`tmp/probe` Vitest over the real `projectSpan`, swept after the run): for the abutting-join
  source `{text 'a\nb', segments [{0,0,1},{2,1,2}]}`, `projectSpan 0..3 -> {0,2}` bridges the
  uncovered interior separator, `1..2 -> undefined`, `2..3 -> {1,2}`. The rival reading — that
  any range spanning an uncovered position reports `undefined` — would have printed
  `undefined` for `0..3`, so the instrument discriminates. The guide's coverage sentence at
  the parse-pipeline paragraph over-claims; the true rule is boundary resolution: `undefined`
  when a range boundary lands in an uncovered position, and a range whose boundaries both
  resolve bridges an uncovered interior. Carrier: `h2.2.1-prose`.
- **Claim 6 — BROKEN stands, and the defect is the Orchestrator's integration step, not the
  unit.** The unit's report predates the serial integration of its own `Markdown.ts` patch, so
  the retained report's diff is the two-file diff. The unit report stays immutable; the record
  gap closes with `h2.2-integration-evidence.txt`, the Orchestrator-captured three-file diff
  and status of the held tree. Claim 5's CONFIRMED already proves the integrated hunk matches
  the report's patch byte-for-byte.
- **Claims 3, 4, 5, 7 — CONFIRMED.** The claim-5 overturn ruling, the TSDoc openers, the
  integrated hunk, and the writing-rules sweep all stand.
- **The finding outside the claims — the `Markdown` class TSDoc still carries the falsified
  absence clause — corroborates the finding the `h2.2-prose` report already returned.** Its
  carrier moves to `h2.2.1-prose` (previously routed to `h2.3-fences`), so the finding keeps
  exactly one carrier.

## The successor unit

`h2.2.1-prose` (Opus `implementer`, native, brief `markdown/h2.2.1-prose-brief.md`) carries
every broken finding: the cascade bullet, the derivation qualifier, the coverage sentence with
its blast radius across the guide and the `MarkdownSource` remark, and the `Markdown` class
TSDoc rewrite. Its corrections adopt the lane's prescriptions against the two Orchestrator
measurements, so its audit rides as added claims in the `h2.3-fences` Sol `analyst` round —
Sol did not write the corrections, and the prose must settle before `h2.3` transcribes the
fences it proves. `h2.3-fences` runs after `h2.2.1-prose` returns.
