# h2-round2 — subjective lane verdict, verbatim

Lane: subjective review, engine Opus 5, role `reviewer`, clean context, launched per the
audit step's second-lane trigger after the objective lane returned FAIL. Brief:
`lsp/tmp/units/h2-round2-review-brief.md`. Subject: the `h2.2.1-prose` corrections
(`e4c434c`), the `h2.3-fences` transcription (`e5876ff`), and the `h2.3.1-rows` closures
(`bda6e1e`) at head `bda6e1e`. Returned 2026-08-26. The `h2.3.2-prose` unit carries the
broken findings; the Orchestrator's round reconciliation lands in
`h2-round2-audit-verdict.md` beside this file.

---

Lane held: subjective — design fit, vocabulary, guide voice, TSDoc shape, and row naming. No correctness ruling is made here.

**1. Guide voice — BROKEN.**
`/home/user/markdown/guides/markdown.md:282` reads:
`- **A node this handle holds no region for reports `undefined`** — a foreign node, every node of an adopted document, and a rewrite output the handler built fresh from separate source nodes.`
The bold lead's subject is a seven-word noun phrase whose embedded relative clause ends in a preposition that abuts the main verb: `…holds no region for reports `undefined``. Every other bold lead in the same list keeps a bare two-to-three-word subject and puts qualification after the verb or after the dash — `:269` **A region addresses the ORIGINAL constructor string**, `:273` **A region is half-open and counted in UTF-16 code units**, `:275` **A parsed node covers the source it was produced from**, `:280` **A one-source rewrite keeps its source's region through `map`.** The superseded line the round replaced also had that shape (**A node with no single source region reports `undefined`**), so the register break is round-introduced, and the same round wrote the clean condition-first form for the same rule in TSDoc (`src/core/types.ts:510-511`, `src/core/Markdown.ts:79-80`: "when this handle holds no region for the node"). Why it matters: the bold lead is the line a scanning reader takes the rule from, and this one has to be re-parsed. Smallest fix: restore a bare-noun subject and carry the condition after the verb — `- **A node with no region in this handle reports `undefined`** — a foreign node, …` — which keeps the corrected semantics the round exists to state.
Refutation attempted and failed to save it: the surrounding prose is dense and dash-heavy, so density alone is in register; the break is the subject-verb collision specifically, which no sibling bullet has. Ruled on the guide's own list, not on general taste.

**2. One concept, one term — CONFIRMED.**
Ruled on the claim's three named concepts across the added lines of `e4c434c`, `e5876ff`, and `bda6e1e`.
- Original-source region: "region" at every added site (`guides/markdown.md:192, 229, 270-283, 324-328, 356-360`; `src/core/types.ts:46, 62, 74-86, 108-114, 438-449, 507-523`; `src/core/Markdown.ts:27-31, 79-80`). `span`/`spans` appear only as the API name and the field name, which is the separation the untouched opener at `guides/markdown.md:251` already draws.
- Mapping record: "segment"/"run" pair unchanged from the untouched convention — `segment` addresses the declared member and its `offset`, `run` the mapped stretch (`src/core/types.ts:74-86`), matching `guides/markdown.md:229`'s untouched "the `MarkdownSegment` runs". "entry" for a `derivations` row and "derivation edge" for a hop name different things (`src/core/types.ts:434-449`).
- Projection operation: `projectSpan` "projects" a position and "resolves" a boundary or a choice, the same pairing at both sites (`src/core/types.ts:74-86, 108-114`; `guides/markdown.md:229`), and "resolve" for span selection predates at `guides/markdown.md:324`.
Refutation attempted: `src/core/types.ts:515` "where THIS handle holds coordinates for it" sits one line from the `@returns` "holds no region for the node". It did not land — the immediately following untouched sentence is "so no coordinates exist to report", so the added line matches its own neighbour's established word.

**3. Row naming — CONFIRMED.**
Read every `it(...)` the three commits add or move: `Markdown.test.ts:65, 76, 87, 125, 283, 293`; `parsers.test.ts` (added then moved) `records each emitted node against the original coordinates of the scanned line`; `helpers.test.ts:271, 283, 756`. Each name states an observable outcome in the file's landed voice — third-person `-s` verb first, lowercase, no `should` — matching landed neighbours such as `Markdown.test.ts:54, 98` and `helpers.test.ts:260`. No name leaks an implementation detail: `segments`, `offset`, `region`, and `span` are all published contract vocabulary. No name carries a control identifier — `helpers.test.ts:271` names the corrected rule ("through the last one at that offset"), never the superseded `{5,5}` expectation the discrimination control used.
Refutation attempted: `helpers.test.ts:260` "projects a zero-width abutment through the later segment" beside the added `:271` "…through the last one at that offset" could read as two rules coexisting. It did not land — with that row's two segments the later run is the last at that offset, so the landed name is true of its own fixture, and the pair is what discriminates the corrected rule.

**4. TSDoc shape — CONFIRMED.**
Every corrected opener now takes the third-person `-s` verb form `.claude/rules/typescript.md` § Comments fixes, replacing a noun phrase: `src/core/types.ts:39` "Addresses…", `:62` "Maps…", `:98` "Pairs…", `:435` "Pairs…", `:507` "Reads…". None repeats its own symbol name. `@param`, `@returns`, and `@remarks` placement is unchanged and correct; the detail lists sit under `@remarks` as the rule requires, each introduced by a complete sentence (`:74-76`, `:437-440`). Tense is present throughout, matching the untouched behavioural remarks in the same file. `src/core/Markdown.ts:23-31` states the same resolution order in the class block's own bullet voice.
Refutations attempted, neither landed: (a) the corrected openers are verb-first while untouched data-type remarks nearby still open with noun phrases (`src/core/types.ts:124` "A run of plain text -", `:136` "Emphasized inline content -") — the divergence runs toward the rule, and `AGENTS.md` makes the rule outrank existing code, so the untouched blocks carry that drift, not the corrected ones; (b) `src/core/types.ts:437-449` is a bulleted list under "in a fixed order" where numbering might be expected — the "wins" / "otherwise" / "otherwise none" chain carries the order lexically, more strongly than numerals would.

**5. Reuse over duplication — CONFIRMED.**
Ruled per carried reading in `h2.3-fences-report.md` § Reuse table against the landed rows. Each named carrier exists at head with the exact name and pins the reading: fresh-output-from-separate-sources → `Markdown.test.ts:303`; identity keying → `:133`; rebuilt ancestor → `:269`; foreign and adopted → `:109`; fresh value per call → `:98`; direct input with no second edge → `:326`. None would be materially clearer as its own row — each carrier's name already states the rule the fence reading claims, and each asserts the fence's documented outcome (`undefined`, or the region) on an equivalent construction.
Refutation attempted on the added side and failed: `Markdown.test.ts:65` ("covers the marker…") looks like `:54`, and `:283` ("gives a lowercased text node…") looks like `:269`. Neither is duplication. The landed rows prove the rule over their own vehicles and assert a slice; the added rows are the fence transcriptions, using each fence's own source string and asserting each fence comment's exact pair (`{0,7}` for `'# Title\n\nA **bold** word.'`, `{2,4}` for `'# Hi\n\nText.'`). `.claude/rules/documentation.md` makes those different subjects: one proves the code's rule, the other proves the guide's number is the code's number.

**6. Placement coherence — CONFIRMED.**
`scanInlineSource` and `projectSpan` are `src/core/helpers.ts` exports (`:918`, `:220`), so `tests/src/core/helpers.test.ts` is their mirror and the move lands them where the mirror rule points. The two describes sit adjacently — `helpers.test.ts:738` `scanInlineSource` and `:755` `scanInlineSource — coordinates inside one line`, with nothing between them — and are distinctly scoped: `:739` proves nested-node spans over a mid-line window, `:756` proves top-level text and emphasis coordinates off a `splitLines` quoted line. The row names do not overlap. `tests/src/core/parsers.test.ts:28-38` reads true of the file it now describes; the move made it truer, because the header names the scanner leaves as covered in `helpers.test.ts` and a `scanInlineSource` block no longer contradicts it.
Refutations attempted, neither landed: (a) one describe would carry both blocks — the split is defensible, the second block is named for the guide section it transcribes (`§ Coordinates inside a line`), and merging is a preference the claim's stated test does not require; (b) the moved comment at `helpers.test.ts:752-754` orients through `parseInline`, which lives in the other file, while the guide's own sentence at `guides/markdown.md:383` orients through `scanInline` — the sibling describe directly above it. The comment stays true, so this is a residual for whoever next edits that block, not a break.

**7. Plain-language reading — BROKEN.**
Population read: every added prose line of the three commits, code and assertions excluded — `guides/markdown.md` table rows `:192-193`, paragraph tail `:229`, bullets `:269-283`, paragraph `:285-293`, paragraph `:323-328`, lead-in `:344-347`, bullet `:356-360`; `src/core/types.ts` remarks `:43-52`, `:65-86`, `:102-114`, `:414-425`, `:437-449`, `:509-523`; `src/core/Markdown.ts:27-31` and `:79-80`; and the moved comment at `tests/src/core/helpers.test.ts:752-754`. Key-point-first and one-idea-per-sentence hold across that population, and no line builds up to its point. Three lines fail on first read:

- `guides/markdown.md:229` — "It reports `undefined` when either boundary lands in a position the runs leave uncovered — as a separator joined between two abutting regions is — and it bridges an uncovered interior when both boundaries resolve." The aside strands its copula: the antecedent predicate is "lands in a position the runs leave uncovered", whose subject is a boundary, so the elision demands "does", not "is", and the reader must silently swap the subject from a boundary to a separator and supply "uncovered" as an adjective. The superseded sentence carried a clean ellipsis ("as they do not for a separator joined between two abutting regions"), so the round traded clarity for compression. Smallest fix: adopt the split the same round wrote at `src/core/types.ts:108-114` — end the first sentence at "uncovered", then state the boundary rule — or write "…leave uncovered, as the derived position of a separator joined between two abutting regions is."
- `guides/markdown.md:271-272`, `src/core/types.ts:48`, and `src/core/types.ts:522` — "the characters normalization removed". "characters normalization" parses as a compound noun on first read, and in this document's own domain (`splitLines` normalizes line endings; "normalization" is already a technical noun here), so the reader re-parses at "removed". Smallest fix: "the characters that normalization removed", or "the characters normalization strips".
- `guides/markdown.md:278` and `src/core/types.ts:49` — "the node values `a\nb`" / "The text node of `'a \nb'` values `a\nb`". This uses the `value` property name as an English verb, which `.claude/rules/writing.md` § Code tokens forbids outright, and "values" garden-paths as a plural noun before the reader reaches the region clause. The construction appears nowhere else in the repository's prose, so it is a round-introduced coinage. Smallest fix: "the text node's `value` is `a\nb` while its region is `{ start: 0, end: 4 }`".

Findings outside the claims:

- **Two live spellings for the node-to-source relation, introduced by this round.** The round deliberately replaced "parsed from" with "produced from" at `guides/markdown.md:192`, `:270`, `:275`, and `src/core/types.ts:507`, `:521`, because "parsed from" had become false. It left "came from" for the same relation at `src/core/Markdown.ts:24` (the class Provenance bullet the round otherwise rewrote), `src/core/Markdown.ts:75-76` (the `span` method opener, whose `@returns` the round did correct), `guides/markdown.md:251` (the § Source provenance opening sentence), and the surface-table rows `guides/markdown.md:20` and `:46`. Why it matters: `AGENTS.md` § Design laws fixes one term per concept, and a consumer reading `MarkdownInterface.span` and `Markdown.span` now meets two verbs for the relation whose exact meaning this whole round exists to correct — a reader cannot tell from the prose whether "came from" carries the narrower old sense. What right looks like: one term, "produced from", at `src/core/Markdown.ts:24` and `:75-76` and `guides/markdown.md:251`; if the untouched surface-table rows `:20` and `:46` are out of the round's scope, name the carrier unit for them rather than leaving the split unrecorded.

Referral to the objective lane (no verdict from me): the fence transcriptions landed in the mirrored suites (`tests/src/core/Markdown.test.ts`, `tests/src/core/parsers.test.ts`, `tests/src/core/helpers.test.ts`), while `.claude/rules/tests.md` § Cross-cutting proofs names `tests/guides.test.ts` as the home that "executes the flagship fences". This package's `tests/guides.test.ts:1-3` is a vendored `@orkestrel/guide` parity drop-in that transcribes no fence and asserts no fence value — a grep for `Title`, `start: 0, end: 7`, and `slice` over that file returns no match. Whether the rule's named home is satisfied by mirrored-suite transcription, or whether the drop-in leaves the fence-execution obligation unhomed, is a placement-and-contract question outside my lane.

VERDICT: FAIL -- 2 broken, 0 unresolved, 0 not-evidenced, 1 findings outside the claims
