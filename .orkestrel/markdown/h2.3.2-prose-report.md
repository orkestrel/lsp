# Unit report: h2.3.2-prose — the subjective lane's voice repairs

`implementer` — Opus 5, native subagent. Checkout `/home/user/markdown`, branch
`claude/lsp-spec-audit-est33d`, from clean baseline `bda6e1e`. Date: 2026-08-26.

Every named finding is closed. Conditions the brief did not anticipate are recorded under
**Deviations**: a baseline format failure that leaves acceptance criterion 1 unreachable for
a reason predating this unit, a `came from` site list narrower than the brief's own
criterion 2, a partial quote mismatch at `src/core/types.ts:522`, and the absence of the
review verdict the brief names.

## Deliverable 1 — the bold lead at `guides/markdown.md:282`

Before:

```text
- **A node this handle holds no region for reports `undefined`** — a foreign node, every node of an
  adopted document, and a rewrite output the handler built fresh from separate source nodes.
```

After:

```text
- **A node with no region in this handle reports `undefined`** — a foreign node, every node of an
  adopted document, and a rewrite output the handler built fresh from separate source nodes.
```

The review's proposed shape, taken verbatim. The dash tail is unchanged. Semantics hold:
"a node for which this handle holds no region" and "a node with no region in this handle"
select the same nodes. The subject is now a bare noun, matching the register of the sibling
bullets `A region addresses…`, `A region is half-open…`, `A parsed node covers…`, and
`A one-source rewrite keeps…`.

The brief describes the shape as "carrying the condition after the verb" while the shape it
quotes places the condition before the verb. I wrote the quoted shape, because the brief
names it as the review's own proposal and the surrounding sentence admits no other reading.

## Deliverable 2 — the stranded copula at `guides/markdown.md:229`

Before:

```text
It reports `undefined` when either boundary lands in a position the runs leave uncovered — as a separator joined between two abutting regions is — and it bridges an uncovered interior when both boundaries resolve.
```

After:

```text
It reports `undefined` when either boundary lands in a position the runs leave uncovered: joining two abutting regions with a separator leaves that separator's derived position uncovered. It bridges an uncovered interior when both boundaries resolve.
```

The aside's elided `is` had `a separator` for its subject while its antecedent was
`a position`, so the elided predicate could not agree. The rewrite follows the split shape
already written at `src/core/types.ts:108-114`: the `undefined` rule takes its separator
example as a full clause with its own subject and verb (`joining … leaves … uncovered`), so
no verb is elided at all, and the bridging rule becomes its own sentence. Every measured
value and rule is unchanged — `undefined` when either boundary lands uncovered, the
separator case, and bridging when both boundaries resolve.

## Deliverable 3 — the noun-pile

Each site takes `that` before `normalization`, and each sentence is otherwise unchanged.

| Site                                | Before                                  | After                                        |
| ----------------------------------- | --------------------------------------- | -------------------------------------------- |
| `guides/markdown.md:271-272`        | `and the characters` / `normalization removed.` | `and the characters` / `that normalization removed.` |
| `src/core/types.ts:47-48`           | `and the` / `characters normalization removed, such as…` | `and the` / `characters that normalization removed, such as…` |
| `src/core/types.ts:521-522`         | `and characters normalization removed.` | `and characters that normalization removed.` |

The `src/core/types.ts:522` site carries no definite article before `characters`; see
**Deviations**.

## Deliverable 4 — the `value` verb

Before, `guides/markdown.md:278-279`:

```text
  `'a \nb'` the paragraph phase trims the trailing space, so the node values `a\nb` while its region
  is `{ start: 0, end: 4 }` — the whole `a \nb`, trimmed space included.
```

After:

```text
  `'a \nb'` the paragraph phase trims the trailing space, so the text node's `value` is `a\nb`
  while its region is `{ start: 0, end: 4 }` — the whole `a \nb`, trimmed space included.
```

Before, `src/core/types.ts:49`:

```text
 * trimmed. The text node of `'a \nb'` values `a\nb` and reports
```

After:

```text
 * trimmed. The text node of `'a \nb'` has the `value` `a\nb` and reports
```

Both sites now name the property instead of conjugating the token. The bare
`` `value` is … `` form is the house pattern already used at `src/core/types.ts:124` and
`src/core/types.ts:150`, so I did not add a following noun such as `property`.

## Deliverable 5 — one term for the node-to-source relation

`came from` becomes `was produced from`, matching the passive already at
`guides/markdown.md:270`, `guides/markdown.md:275`, `src/core/types.ts:46`, and
`src/core/types.ts:521`.

Sites the brief named:

| Site                        | Before                                                          | After                                                                 |
| --------------------------- | --------------------------------------------------------------- | --------------------------------------------------------------------- |
| `src/core/Markdown.ts:24`   | `node came from, and it is handle-relative`                      | `node was produced from, and it is handle-relative`                    |
| `src/core/Markdown.ts:75-76`| `a node of this handle's tree came` / `from.`                    | `a node of this handle's tree was` / `produced from.`                  |
| `guides/markdown.md:251`    | `the region of the source each node came from`                   | `the region of the source each node was produced from`                 |
| `guides/markdown.md:20`     | `address the original region that run came from`                 | `address the original region that run was produced from`               |
| `guides/markdown.md:46`     | `the input node each rewritten node came from`                   | `the input node each rewritten node was produced from`                 |

Sites the brief did not name, carried to satisfy acceptance criterion 2; see **Deviations**:

| Site                        | Before                                                          | After                                                                 |
| --------------------------- | --------------------------------------------------------------- | --------------------------------------------------------------------- |
| `src/core/types.ts:91`      | `the original-string region the run came from, inclusive.`       | `the original-string region the run was produced from, inclusive.`     |
| `src/core/types.ts:435`     | `the input node each rewritten node came from - what`            | `the input node each rewritten node was produced from -` / `what`      |
| `guides/markdown.md:796`    | `// the input node each rewritten node came from.`               | `// the input node each rewritten node was produced from.`             |

Three wraps required an adjacent line to be rewrapped so no word was dropped:
`guides/markdown.md:251-252` (`that ran`), `src/core/Markdown.ts:24-25` (`the`),
`src/core/types.ts:435-436` (`what`), and `src/core/types.ts:522-523` (`than`). Each is
verified in the final read.

### The `came from` sweep

Pattern: `came from`, case-insensitive (`grep -rni "came from"`). Population: the three
owned files `guides/markdown.md`, `src/core/types.ts`, `src/core/Markdown.ts`, whole files,
no path filter.

No hit remains in the node-to-source sense. Two hits remain in another sense, both ruled
permitted and kept:

- `guides/markdown.md:7` — "None of the four assumes its input **came from** a trusted
  parse". The relation is a function's argument to its origin, not a node to its source
  region. Outside the term this deliverable unifies.
- `guides/markdown.md:414` — "it can never assume its input **came from** `parseDocument`
  on trusted markdown". Same argument-origin sense.

## Writing-rules sweep

Pattern set, case-insensitive with inflections, over the `.claude/rules/writing.md`
substitutions table plus the Claims-and-time and Code-tokens bans:
`should|simpl|eas(y|ier|ily)|just|currently|now|new|latest|utiliz|leverag|via|in order to|e\.g\.|i\.e\.|etc\.|performant|robust|allows you to|and/or|since|once|please|sanity check|dummy|blacklist|whitelist|master|slave|ensure|guarantee|above|below|here|this document|we|our|let's`,
with word boundaries on the short tokens.

Population: the 54 added lines of this unit's diff across all three owned files, extracted
with `git diff -U0 … | grep "^+"`. Result: no hit. Exit code 1 from `grep`, meaning no
match.

## Gate readings

| Gate                                                              | Exit | Reading                              |
| ----------------------------------------------------------------- | ---- | ------------------------------------ |
| `npx oxfmt --config .oxfmtrc.json --check <the three owned files>` | 1    | `guides/markdown.md` — pre-existing; see Deviations |
| `npm run check`                                                    | 0    | clean                                |
| `npx vitest run … --project src:core`                              | 0    | `Tests 602 passed (602)`, `Test Files 6 passed (6)` |
| `npm run test:guides`                                              | 0    | `Tests 18 passed (18)`               |

Acceptance criteria: criterion 1 **not met** (pre-existing, unreachable — see Deviations);
criterion 2 **met**; criterion 3 **met** at `602 passed (602)` unchanged and `test:guides`
exit 0; criterion 4 **met**.

## Deviations

### 1. Acceptance criterion 1 is unreachable at the baseline

**Expected.** `npx oxfmt --config .oxfmtrc.json --check guides/markdown.md src/core/types.ts src/core/Markdown.ts`
exits 0.

**Found.** It exits 1 on `guides/markdown.md`, and it did so at the clean baseline
`bda6e1e` before this unit made any edit.

**Evidence.** The committed file was extracted with `git show HEAD:guides/markdown.md` into
a scratchpad and checked with the same config:

```text
BASELINE_EXIT=1
```

Formatting that untouched HEAD copy and diffing shows the disagreement is confined to the
`## Methods` table:

```text
187,191c187,191
193,196c193,196
```

oxfmt wants wider cell padding there. No deliverable names those lines and no finding
carries them.

**Done vs not done.** My own edits contribute zero format failures. My row edits to the
surface table lengthened two cells past the table's old width, which made oxfmt want to
re-pad lines 15-47 — a new failure my work introduced. I closed it by porting exactly the
formatter's own output for lines 15-47 and nothing else, proving first that the splice
changed only that region and then that the change was padding and separator dashes with
identical cell text:

```text
REGIONS CHANGED BY SPLICE:
15,47c15,47
CELL_TEXT_IDENTICAL=yes
```

Re-formatting the finished file now reports only the pre-existing region:

```text
REMAINING DISAGREEMENT REGIONS:
187,191c187,191
193,196c193,196
```

**Not done:** I did not re-pad the `## Methods` table. It is whitespace-only and inside a
file I own, so I could have made the gate green — I chose not to, because absorbing it would
attribute an unrelated repair to this unit and would hide that `bda6e1e` was committed
format-dirty. The Orchestrator owns that call.

**Hypothesis.** A prior unit edited a `## Methods` cell without re-padding the table, and
the scoped format check was not run on the guide at that point.

### 2. The brief's `came from` site list is narrower than its own criterion 2

**Expected.** Replacing `came from` at the five named sites leaves no hit in the
node-to-source sense.

**Found.** Three further hits sit in exactly the senses the deliverable unifies, and the
five named sites do not reach them: `src/core/types.ts:91` (the run-to-original-region
relation, the TSDoc twin of guide row 20), `src/core/types.ts:435` (the
rewritten-node-to-input-node relation, the TSDoc twin of guide row 46), and
`guides/markdown.md:796` (a `//` comment inside a `ts` fence carrying row 46's sentence
verbatim).

**Evidence.** The baseline sweep before any edit:

```text
guides/markdown.md:7    …input came from a trusted parse…          (other sense)
guides/markdown.md:20   …the original region that run came from.   (named)
guides/markdown.md:46   …each rewritten node came from.            (named)
guides/markdown.md:251  …each node came from…                      (named)
guides/markdown.md:414  …input came from `parseDocument`…          (other sense)
guides/markdown.md:796  // …each rewritten node came from.         (NOT named)
src/core/types.ts:91    …region the run came from, inclusive.      (NOT named)
src/core/types.ts:435   …each rewritten node came from - what      (NOT named)
src/core/Markdown.ts:24 …node came from…                           (named)
```

**Done.** I changed all three. Leaving them would have failed acceptance criterion 2 ("no
hit in the node-to-source sense") and would have produced exactly the drift the deliverable
exists to remove — a guide row reading `was produced from` beside its own TSDoc twin still
reading `came from`. All three are TSDoc or guide text inside my owned files. The
deliverable's closing sentence anticipates unlisted hits by asking for a ruling on each, so
I ruled and carried rather than stopping.

### 3. A partial quote mismatch at `src/core/types.ts:522`

**Expected.** Deliverable 3's quote, `the characters normalization removed`.

**Found.** The site reads `and characters normalization removed` — no definite article
before `characters`.

**Evidence.**

```text
src/core/types.ts:522:	 * and characters normalization removed. Each call returns a fresh value rather than
```

**Done.** I applied the transformation the deliverable names — insert `that` — and kept the
site's own article, per "keep the sentence otherwise": the line now reads
`and characters that normalization removed`. The noun pile is closed. I did not stop the
unit, because the finding's subject is present at the named line and only one article
differs; stopping over that would be the unscoped-contract failure the deviation contract
warns against.

### 4. The named review verdict does not exist

**Expected.** `/home/user/lsp/.orkestrel/markdown/h2-round2-review-verdict.md`, named by the
brief's Context and by the dispatch.

**Found.** No such file. `ls` of `/home/user/lsp/.orkestrel/markdown/` lists
`h2-round2-audit-verdict-objective.md` and no subjective counterpart. A grep for `h2.3.2`,
`came from`, `produced from`, `bold lead`, `noun-pile`, and `copula` across
`campaign/routing-2026-08-26-resume.md` and `h2-round2-audit-verdict-objective.md` returned
nothing.

**Done.** I proceeded on the brief's restatements, which carry each finding's before-quote
and proposed shape and are self-contained. The full reasoning the brief points to was
unavailable, so where the brief's own prose and its quoted shape disagreed — deliverable 1 —
I recorded the choice rather than resolving it from the verdict.

## Scope honesty

Touched files, all owned: `guides/markdown.md` (prose, one fence comment, and the
formatter's padding for lines 15-47); `src/core/types.ts` (TSDoc text only);
`src/core/Markdown.ts` (TSDoc text only). No declaration, no runtime line, and nothing under
`tests/` was edited — confirmed by reading the whole source diff, which touches only lines
inside `/** … */` blocks. No git state command was run beyond `status`, `diff`, `show`, and
`rev-parse`. Nothing was committed or pushed.

## Evidence

```text
=== git status --short ===
 M guides/markdown.md
 M src/core/Markdown.ts
 M src/core/types.ts
=== git diff --stat ===
 guides/markdown.md   | 85 ++++++++++++++++++++++++++--------------------------
 src/core/Markdown.ts |  8 ++---
 src/core/types.ts    | 14 ++++-----
 3 files changed, 54 insertions(+), 53 deletions(-)
```

The guide's changed lines are the prose edits plus the padding-only surface-table lines
15-47 described under Deviations 1.
