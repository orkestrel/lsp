# Unit brief: h2.4-mechanical — the R5 and R6 closures and the format re-pad

## Role and engine

`builder` — Sonnet, native subagent. You write in `/home/user/markdown`, the sole writer
in that checkout, from the clean committed baseline `c6b48f6` on branch
`claude/lsp-spec-audit-est33d`. You perform the assignment directly yourself and spawn
nothing. Every deliverable is ruled; where a deliverable ports existing documentation,
you adapt the ported text minimally and record the adaptation.

## Objective

Close the h2 reconciliation's R6 ruling (one home for the escaped-pipe rule; the
`limit` → `end` rename), the R5 scan-leaf TSDoc restoration it carries, and the
pre-existing `guides/markdown.md` format red. The scoped gates read green, the format
check over the guide included.

## Context

- Read before editing: `/home/user/markdown/AGENTS.md`;
  `/home/user/markdown/.claude/rules/typescript.md` (TSDoc), `names.md`, `tests.md`,
  `writing.md`; no skill (explicit none).
- The rulings are `/home/user/lsp/.orkestrel/markdown/h2-audit-reconciliation.md` § R5
  and § R6 — read them; the deliverables below restate them. Recorded line numbers there
  predate later units, so locate every site by symbol name, never by the recorded
  number.
- Standing condition: `guides/markdown.md` fails `npx oxfmt --config .oxfmtrc.json
  --check` at the baseline — the disagreement is confined to the `## Methods` table
  region (near lines 187-196). That red is yours to close (deliverable 4); every other
  file is format-clean.
- Baselines: `src:core` `602 passed (602)`; `test:guides` `18 passed (18)`.

## Scope

- Owned: `src/core/helpers.ts`, `src/core/parsers.ts`, `guides/markdown.md`.
- Off-limits: everything else, all of `tests/` included. The R6 derivation keeps
  `splitTableRow` behavior identical, so no test row goes false; a red row is a
  deviation, never an edit.

## Deliverables

1. **One home for the escaped-pipe rule (R6).** `splitTableRow` re-implements the
   escaped-pipe splitting rule `splitTableSources` owns. Per the ruling, derive the
   string form from the source form: `splitTableRow` returns
   `splitTableSources(...).map((cell) => cell.text)` (adapted to the real signatures),
   so one implementation owns the syntax rule. `splitTableRow` keeps its name, export,
   and exact observable behavior for its consumers (`delimiterToAlignments` and the
   validator site).
2. **The boundary synonym (R6).** Rename the `limit` parameter to `end` in `parseBlocks`
   and `collectList` in `src/core/parsers.ts` — declaration, internal reads, and any
   TSDoc naming it — and update the guide rows that document the parameter (the
   reconciliation names the rows near `guides/markdown.md:64` and `:100`; locate by the
   symbol they document).
3. **The scan-leaf TSDoc (R5).** `scanLink` and `scanEmphasis` ship without the
   description, `@example`, and `@param depth` degrade note their `locateLink` and
   `locateEmphasis` sources kept; `scanInlineSource` ships without an `@example`. Port
   the kept documentation from each `locate*` twin onto its `scan*` leaf, adapted to the
   leaf's actual signature and behavior, and give `scanInlineSource` an `@example` whose
   readings you execute through a throwaway `tmp/probe/` instrument before writing (a
   fence or example states an executed reading; delete the instrument).
4. **The format re-pad.** Bring `guides/markdown.md` to `oxfmt --check` clean by porting
   the formatter's own output for the `## Methods` table region: run the formatter on
   the file, keep only the region it disputes, and confirm the cell text is unchanged —
   the diff outside padding is empty.

## Execution

You perform the assignment directly and spawn nothing. Validate scoped, cheap-first:

```text
npx oxfmt --config .oxfmtrc.json --check guides/markdown.md src/core/helpers.ts src/core/parsers.ts
npx --no-install oxlint --config .oxlintrc.json --deny-warnings src/core/helpers.ts src/core/parsers.ts
npm run check
npx vitest run --config vite.config.ts --no-cache --reporter=dot --project src:core
npm run test:guides
```

## Deviation contract

Stop and report — expected, found, exact evidence — when the derivation cannot keep
`splitTableRow` behavior identical, when the rename forces an edit outside the owned
set, when a test row reds, or when the executed `@example` reading contradicts what the
`locate*` twin's documentation claims. Ancillary TSDoc wording inside the ported shape is
yours to decide and record.

## Acceptance criteria, cheap-first

1. Scoped format check exit 0 — the guide included, from its recorded red baseline.
2. Scoped lint exit 0; `npm run check` exit 0.
3. `grep -c 'limit' src/core/parsers.ts` reports only hits outside the renamed
   parameter's sense; name each survivor and its sense.
4. `src:core` exit 0 at `602 passed (602)` unchanged; `test:guides` exit 0 at
   `18 passed (18)`.
5. A writing-rules sweep over your added and changed prose lines passes, pattern and
   population named.

## Output

Write the report to `/home/user/lsp/tmp/units/h2.4-mechanical-report.md`: each
deliverable with its before and after (the derivation's diff, the rename's site list,
the ported TSDoc, the re-pad's confirmation that only padding moved), the executed
`@example` readings, the gate readings with exit codes, the sweep, and the actual
`git status --short` and `git diff --stat` output. Your final message is a short summary
naming the report path.
