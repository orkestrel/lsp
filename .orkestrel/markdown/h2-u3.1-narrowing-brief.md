# Unit H2-U3.1 — the block assert helpers accept an indexed child

Role and engine: `builder`, Sonnet, native subagent working in `/home/user/markdown` as its sole
writer. You perform this assignment directly and spawn nothing. This is a fix unit: it closes the
root-project diagnostics the H2-U3 unit's test rows introduced, found by the Orchestrator's
tree-wide check on 2026-08-26. The H2-U3 brief and report sit at
`/home/user/lsp/.orkestrel/markdown/h2-u3-threading-brief.md` and `h2-u3-threading-report.md`;
this unit supersedes nothing in them — it repairs one omission their scoped gates could not see.

Read before editing: the markdown repository's `AGENTS.md` § Non-negotiable rules and
`.claude/rules/tests.md` § helpers.

## Standing tree state, named so you do not stop on it

The markdown tree is a held round and is DIRTY by design: the H2 units U1 through U5 sit
uncommitted across `src/core/types.ts`, `src/core/parsers.ts`, `src/core/helpers.ts`,
`src/core/Markdown.ts`, `guides/markdown.md`, and the core test files. Never revert, stash, or
reset anything. The round commits as one commit after the Orchestrator's gate chain. The `guides`
vitest project stays red until H2-U6 lands; that red is not yours.

The defect: `configs/src/tsconfig.core.json` includes only `src/core/**`, so the scoped
`check:src:core` gate never compiles `tests/`. The root `tsconfig.json` compiles everything under
`noUncheckedIndexedAccess`, and `npx tsc --noEmit -p tsconfig.json` exits 2 today with exactly
these diagnostics, all TS2345 `BlockNode | undefined` is not assignable to `BlockNode`, all in
`tests/src/core/parsers.test.ts`, at lines 765, 791, 804, 820, 821, 832, 842, 858, and 871
(measured 2026-08-26). Every site passes an indexed child — `document.children[0]` or
`outer.items[0]?.children[1]` — directly to a block assert helper.

## Objective

The root project compiles clean: every block assert helper in `tests/setup.ts` accepts
`BlockNode | undefined` and narrows the `undefined` itself, the shape the same file already gives
`assertParagraphNode` and `assertEmphasisNode`.

## The work — fully specified

In `/home/user/markdown/tests/setup.ts`, change `assertHeadingNode`, `assertListNode`,
`assertTableNode`, `assertCodeBlockNode`, and `assertBlockquoteNode` to the exact shape
`assertParagraphNode` carries at `tests/setup.ts:81-86`:

```ts
export function assertHeadingNode(block: BlockNode | undefined): HeadingNode {
	if (block === undefined || !isHeadingNode(block)) {
		throw new Error(`expected heading, got ${block?.element}`)
	}
	return block
}
```

Apply the same widening, guard, and `${block?.element}` message to each of the named helpers,
keeping each one's element word in its message. Change nothing else: no test row edits, no new
helper, no signature change beyond the parameter type, no `any`, no assertion, no suppression.

## Output

Your final report, written to `/home/user/lsp/tmp/units/h2-u3.1-narrowing-report.md` and returned
as your final message: the helpers changed; the red reading (`npx tsc --noEmit -p tsconfig.json`
exit 2 with the nine diagnostics) and the green reading (exit 0) with both commands verbatim; the
scoped gate readings listed under acceptance; `git diff --stat` and `git status --short`;
observations outside scope, each named against the unit that owns it.

## Deviation contract

A conflict with the objective stops the unit: report expected, found, exact evidence, done or not
done, and at most one short hypothesis. Named stop conditions: a diagnostic outside the nine
survives the fix or a new one appears anywhere in the root project; any test in
`tests/src/core/parsers.test.ts` or `tests/src/core/Markdown.test.ts` reddens. Message wording
beyond the prescribed shape is yours to decide, record, and carry on from.

## Acceptance criteria

Ordered cheap-first.

1. `git status --short` shows changes only in `tests/setup.ts` beyond the standing held entries.
2. `npx oxfmt --config .oxfmtrc.json --check tests/setup.ts` and
   `npx oxlint --config .oxlintrc.json --deny-warnings tests/setup.ts` exit 0.
3. `npx tsc --noEmit -p tsconfig.json` exits 0.
4. `npm run test:src:core -- tests/src/core/parsers.test.ts tests/src/core/Markdown.test.ts`
   exits 0 — the type widening changes no behavior.

## Review evidence

The Orchestrator captures the actual diff and status after you exit; the H2 round audit covers
this diff with the round.
