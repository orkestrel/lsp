# Unit h2.3-fences — executed proofs for the guide's provenance fences (R4)

## Role and engine

You are the `implementer` lane, Claude Opus 5, native, writing directly in
`/home/user/markdown`. Do the work yourself and spawn nothing. You are the only writer in that
tree for the unit's duration. You do not commit, push, or run `git checkout`, `git restore`,
`git stash`, `git reset`, or `git clean` — an edit you must undo, you undo exactly, by hand. You
do not accept your own work; the round's Sol `analyst` audit covers this unit and the
`h2.2.1-prose` corrections together.

## Read first, in order

1. `/home/user/markdown/AGENTS.md`, then `.claude/rules/tests.md`, `.claude/rules/typescript.md`,
   and `.claude/rules/documentation.md` in that repository. The documentation rule this unit
   exists for: where a prose claim about behavior sits under no fence with an executed assertion,
   add the assertion that would break if the claim went false.
2. `/home/user/lsp/.orkestrel/markdown/h2-audit-reconciliation.md` — the round's charter. Its R4
   ruling is this unit's mandate, and its Amendment 2026-08-26 fixes the derivation-resolution
   rule the fences describe: own region, else the direct input's region, else `undefined`, never
   a further derivation edge.
3. `/home/user/markdown/guides/markdown.md` § Source provenance — the corrected prose whose
   fences you transcribe. You do not edit the guide.

No skill is named.

## Standing conditions (verified 2026-08-26)

- `/home/user/markdown` is CLEAN at commit `e4c434c` on the `claude/lsp-spec-audit-est33d`
  branch. That commit carries the `h2.2-prose` and `h2.2.1-prose` corrections. Dependencies are
  installed.
- The `src:core` project reads `592 passed (592)` at this baseline
  (`npx vitest run --project src:core`, exit 0, run by the `h2.2.1-prose` unit at the same tree
  state). Re-derive your own baselines for the files you touch before editing.
- The `## Source provenance` section spans `guides/markdown.md:249-403` at this commit, and its
  `ts` fences open at lines 254, 295, 312, 330, 362, and 386
  (`grep -n '^\x60\x60\x60' guides/markdown.md` over that range). The parse-pipeline paragraph at
  line 229 carries the boundary rule in prose with no fence of its own.
- The measured readings behind the corrected prose, taken by the Orchestrator through the real
  `projectSpan` via the `@src/core` barrel: the cascade reading
  (`segments [{offset 0, start 0, end 1}, {offset 1, start 5, end 5}, {offset 1, start 9, end 10}]`,
  `projectSpan 1..1 -> {"start":9,"end":9}`) and the bridge reading (over
  `{ text: 'a\nb', segments: [{offset 0, start 0, end 1}, {offset 2, start 1, end 2}] }`,
  `projectSpan 0..3 -> {"start":0,"end":2}`, `1..2 -> undefined`, `2..3 -> {"start":1,"end":2}`).
  A transcription touching either behavior asserts these values.

## The assignment

Transcribe every behavioral claim the § Source provenance fences make into executed rows in the
round's own test files, so the guide's largest behavioral surface carries proof of what ships.

- A fence's commented reading is an assertion: where a fence comment states a value
  (`// { start: 2, end: 4 }`, `// undefined`), the transcribed row runs the fence's real calls
  and asserts that exact value. A substring or presence check closes nothing.
- Name each row for the behavior it proves, never for the fence or the ruling that specified it.
- Cover the section's prose rules that the fences exercise: the verbatim-slice law (a region
  slices the constructor string, value normalization notwithstanding), the handle-relative rule,
  the rewrite derivation order the Amendment fixes, the provenance stops (adopted document,
  inbound projection, fresh multi-source rewrite output), and the boundary rule where a fence
  reaches `projectSpan`. A prose rule in the section that no fence exercises and no existing row
  pins gets a row only where the section's own examples supply the input; invent no fixture the
  guide does not describe, and report any such uncovered rule as an observation instead.
- Reuse an existing row rather than duplicating it: where a landed test already asserts a fence's
  exact reading, name that row in your report instead of transcribing a sibling.
- The vendored parity drop-in (`tests/guides.test.ts`) is off-limits, per the R4 ruling.

## Unknowns, named as unknowns

- **Which fence readings existing rows already pin.** The `h2.1-derive` unit landed red-first
  rows on the derivation chain and the zero-width boundary in `tests/src/core/Markdown.test.ts`
  and `tests/src/core/helpers.test.ts`. Enumerate the overlap before writing, and report the
  reuse table: fence reading → transcribed row or named existing row.
- **Whether a fence's commented value is true of the shipped code.** The `h2.2-prose` and
  `h2.2.1-prose` corrections were prose-only, so a fence comment can still carry a stale value.
  Run each fence's calls before transcribing; a reading that contradicts its comment is a
  primary-objective deviation — stop and report it, because the guide is then still false and
  the fix belongs to a prose unit, not to this one.

## Scope

- Owned: `tests/src/core/Markdown.test.ts` and `tests/src/core/parsers.test.ts`, scoped to the
  rows you add, plus `tmp/probe/` for a throwaway probe you delete before returning.
- Off-limits: everything under `src/` and `guides/`, `tests/guides.test.ts`, every other test
  file, `vite.config.ts`, `package.json`, the lockfile, and everything under `/home/user/lsp`
  except reading the reconciliation.
- Allowed tools: read, edit, and the acceptance-criteria commands.

## Deviation contract

A fence whose executed reading contradicts its comment, or a transcription that would require a
`src/` or `guides/` edit, stops the unit: report expected, found, the exact command and output,
done or not done, and at most one short hypothesis. Row placement, describe-block organization,
and fixture wording are yours to decide and record.

## Acceptance criteria, cheap-first, each command and output recorded

1. `npx oxfmt --config .oxfmtrc.json --check tests/src/core/Markdown.test.ts tests/src/core/parsers.test.ts`
   exits 0.
2. `npx --no-install oxlint --config .oxlintrc.json --deny-warnings tests/src/core/Markdown.test.ts tests/src/core/parsers.test.ts`
   exits 0.
3. `npm run check` exits 0.
4. `npx vitest run --project src:core` exits 0, with every added row collected and named in the
   output and no prior row lost (the baseline is `592 passed (592)`; record the count the run
   reports).
5. The writing-rules sweep over your added rows' names and comments
   (`git diff -U0` added lines against the substitution table and the count ban) reports clean
   or every hit ruled permitted, with the pattern named.

## Review evidence

This is a code change: your report carries the actual `git diff` and the actual
`git status --short` output.

## Output

Write your report to `/home/user/lsp/tmp/units/h2.3-fences-report.md`: the reuse table (fence
reading → transcribed row or named existing row), each added row with the fence claim it
executes, any uncovered prose rule reported as an observation, the gate readings with exit
codes, the diff, the status output, and any claim you flag for the analyst. No process diary.
