# h2.4.1 — the review's prescribed one-line repairs, Orchestrator-owned

The h2.4 review's findings outside the claims prescribed the exact repairs, so the
Orchestrator adopted them verbatim as a typo-class direct unit, 2026-08-26. The auditing
engine (Opus 5, the h2.4 reviewer) is one the Orchestrator does not share, and the
prescription precedes the edit.

## The edits

- `src/core/helpers.ts:1242` and `:1245`: the local `itemLimit` renames to `itemEnd`, so
  the boundary concept R6 renamed carries one term inside `collectList` too.
- `src/core/helpers.ts:839`: `{@link locateEmphasis} - and returns` becomes
  `{@link locateEmphasis}, and returns`, matching the `scanLink` sibling's punctuation.

## The referral, settled by measurement

The review referred the derived `splitTableRow`'s runtime equivalence on a row ending in a
lone backslash and a row with a whitespace-only interior cell — reasoned, not executed, in
a shell-less lane. The Orchestrator's instrument
(`h2.4-split-equivalence-instrument.ts`, run as a `tmp/probe` project test) compared the
derived body against the removed loop copied verbatim from the diff: both referral inputs
and a deterministic 3000-row corpus over pipe, escaped-pipe, backslash, whitespace, and
astral atoms agree exactly, and the negative control — a rule keeping the outer empty
cells — reports differences, so the comparator can fail. Reading:
`3 passed (3)`, exit 0, 2026-08-26.

## Gates after the edits

Scoped format, lint, and `npm run check` exit 0; the full `npm test` chain exit 0 —
`src:core` `602 passed`, policy `93`, config `46`, setup `24`, guides `18`. `itemLimit`
has zero word-boundary survivors in `src/core/helpers.ts`; `itemEnd` sits at the two
prescribed sites.

## Carried, not repaired here

The review's pre-existing observations — the `via` hits at `guides/markdown.md:158`,
`:194`, `:227`, `:453`, `src/core/helpers.ts:2669`, `src/core/parsers.ts:193`, and the
imperative TSDoc openers at `helpers.ts:434` and `:500` — predate the H2 round and go to
the markdown vocabulary-sweep row in the campaign's registered capabilities.
