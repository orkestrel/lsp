# Unit H2-U3.1 report — the block assert helpers accept an indexed child

## Helpers changed

In `/home/user/markdown/tests/setup.ts`, `assertHeadingNode`, `assertListNode`, `assertTableNode`,
`assertCodeBlockNode`, and `assertBlockquoteNode` now accept `BlockNode | undefined` and narrow the
`undefined` themselves, matching the shape `assertParagraphNode` already carries. Each keeps its own
element word in its thrown message and its `${block?.element}` interpolation. Nothing else changed:
no test row edits, no new helper, no signature change beyond the parameter type.

## Red reading (before the edit)

Command: `npx tsc --noEmit -p tsconfig.json`

Exit code: `2`, with the nine diagnostics the brief named — all `TS2345` `BlockNode | undefined` is
not assignable to `BlockNode`, at `tests/src/core/parsers.test.ts:765,791,804,820,821,832,842,858,871`.
This reading was taken by the Orchestrator on 2026-08-26 and is recorded in the brief; this unit did
not retake it, per the brief's fully specified change.

## Green reading (after the edit)

Command: `npx tsc --noEmit -p tsconfig.json`

Output: (none)
Exit code: `0`

## Scoped gate readings

1. `git status --short` — shows changes only in `tests/setup.ts` beyond the standing held H2
   entries (see status output below). PASS.
2. `npx oxfmt --config .oxfmtrc.json --check tests/setup.ts` — "All matched files use the correct
   format." Exit `0`. PASS.
   `npx oxlint --config .oxlintrc.json --deny-warnings tests/setup.ts` — no output, exit `0`. PASS.
3. `npx tsc --noEmit -p tsconfig.json` — exit `0`. PASS.
4. `npm run test:src:core -- tests/src/core/parsers.test.ts tests/src/core/Markdown.test.ts` —
   `Test Files 2 passed (2)`, `Tests 177 passed (177)`. Exit `0`. PASS.

## `git diff --stat`

```
 guides/markdown.md              |  78 ++--
 src/core/Markdown.ts            |  98 +++++-
 src/core/helpers.ts             | 764 ++++++++++++++++++++++++++++++++--------
 src/core/parsers.ts             | 167 +++++++--
 src/core/types.ts               | 116 +++++-
 tests/setup.ts                  |  30 +-
 tests/src/core/Markdown.test.ts | 106 +++++-
 tests/src/core/helpers.test.ts  | 452 ++++++++++++++++++++++--
 tests/src/core/parsers.test.ts  | 217 +++++++++++-
 9 files changed, 1748 insertions(+), 280 deletions(-)
```

All rows except `tests/setup.ts` are the standing H2 U1-U5 round, present before this unit started
and untouched by it.

## `git status --short`

```
 M guides/markdown.md
 M src/core/Markdown.ts
 M src/core/helpers.ts
 M src/core/parsers.ts
 M src/core/types.ts
 M tests/setup.ts
 M tests/src/core/Markdown.test.ts
 M tests/src/core/helpers.test.ts
 M tests/src/core/parsers.test.ts
```

## Observations outside scope

- The `guides` vitest project stays red until H2-U6 lands. That red is not this unit's; it is named
  standing state in the brief and owned by H2-U6.

## Deviations

None. No diagnostic outside the nine named survived the fix, no new diagnostic appeared anywhere in
the root project, and neither `tests/src/core/parsers.test.ts` nor `tests/src/core/Markdown.test.ts`
reddened.
