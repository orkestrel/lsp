# Audit brief — unit h2.1-derive, reviewer lane

## Role and engine

You are the `reviewer` lane, Claude Opus 5, native, read-only. You audit; you never edit, never
accept, and spawn nothing. Your tools are Read, Grep, and Glob — you cannot execute commands, so
the Orchestrator has run every behavioral probe and supplies the records named under Review
evidence. You rule on the evidence supplied plus what you read in the trees.

## Subject and chain

The subject is the `h2.1-derive` fix unit, written by GPT-5.6 Sol, landed inside the markdown
checkpoint commit `d310eea` on the `claude/lsp-spec-audit-est33d` branch of `/home/user/markdown`.
You are the cross-engine auditor: Sol wrote this fix, so attack it as the engine that did not.

The chain: the H2 provenance round (units U1 through U6) landed span provenance across the parse
and rewrite surfaces; its round audit ran a subjective lane (PASS with findings) and an objective
lane (FAIL), reconciled in `/home/user/lsp/.orkestrel/markdown/h2-audit-reconciliation.md`; the
`h2.1-derive` unit repairs rulings R2 (the derivation chain answered with another node's region)
and R3 (`projectSpan` resolved a zero-width boundary position to the earlier segment). Rulings R1,
R4, R5, and R6 are carried by later units — they are out of this audit's scope and re-reporting
them is not a finding.

## What this round decides

Whether the `h2.1-derive` fix is accepted. On PASS, the remaining fix units (`h2.2-prose` first)
build on this behavior as settled. On FAIL, a successor fix round runs before anything else in
this repository. A finding is worth more than a clean pass: the alternative is the prose unit
documenting behavior that is still wrong.

## Already established — verified by the Orchestrator directly; do not re-derive

The Orchestrator produced these with its own runs, not from the writer's report; the record with
exact commands and outputs is `/home/user/lsp/.orkestrel/markdown/h2.1-audit-evidence.md`:

- Scope honesty: reconstruction of the pre-h2.1 tree shows the unit changed exactly its owned
  files — `src/core/Markdown.ts`, `src/core/helpers.ts`, `tests/src/core/Markdown.test.ts`, and
  `tests/src/core/helpers.test.ts` — and nothing else. The isolated diff is
  `/home/user/lsp/.orkestrel/markdown/h2.1-derive-diff.txt`.
- The two named rows pass at `d310eea` and fail under the pre-fix sources with exactly the
  writer's reported counts, and the revert reddened exactly those rows
  (`Tests 2 failed | 345 passed (347)` over the two files).
- The full `src:core` project passes at `d310eea`: `Tests 592 passed (592)`, exit 0.
- The tree is clean at `d310eea`; `git status --porcelain` prints nothing.

## Numbered falsifiable claims

Attempt to refute each. CONFIRMED requires naming the attack you tried that failed. A claim you
cannot decide is UNRESOLVED, not CONFIRMED — say what would settle it. Do not hedge toward an
imagined consensus.

1. **The implementation is exactly the stated rule.** `Markdown.#derive` resolves each output node
   from that node's own prior span, else from the span of the direct input the current rewrite
   names for it, and follows no further derivation edge; no code path in `src/core/Markdown.ts`
   still walks past the direct input, and `rewriteDocument` in `src/core/helpers.ts` records
   nothing the rule cannot resolve. Attack by reading the implementation for a remaining multi-hop
   path or a recorded edge that reintroduces one.
2. **The counterexample resolves as ruled, through the public API.** The `T → S → C` construction
   from ruling R2 yields `undefined` for `T` while the separately emitted `S` keeps the
   `{ start: 6, end: 7 }` region, and the test row `does not cross an input identity into its
   separate output derivation` in `tests/src/core/Markdown.test.ts` pins exactly that construction
   through public exports — it reaches `#derive` through a real `map` chain and asserts the
   property rather than re-deriving it the way the source derives it. Attack the row's
   construction and its assertions against the ruling's recipe in the reconciliation.
3. **No refusal was widened into a regression by the `projectSpan` change.** The repair moves only
   the zero-width position that equals the next segment's offset; a nonzero-width span starting at
   a boundary, a zero-width position interior to a segment, and every `sliceSource` caller keep
   their prior answers, consistent with the documented segment contract in `src/core/types.ts`.
   Attack the changed condition in `src/core/helpers.ts` by constructing a caller pattern whose
   answer changed that the contract does not require to change. The Orchestrator's
   `592 passed (592)` full-project run bounds this from the standing suite's side; your attack is
   on cases the standing suite does not pin.
4. **The writer's flagged claim holds.** The writer flagged for you: a current rewrite never needs
   a second derivation edge, because the source handle's span map has already resolved earlier
   rewrites, and every current entry directly names the input node for that output role. Break it
   by constructing a public map construction where the direct input has no prior span but a later
   current-operation edge is still genuine provenance of the same output role. If you construct
   one, return the exact construction — nodes, maps, expected and actual spans — as the claim's
   refutation; the Orchestrator reproduces it before ruling, so precision matters more than
   certainty.
5. **The self-declared prose verdict holds.** The writer ruled that no sentence in
   `src/core/types.ts:413-420` or `guides/markdown.md:316-319` became false under the direct-input
   rule, and that the rewritten private comment in `src/core/Markdown.ts` states the rule that
   ships. This is a writer's sound-and-unchanged verdict — the kind least able to test itself.
   Read those sentences against the new behavior and attack each one; a sentence promising
   resolution the rule now refuses is a refutation.
6. **The held round survived intact.** Nothing the unit changed reverts or weakens what U1 through
   U6 landed: compare the isolated diff against the retained round diff
   `/home/user/lsp/.orkestrel/markdown/h2-round-diff.txt` for hunks the unit undid or assertions
   it deleted or loosened. Attack by finding a reverted hunk or a weakened standing assertion.

## Unknowns

Whether a public construction exists that breaks claim 4 is genuinely unknown to the Orchestrator
— the writer searched the scoped project and found none, and that search is the writer's. Report
your own conclusion with the construction or with the reasoning that bounds the search.

## Review evidence

- The subject's diff: `/home/user/lsp/.orkestrel/markdown/h2.1-derive-diff.txt` (isolated to the
  unit) and the live tree `/home/user/markdown` at `d310eea` (read anything).
- Status: the tree is clean; the status output is in the evidence record.
- Executed behavioral evidence: `/home/user/lsp/.orkestrel/markdown/h2.1-audit-evidence.md`.
- The unit's instruction and outcome: `h2.1-derive-brief.md` and `h2.1-derive-report.md` beside it.
- The charter: `h2-audit-reconciliation.md` (rulings R2 and R3), with the objective lane's
  counterexamples in `h2-audit-analyst-verdict.md`.

## Output

Return, as your final message, the verdict in the `orkestrel-falsify` shape and nothing else:
numbered verdicts in the claims' order, each exactly one of CONFIRMED, BROKEN, UNRESOLVED, or
NOT-EVIDENCED with its evidence; any finding outside the claims substantiated to the BROKEN
standard; and the single terminal line. No process diary.
