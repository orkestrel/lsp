# h2.3.1-rows — report

Unit: `builder`, engine Sonnet. Checkout `/home/user/markdown`, baseline `e5876ff`.

## Deliverable 1 — the cascade discriminating row

Added to `tests/src/core/helpers.test.ts`, inside `describe('projectSpan', …)`, immediately
after the existing "projects a zero-width abutment through the later segment" row:

```ts
it('resolves a zero-width position shared by several segments through the last one at that offset', () => {
	const source: MarkdownSource = {
		text: 'ab',
		segments: [
			{ offset: 0, start: 0, end: 1 },
			{ offset: 1, start: 5, end: 5 },
			{ offset: 1, start: 9, end: 10 },
		],
	}
	expect(projectSpan(source, 1, 1)).toEqual({ start: 9, end: 9 })
})
```

Discrimination control, run and removed before finishing:

- Changed the expectation to `{ start: 5, end: 5 }` (the superseded reading) and ran
  `npx vitest run --config vite.config.ts --no-cache --reporter=dot --project src:core -t "resolves a zero-width position shared by several segments"`.
  Red reading: `AssertionError: expected { start: 9, end: 9 } to deeply equal { start: 5, end: 5 }`.
- Restored the expectation to `{ start: 9, end: 9 }` and re-ran the same command. Green reading:
  `Test Files 1 passed | 5 skipped (6)`, `Tests 7 passed | 594 skipped (601)`.

## Deliverable 2 — the bridging rows

Added to `tests/src/core/helpers.test.ts`, beside the cascade row, one `it` covering the
bridging clause with the range's two boundaries and the uncovered-interior miss:

```ts
it('bridges an uncovered interior when both range boundaries resolve', () => {
	const source: MarkdownSource = {
		text: 'a\nb',
		segments: [
			{ offset: 0, start: 0, end: 1 },
			{ offset: 2, start: 1, end: 2 },
		],
	}
	expect(projectSpan(source, 0, 3)).toEqual({ start: 0, end: 2 })
	expect(projectSpan(source, 1, 2)).toBeUndefined()
	expect(projectSpan(source, 2, 3)).toEqual({ start: 1, end: 2 })
})
```

Executed reading: passes as part of the `projectSpan` scoped run (see gate output below).

## Deliverable 3 — the `scanInlineSource` move

- Deleted the `describe('scanInlineSource — coordinates inside one line', …)` block and its
  leading comment from `tests/src/core/parsers.test.ts:963-975`.
- Deleted the now-unused `import type { MarkdownNode, MarkdownSpan } from '@src/core'` line and
  the `scanInlineSource` entry from the value import in that file; `splitLines` stays, because
  `parsers.test.ts:740` and `:750` still use it.
- Re-added the block verbatim in `tests/src/core/helpers.test.ts`, beside the existing
  `describe('scanInlineSource', …)` block, row name and assertions unchanged. No new import was
  needed there: `MarkdownNode`, `MarkdownSpan`, `scanInlineSource`, and `splitLines` were already
  imported in that file.

`grep -c 'scanInlineSource' tests/src/core/parsers.test.ts` reports `0`.

## Deliverable 4 — the trailing-space region row

Added to `tests/src/core/Markdown.test.ts`, under `describe('Markdown — span', …)`, beside the
escape-decoding row:

```ts
it('reports the region across a trimmed trailing space, the trimmed space included', () => {
	const source = 'a \nb'
	const markdown = new Markdown(source)
	const [text] = markdown.filter(isTextNode)
	expect(text?.value).toBe('a\nb')
	const region = text === undefined ? undefined : markdown.span(text)
	expect(region).toEqual({ start: 0, end: 4 })
	if (region === undefined) throw new Error('expected text provenance')
	expect(source.slice(region.start, region.end)).toBe('a \nb')
})
```

Executed reading: passes as part of the `Markdown — span` scoped run (see gate output below).

## Gate outputs

```text
$ npx oxfmt --config .oxfmtrc.json --check tests/src/core/helpers.test.ts tests/src/core/Markdown.test.ts tests/src/core/parsers.test.ts
Checking formatting...
All matched files use the correct format.
Finished in 29ms on 3 files using 4 threads.
exit 0

$ npx --no-install oxlint --config .oxlintrc.json --deny-warnings tests/src/core/helpers.test.ts tests/src/core/Markdown.test.ts tests/src/core/parsers.test.ts
exit 0

$ npm run check
> tsc --noEmit --project tsconfig.json && npm run check:src
> npm run check:src:core
> tsc --noEmit -p configs/src/tsconfig.core.json
exit 0

$ npx vitest run --config vite.config.ts --no-cache --reporter=dot --project src:core
Test Files  6 passed (6)
Tests  602 passed (602)
exit 0
```

`602` from the `599 passed (599)` baseline: the move (deliverable 3) keeps the row count and
deliverables 1, 2, and 4 each add one `it`, `599 + 3 = 602`.

## Writing-rules sweep

Pattern: the full substitution alternation from `.claude/rules/writing.md` § Substitutions —
`should|simpl|eas[yi]|\bjust\b|currentl|\bnow\b|\bnew\b|latest|utiliz|leverag|\bvia\b|in order to|e\.g\.|i\.e\.|etc\.|performan|robust|allows you to|and/or|\bsince\b|\bonce\b|please|sanity|dumm|blacklist|whitelist|master|slave`,
case-insensitive.

Population: every added line in the diff over the owned files —
`git diff -U0 tests/src/core/helpers.test.ts tests/src/core/Markdown.test.ts tests/src/core/parsers.test.ts | grep '^+' | grep -v '^+++'`.

Result: `3` hits, all `new` in `new Markdown(source)` and `new Map<MarkdownNode, MarkdownSpan>()`
— literal code identifiers (the `new` keyword), exempt from every row per the substitution
table's header. No hit in a banned sense.

## `git status --short`

```text
 M tests/src/core/Markdown.test.ts
 M tests/src/core/helpers.test.ts
 M tests/src/core/parsers.test.ts
```

## `git diff --stat`

```text
 tests/src/core/Markdown.test.ts | 11 +++++++++++
 tests/src/core/helpers.test.ts  | 39 +++++++++++++++++++++++++++++++++++++++
 tests/src/core/parsers.test.ts  | 16 ----------------
 3 files changed, 50 insertions(+), 16 deletions(-)
```

## Deviations

None. Every specified expectation read as the brief stated when executed, the deletion in
deliverable 3 broke no row outside it, and the scoped suite reds nowhere outside the
discrimination control's deliberate, reverted change.
