# Unit h2.2-prose — the span-contract prose and the type openers

## Role and engine

You are the `implementer` lane, Claude Opus 5, native, writing directly in `/home/user/markdown`.
Do the work yourself and spawn nothing. You are the only writer in this tree for the unit's
duration. You do not audit or accept your own work; the Sol `analyst` lane audits this unit
afterwards.

## Read first, in order

1. `/home/user/markdown/AGENTS.md`, then `.claude/rules/typescript.md`,
   `.claude/rules/documentation.md`, and `.claude/rules/writing.md` in that repository.
2. `/home/user/lsp/.orkestrel/markdown/h2-audit-reconciliation.md` — ruling R1 and the type-opener
   half of R5 are this unit's charter, and the **Amendment 2026-08-26** section at its end is the
   authoritative derivation contract. The superseded criterion clause it names must not be
   documented.
3. `/home/user/lsp/.orkestrel/markdown/h2-audit-analyst-verdict.md` — the counterexamples that
   falsified the enumerated sentences.
4. `/home/user/lsp/.orkestrel/markdown/h2.1-audit-reviewer-verdict.md` — claim 5 reads each
   enumerated sentence against the accepted `h2.1-derive` behavior and is evidence about which
   sentences are already true.

No skill is named.

## Standing conditions (verified 2026-08-26)

- `/home/user/markdown` is CLEAN at commit `d310eea` on the `claude/lsp-spec-audit-est33d` branch.
  The `h2.1-derive` fix is accepted; the shipped behavior it landed is the contract your prose
  states.
- Dependencies are installed; `npx vitest run --project src:core` reports `592 passed (592)`,
  exit 0, at this commit.
- No `git checkout`, `git restore`, `git stash`, `git reset`, or `git clean`. No commit, no push,
  no install.

## Objective

Every span-contract sentence in the owned files is true of the shipped code, and the added public
TSDoc opens with a third-person verb. The shipped behavior is the contract (ruling R1); prose
changes, code does not.

The enumerated population, each site corrected or re-ruled true with its reason recorded:

- `MarkdownSpan` (`src/core/types.ts:44`) — state that slicing a span returns the original source
  region, which can include syntax the node's value drops and characters normalization removed.
- `MarkdownSegment` (`src/core/types.ts:64`) — the affine claim is false for compressed CRLF and
  trailing-space segments; state the boundary rule the code implements (`src/core/helpers.ts:232`),
  including that a zero-width position on a discontinuous abutment resolves through the later
  segment.
- `MarkdownSource` (`src/core/types.ts:87`) — segments do not cover every derived position; a
  fabricated join produces uncovered separators.
- `MarkdownParseResult` (`src/core/types.ts:397`) and `MarkdownInterface.span`
  (`src/core/types.ts:478`) — a parse-time coalesced run reports the enclosing region; only a
  rewrite output assembled from separate source nodes reports `undefined`.
- `MarkdownDerivation` (`src/core/types.ts:413-420`) — an absent entry does not mean retained
  identity alone (synthetic descendants are absent too), resolution prefers the output identity's
  own span, and the resolution follows the Amendment's direct-input rule: own region, else the
  direct input's region, else `undefined`, never a further edge.
- The guide's universal sentences at `guides/markdown.md:269`, `:281`, `:316`, and `:347` — each
  was falsified by the objective lane pre-`h2.1`; re-rule each against the shipped code and
  correct only what remains false. The reviewer's claim 5 found `:316-319` true of the accepted
  behavior — verify rather than assume, and record the ruling either way.
- The type-opener half of R5: the added public TSDoc on `MarkdownSpan`, `MarkdownSegment`,
  `MarkdownSource`, `MarkdownParseResult`, and `MarkdownDerivation` opens with a noun phrase where
  `.claude/rules/typescript.md` requires a third-person verb. Correct the openers while you
  correct the content.

## Unknowns

- Whether a guide sentence outside the enumerated population becomes false as a consequence of
  your corrections. Report any such sentence as an observation with its line; do not expand the
  edit beyond the population plus the sentences your own edits directly touch.

## Scope

- Owned: `src/core/types.ts` (prose and TSDoc only — no type shape, signature, or code change),
  `guides/markdown.md`.
- Off-limits: every other file, `src/core/Markdown.ts`, `src/core/helpers.ts`,
  `src/core/parsers.ts`, every test file, `package.json`, and the lockfile included.
- Allowed tools: read, edit, and the acceptance-criteria commands.

## Deviation contract

A correction that cannot land without changing a type shape, a signature, code, or an off-limits
file stops the unit: report expected, found, exact evidence, done or not done, and at most one
short hypothesis. Sentence wording, paragraph placement, and which clause carries a caveat are
yours to decide and record.

## Acceptance criteria, cheap-first

1. `npx oxfmt --config .oxfmtrc.json --check src/core/types.ts` exits 0 (the guide is excluded
   from the formatter population; record the command you ran).
2. `npx oxlint --config .oxlintrc.json --deny-warnings src/core/types.ts` exits 0.
3. `npm run check` exits 0.
4. `npx vitest run --project src:core` exits 0 with an unchanged pass count — a moved test is a
   deviation, not a fix.
5. The guides parity project, if the workspace registers one, exits 0; record the exact command
   and reading, or record that no such project exists with the command that proved it.

## Review evidence

This is a code change for audit purposes: your report carries the actual `git diff` for both
owned files and the actual `git status --short` output.

## Output

Write your report to `/home/user/lsp/tmp/units/h2.2-prose-report.md`: the sentence-by-sentence
ruling over the enumerated population (corrected, or re-ruled true, each with its reason), the
opener corrections, every observation from Unknowns, the gate readings with exit codes, the diff,
the status output, and any claim of your own you flag for the analyst. No process diary.
