# h2-round2 — objective lane verdict, verbatim

Lane: objective audit, engine Opus 5 (native, substituted for GPT-5.6 Sol; the bench is
dark for the Sol model on this account, recorded in the routing ledger). Brief:
`lsp/tmp/units/h2-round2-audit-brief.md`. Subject: the `h2.2.1-prose` corrections
(`e4c434c`) and the `h2.3-fences` transcription (`e5876ff`). Returned 2026-08-26. The
Orchestrator's reconciliation and the fix routing are in `h2-round2-audit-verdict.md`
beside this file.

---

1. **CONFIRMED** — Every finding the h2.2 analyst verdict broke reads closed in the landed files at head, checked against the verdict's own text rather than the unit's report. Cascade bullet: `src/core/types.ts:81-84` now states "a zero-width `p` that coincides with a later segment's `offset` resolves through the LAST segment whose `offset` equals `p`, skipping every earlier segment at that position whatever its extent". Derivation qualifier: `src/core/types.ts:449-452` carries "Where the output identity holds no region of its own, …" inline plus "Own-region resolution runs first". Guide coverage sentence: `guides/markdown.md:229` states the boundary rule and the bridging clause. Boundary rule at the second site: `src/core/types.ts:111-114` states the same rule for `MarkdownSource`. Class TSDoc resolution order: `src/core/Markdown.ts:27-31` states own region, else the direct input's region, else `undefined`. Attacks tried and failed: (a) closure by deletion — every finding is a rewritten sentence, none removed; (b) report-versus-tree divergence — each "after" block in `h2.2.1-prose-report.md` matches the landed bytes at those lines and the `e4c434c` capture; (c) prose-versus-code — derivation from `src/core/helpers.ts:228-247` and `src/core/Markdown.ts:208-225` confirms each corrected rule is what the code does.

2. **BROKEN** — The prose-versus-code half holds; the pinning half is false for the cascade rule. Prose-versus-code (derivation, `src/core/helpers.ts:236-241`): the `from === next.offset` guard continues past the current segment regardless of `segment.end - segment.start`, so resolution answers through the last segment whose `offset` equals `p` — the supplied cascade reading `1..1 -> {9,9}` and the unit's extent reading `2..2 -> {9,9}` both follow from that loop. Pinning rows: the fixed derivation order is pinned by `tests/src/core/Markdown.test.ts:258-270`, `:282-290`, and `:315-344`; the later-segment resolution for a zero-width boundary is pinned by `tests/src/core/helpers.test.ts:260-269`. The cascade rule has **no** pinning row. Falsifying state: the only `projectSpan` rows in the repository are `tests/src/core/helpers.test.ts:199, 240, 248-249, 257, 268, 272, 484` (pattern `projectSpan` over `/home/user/markdown/tests`, which reports matches in that file alone), and none supplies a source with two segments at one `offset`. `:260-269` uses `segments: [{ offset: 0, start: 0, end: 1 }, { offset: 1, start: 5, end: 6 }]` and asserts `{ start: 5, end: 5 }`, which is the identical answer under the **superseded** wording the h2.2 audit broke ("that LATER segment"), so the row cannot exclude the rival reading and pins nothing the correction added. What would settle it: a row in `tests/src/core/helpers.test.ts` asserting `projectSpan({ text: 'ab', segments: [{ offset: 0, start: 0, end: 1 }, { offset: 1, start: 5, end: 5 }, { offset: 1, start: 9, end: 10 }] }, 1, 1)` equals `{ start: 9, end: 9 }` — the superseded rule answers `{ start: 5, end: 5 }` there, so the row discriminates. Carrier: a unit owning `tests/src/core/helpers.test.ts`, which no unit in the reconciliation's remaining table owns.

3. **CONFIRMED** — Walked every `ts` fence in § Source provenance (`guides/markdown.md:254, 295, 312, 330, 362, 386`) against the `e5876ff` diff and the reuse table. Each behavioral reading has a carrier: heading region and slice → `Markdown.test.ts:65-74`; decoded-escape value, region, and spelling slice → `:76-85`; handle-free document span → `parsers.test.ts:764-767`; lowered value and region → `Markdown.test.ts:272-280`; adopted-HTML document `undefined` → `:114-120`; merged-output `undefined` → landed `:292-298`; `scanInlineSource` text and emphasis coordinates → `parsers.test.ts:966-975`. Attack tried and failed: every named reuse row exists in the real file at its current line and asserts the value the table claims — `:292-298` asserts `toBeUndefined()` for one fresh node returned for several text inputs, `:122-134` asserts cross-handle `undefined`, `:258-270` asserts `{ start: 9, end: 13 }` and the rebuilt ancestor's `{ start: 0, end: 13 }`, `:98-112` asserts `undefined` for a foreign node and every adopted node, `:87-96` asserts freshness, `:315-344` asserts `chained.span(replacement)` undefined with `chained.span(shared)` `{ start: 6, end: 7 }`. No fence reading is uncarried and no reuse row asserts something else.

4. **CONFIRMED** — Read against the real test sources, not the report's copy. `tests/src/core/Markdown.test.ts:71-73` asserts `{ start: 0, end: 7 }` and `slice` `'# Title'`; `:80-84` asserts `'a * b '`, `{ start: 0, end: 7 }`, and `slice` `'a \\* b '`; `tests/src/core/parsers.test.ts:766` asserts `{ start: 0, end: 25 }`; `Markdown.test.ts:278-279` asserts `'hi'` and `{ start: 2, end: 4 }`; `:286` and `:289` assert `{ start: 0, end: 2 }` before and after the rewrite; `:116` and `:119` assert `undefined`; `parsers.test.ts:972-973` assert `{ start: 2, end: 4 }` and `{ start: 4, end: 7 }`. Attacks tried and failed: (a) value drift between fence comment and row — none, each pair matches character for character; (b) a row that asserts a weaker thing than the fence — the escape row adds the `slice` assertion the fence comment implies, and the own-region row adds `toHaveLength(3)` proving the several-inputs precondition; (c) length arithmetic behind `{ start: 0, end: 25 }` — `'# Title'` (7) + `'\n\n'` (2) + `'A **bold** word.'` (16) = 25 (derivation).

5. **CONFIRMED** — The wider assertion is correct against the shipped resolution semantics and must not be narrowed. Derivation from `src/core/Markdown.ts:58-66`: a `MarkdownDocument` argument takes the adopted branch, which sets `this.#spans = new Map()`; `span` at `:91-94` reads only `#spans.get(node)`, so every node of an adopted tree resolves `undefined`, not the document alone. `tests/setup.ts:157-159` shows `projectHTML` returns a `MarkdownDocument`, so the row's handle is on that branch. The row's `expect(nodes.length).toBeGreaterThan(1)` is load-bearing rather than surplus: without it `nodes.every(...)` passes vacuously on an empty walk, which `.claude/rules/tests.md` § Discovery and adequacy audit requires guarding against. Executed corroboration is Orchestrator-supplied (`src:core` `599 passed (599)`, 2026-08-26); the semantics ruling is my derivation.

6. **BROKEN** — The placement is a conformance defect against `.claude/rules/tests.md` § Test contract, first bullet: "Mirror module/application structure: `tests/{src,app}/[environment]/[domain]/[module].test.ts`". `scanInlineSource` is declared at `src/core/helpers.ts:918`, whose mirrored suite is `tests/src/core/helpers.test.ts`; the added block sits at `tests/src/core/parsers.test.ts:966-975`, mirroring `src/core/parsers.ts`, which declares `parseBlocks` (`:46`), `parseDocument` (`:199`), `parseProvenance` (`:210`), and `parseInline` (`:227`) and does not declare `scanInlineSource`. Two aggravating facts the unit's recorded reasons do not address: `tests/src/core/helpers.test.ts:713-725` already carries a `describe('scanInlineSource', …)` block, so the tree now holds two describe blocks for one subject in two files; and `tests/src/core/parsers.test.ts:30-40`, the file's own header, states that the leaves those entry points compose "are helpers.ts leaves, covered in helpers.test.ts", which the added block contradicts. No clause permits it: `.claude/rules/tests.md` names `integration.test.ts` as the only filename whose scope is a directory rather than a module, and the same section rules that "A test the mirror rule flags is a misplaced test until its placement is checked. Move it to the location its scope names." Smallest correct fix: move the `describe` block and its leading comment verbatim into `tests/src/core/helpers.test.ts` beside `:713-725`, in a unit that owns that file — the same carrier claim 2's missing cascade row and claim 8's bridging row need.

7. **CONFIRMED** — Attack tried: read "the h2.2.1 unit's owned prose surfaces" at region granularity and found hunks outside them — `guides/markdown.md:192-193` (the surface-table `span` and `map` rows, outside the brief's bound of the coverage sentence plus § Source provenance), `src/core/types.ts` hunks in `MarkdownSpan` (`:38-52`), `MarkdownParseResult` (`:416-428`), and `MarkdownInterface.span` (`:506-524`) (the brief owned only the `MarkdownSegment`, `MarkdownDerivation`, and `MarkdownSource` remarks), and `src/core/Markdown.ts:79-80` (the brief owned the class-level block only). The attack fails on the record: those hunks are the `h2.2-prose` unit's work and the Orchestrator-integrated `@returns` hunk, `h2-audit-reconciliation.md:128` gives `h2.2-prose` whole ownership of `src/core/types.ts` and `guides/markdown.md`, the `@returns` hunk is named as already integrated in the h2.2.1 brief's standing conditions and was ruled CONFIRMED at `h2.2-audit-analyst-verdict.md:9`, and both the commit message (`e4c434c`, lines 11-15 of the capture) and the h2.3 brief's standing conditions state the commit carries `h2.2-prose` and `h2.2.1-prose` together. So no hunk is unowned and the record agrees with itself. Prose-only test: every `src/` hunk in `e4c434c` lies inside a `/** … */` block — the `span(node: MarkdownNode): MarkdownSpan | undefined` line and every other declaration appear as context, so no runtime line moved. `e5876ff` contains exactly two `diff --git` headers, `tests/src/core/Markdown.test.ts` and `tests/src/core/parsers.test.ts`, and its hunks match the landed files.

8. **CONFIRMED** — The gap is real and the report carries it. Read the whole suite: `tests/src/core/helpers.test.ts` holds every `projectSpan` row in the repository, and none projects across an uncovered interior — `:185-200` projects across a **recorded** separator segment `{ offset: 1, start: 1, end: 3 }`, `:214-227` builds the uncovered interior (`joinSources` over a blank source yields `segments: [{ offset: 0, start: 0, end: 1 }, { offset: 3, start: 2, end: 3 }]`) but asserts only the join and never calls `projectSpan`, and `:243-250` pins the unmapped-boundary `undefined`. So the bridging clause at `guides/markdown.md:229` and its twin at `src/core/types.ts:111-114` have no row. The h2.3 report names the carrier ("a unit that owns `helpers.test.ts`") and records the measured values a row would assert. One fact for the Orchestrator inside this observation rather than as a finding: `h2-audit-reconciliation.md:127-131` gives no remaining unit ownership of `tests/src/core/helpers.test.ts` — `h2.4-mechanical` owns `src/core/helpers.ts` scoped to `splitTableSources` and the scan-leaf TSDoc, `src/core/parsers.ts`, and guide rows — so the named carrier is unallocated until a re-baseline allocates it.

9. **CONFIRMED** — Population: the added and changed lines the two captured commits name — `guides/markdown.md:192-193, 229, 269-283, 285-293, 323-328, 344-347, 356-360`; `src/core/Markdown.ts:27-31, 79-80`; `src/core/types.ts:38-52, 61-86, 97-114, 416-428, 434-456, 506-524`; and in `e5876ff` the added lines at `tests/src/core/Markdown.test.ts:12, 65-85, 114-120, 272-290` and `tests/src/core/parsers.test.ts:1, 25, 764-767, 963-975`. Instrument: case-insensitive Grep over the four real files with `should|simpl|eas[yi]|\bjust\b|currentl|\bnow\b|\bnew\b|latest|utiliz|leverag|\bvia\b|in order to|e\.g\.|i\.e\.|etc\.|performan|robust|allows you to|and/or|\bsince\b|\bonce\b|please|sanity|dumm|blacklist|whitelist|master|slave`, then filtered to those line ranges; its coverage is text alone, so it reports on spellings in that alternation and nothing else. Substitution hits inside the population: `guides/markdown.md:193` `NEW` — permitted, it names a fresh instance rather than dating one, the same sense `h2.2-audit-analyst-verdict.md:13` already ruled; the `new` hits in the test rows (`new Markdown(…)`, `new Map<MarkdownNode, MarkdownSpan>()`) are literal code identifiers, exempt from every row. Every other hit in the four files (`guides/markdown.md:226, 227, 258, 299, 349, 366, 369, 391`; `src/core/Markdown.ts:20, 35, 37, 39, 46, 62, 65, 85, 137, 140, 143, 178, 187, 204, 212`; `src/core/types.ts:328, 412, 469, 477, 526`; `Markdown.test.ts:138, 470, 668`; `parsers.test.ts:302, 626, 628, 686`) falls outside the changed ranges. Count ban over the same population, ruled hit by hit: `{ start: 0, end: 4 }`, `{ start: 0, end: 7 }`, `{ start: 0, end: 25 }`, `{ start: 2, end: 4 }`, `toHaveLength(3)`, `toBeGreaterThan(1)` are measured values; "one derived code unit over a two-unit original region" is a size; "the range's two boundaries" and "when both boundaries resolve" name a pair that cannot grow and the sentence names its members; "the LAST segment whose `offset` equals `p`" selects an element of a runtime array rather than naming a prose list item; "Own-region resolution runs first" orders a step in time; "never follows a second derivation edge" names resolution depth; "coordinates inside one line" names a scope. No hit in a banned sense.

**Findings outside the claims**

**BROKEN — the trailing-space region rule is a § Source provenance behavior claim with no executed row and no recorded observation.** `guides/markdown.md:277-279` states "in `'a \nb'` the paragraph phase trims the trailing space, so the node values `a\nb` while its region is `{ start: 0, end: 4 }` — the whole `a \nb`, trimmed space included", and `src/core/types.ts:49-50` repeats it as "The text node of `'a \nb'` values `a\nb` and reports `{ start: 0, end: 4 }`". The rule is true of the shipped code (derivation: `splitLines` yields `{ text: 'a ', segments: [{ offset: 0, start: 0, end: 2 }] }` and `{ text: 'b', segments: [{ offset: 0, start: 3, end: 4 }] }`; the retained coordinates plus the recorded separator make `projectSpan(joined, 0, 3)` resolve `start = 0` and, at `to === limit` on the final run, `end = 4` through `src/core/helpers.ts:244-245`). No test asserts it: Grep over `/home/user/markdown/tests` for the fixture spelling `a \\nb` returns no match, and for `start: 0, end: 4` returns only `tests/src/core/helpers.test.ts:432`, a `stripQuote` row over `'> hi'`. The nearest rows prove different behaviors — `tests/src/core/parsers.test.ts:782-792` pins the escape spelling and excludes the line's surrounding indent, and `tests/src/core/helpers.test.ts:286-296` pins `normalizeParagraphLine` at helper granularity, not the node region. This rule sits inside the section the h2.3 brief assigned, its fixture is supplied by the section itself rather than invented, and the brief required "report any such uncovered rule as an observation instead" — the h2.3 report's Observations section records only the `projectSpan` bridging clause, so this gap is neither transcribed nor carried. `.claude/rules/documentation.md` § Parity requires the executed assertion wherever a prose claim about behavior sits under no fence. Smallest correct fix: add a row to `tests/src/core/Markdown.test.ts` › `Markdown — span` asserting, for `new Markdown('a \nb')`, that the text node values `'a\nb'` and that `span` reports `{ start: 0, end: 4 }` slicing `'a \nb'`.

VERDICT: FAIL -- 2 broken, 0 unresolved, 0 not-evidenced, 1 findings outside the claims
