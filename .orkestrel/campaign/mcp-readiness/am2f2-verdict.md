This is a source review of commit `afca2f8` against `tmp/audit/m2f2.diff`, with `tmp/audit/runs.txt` as the executed suite record. Attacks that needed a live interleaving were walked on the shipped `send` → `#stamp` → `#exchange` → `#deliver` → `#select` path.

1. **CONFIRMED** — The guard closes the named races. Cursorless `#stamp` increments `#generation` first, then records it; a continuation records the generation already current (`src/server/transports/HTTPClientTransport.ts:149-153`, identical at `src/browser/transports/HTTPClientTransport.ts:151-155`). `#select` clears and `set`s only when `this.#stamps.get(sent) === this.#generation` (`src/server/transports/HTTPClientTransport.ts:300-301,318` and `src/browser/transports/HTTPClientTransport.ts:298-299,316`).

   - Stale continuation after a newer fresh listing: continuation stamped with the old generation; the later cursorless send moves `#generation`; the late continuation is not current. Pinned on both faces (`tests/src/server/transports/HTTPClientTransport.test.ts:906`, `tests/src/browser/transports/HTTPClientTransport.test.ts:396`). `tmp/audit/runs.txt` records those suites green.
   - Two fresh listings out of order: each cursorless send increments, so the earlier stamp is never current after the later send. The later arrival clears and caches; the earlier arrival does not. No merge.
   - Fresh listing and its own continuation overlapping: a well-formed continuation cannot be in flight before its fresh page resolved, because the cursor is taken from that page. The faces pin the in-order overlap (`tests/src/server/transports/HTTPClientTransport.test.ts:1021`, `tests/src/browser/transports/HTTPClientTransport.test.ts:508`).

   **Fabricated-cursor ruling.** A caller that invents a cursor can put a continuation in flight beside its fresh page. If that continuation arrives first and the fresh page then arrives, the fresh page’s current cursorless clear wipes the early continuation (`#select` clear is gated on `current && cursor === undefined`). Final table is the fresh page only — under-projection, the documented safe direction. If the fresh page never delivers, a fabricated continuation that already `set` can remain on the previous lineage’s still-standing table. That is a non-arrival plus a fabricated cursor, not an arrival order of well-formed paging, and `MCPClient.tools()` never sends a cursor (`src/core/MCPClient.ts:390-391,555-560`).

2. **CONFIRMED** — Delivery is ungated. `#capture` always emits `#select`’s return (`src/server/transports/HTTPClientTransport.ts:270`, `src/browser/transports/HTTPClientTransport.ts:268`). Inside `#select`, `current` wraps only `clear` and `set`. The unnamed-tool keep, the invalid-annotation `error` emit, `kept.push`, and the returned `{ ...message, result: { ...result, tools: kept } }` run on every modern `tools/list` result (`src/server/transports/HTTPClientTransport.ts:302-321`). The tripwire asserts the superseded page still arrives with the invalid definition dropped, the exclusion on `error`, and no projection (`tests/src/server/transports/HTTPClientTransport.test.ts:958`, `tests/src/browser/transports/HTTPClientTransport.test.ts:448`). No path was found where the guard skips or reorders a caller-visible message or `error`.

3. **CONFIRMED** — Stamp bookkeeping does not leak or misfire on shipped paths. `send` stamps, then passes the same object into `#exchange` → `#deliver` → `#capture` → `#select` (`src/server/transports/HTTPClientTransport.ts:135-140,188,252,270,294`). `#exchange` issues one `fetch` and does not retry or re-enter after `#generation` moves. `#select`’s current check is reached only for a modern `tools/list`, which is the same predicate `#stamp` records; an unstamped `WeakMap.get` is `undefined`, which is never `===` a numeric generation, so the miss cannot cache. `MCPClient.#request` allocates a new request object per call (`src/core/MCPClient.ts:555-560,592`). `tests/conformanceClient.ts` drives only `client.tools()` and `client.call()` (`tests/conformanceClient.ts:174,199,204-206`) and never reuses a sent message. Re-sending the same object through public `send` would overwrite the stamp; that path is not in `MCPClient` or the conformance driver.

4. **CONFIRMED** — The landed sentences match the code. Client-projection and both HTTP API clauses state that a listing another cursorless `tools/list` supersedes is delivered and caches nothing (`guides/mcp.md:4461-4463,5059-5061,5335-5338`), which is what `#select` does. The `#select` comment’s “server’s own bounded lookup stays the validation authority” is true of the server this package ships: a modern `tools/call` walks `tools/list` through `MCP_LOOKUP_PAGES` and validates with `inferParameterRefusal` against those served projections (`src/server/handlers.ts:138-180`, `src/server/inferers.ts:173-177,203-231`). The client table is not the authority. A name no served definition annotates is forwarded untouched, which is why under-projection is the safe client direction.

5. **CONFIRMED** — Carried findings landed as prescribed. The `DEFAULT_MCP_LIMITS` remarks match the M5 patch with only a rewrap: `64 keys admits `_meta`'s reserved keys plus many extensions, and bounds a produced result's breadth by the same leaf` (`src/core/constants.ts:163-165`; M5 prescription in `tmp/units/m5-report.md` at the `DEFAULT_MCP_LIMITS` TSDoc finding). No word changed. The `modern-method-not-found` row is the same `[name, invocation, response]` tuple as `unknown-method`, with method `server/discover` expecting `-32601` (`tests/src/core/MCPLegacy.test.ts:239-259`). That request has no modern `_meta`, so it reaches `#legacy`’s `default` (`src/core/MCPLegacy.ts:80-87,128,159-164`). A real `case 'server/discover'` in that switch would redden the row; a modern-only handler behind `isModernRequest` would not, which is the door this row owns.

6. **CONFIRMED** — Scope honesty holds. The diff touches exactly the report table: `guides/mcp.md`, both `HTTPClientTransport.ts` faces, both face tests, `src/core/constants.ts`, `tests/src/core/MCPLegacy.test.ts`. Implementation hunks add no `any`, non-null assertion, `as` assertion, ts-ignore family, or `eslint-disable`. New function values are anonymous callbacks passed as arguments (`new Promise((resolve) => { ... })` in the face tests).

No finding outside the claims.

VERDICT: PASS — 0 broken, 0 unresolved, 0 not-evidenced
