**1. CONFIRMED.** Attack: a `prompts/get` or `resources/read` `input_required` result with `inputRequests` leaves `#forward` without `#gate`. Failed: `#read` and `#prompt` are the only built-in handlers that can stamp that result, and both call `#forward` (`src/core/MCPServer.ts:534-536`, `680-682`, `327-341`). `#forward` runs `inputRequests` through `#gate` before the stamp (`1052-1061`). Attack: a continuation-only result is still gated, or is unsound. Failed: when `inputRequests` is absent the gate is skipped and the result is stamped (`1053-1068`); `isMCPInputResult` admits `{ resultType: 'input_required', requestState }` (`src/core/types.ts:675-679`); the fixture arms `memory://resource/input` and prompt `input` are that shape (`tests/setup.ts:572-574`, `tests/src/core/MCPServer.test.ts:188-190`, `6727-6749`). A consumer `methods.add` replacement of those names is not this package's path (`src/core/MCPMethodManager.ts:12-13`, `29-30`).

**2. CONFIRMED.** Attack: `MCPSampleContent` is not the schema `anyOf` because the array arm is missing. Failed as an admission mismatch: `CreateMessageResult.content` is one of Text/Image/Audio/ToolUse/ToolResult or an array of `SamplingMessageContentBlock` (`tests/mirrors/ext-tasks-2026-07-28-schema.json:1858-1880`, `2660-2676`); that set is `MCPSampleContent | readonly MCPSampleContent[]` on `MCPSampleResult.content` (`src/core/types.ts:441-446`, `640`). Attack: a resource arm is admitted, or a schema arm is refused. Failed: `isMCPSampleContent` keeps only `text`/`image`/`audio` after `isMCPContent` and owns `tool_use`/`tool_result` (`src/core/validators.ts:1937-1948`); `resource_link` and `resource` are refused (`tests/src/core/validators.test.ts:1100-1103`). Inner `tool_result.content` is `ContentBlock` ≡ `MCPContent` (`schema.json:1755-1772`, `2990-2995`). Empty arrays pass `every` and the schema array has no `minItems`.

**3. CONFIRMED.** Attack: the mixed payload's `form` member is a key `isFormElicitationSupported` never reads. Failed: that predicate returns true when `elicitation.form` is a record (`src/core/helpers.ts:90-93`). Payloads and the declaration that satisfies each, on the identical round (`computeMissingCapabilities`, which `#gate` calls at `src/core/MCPServer.ts:1036`):

- URL-only missing → `{ url: {} }`. Declare `elicitation: { url: {} }`. Passes (`tests/src/core/validators.test.ts:801-803`).
- Form-only missing → `{}`. Declare `elicitation: {}` (empty = form-only) or `{ form: {} }`. Passes (`helpers.ts:93`; `validators.test.ts:762-764`, `790-792`).
- Both missing → `{ form: {}, url: {} }`. Declare exactly that. Passes (`helpers.ts:158`; `validators.test.ts:816-827`).

`{ url: {} }` against a form round still asks for `{}`; `{ form: {}, url: {} }` against that mixed round returns `undefined`.

**4. CONFIRMED.** Attack: an unparsable `_meta` on a retry-shaped `tools/call` is answered by `#retry`'s pairing `-32602`, not the ingress. Failed: `#dispatch` always enters `#modern` first (`src/core/MCPServer.ts:254`, `223-254`); unparsable context is `-32602` `malformed modern request metadata` there (`387-396`) before any handler. The pin drives that retry shape through `dispatch` and records no second selector run (`tests/src/core/MCPServer.test.ts:4296-4324`). Attack: `#defer` / `#named` answer `-32021` when `context === undefined` (`846-852`, `1465-1471`). Failed as reachable: those run only after `#modern` already parsed the context. Attack: `MCPLegacy` forwards unparsable modern metadata. Failed: a modern request is handed to the dispatcher; a legacy `tools/call` is restamped with `capabilities: {}` (`src/core/helpers.ts:895-908`). `#gate` and `#retry` repeat the ingress words (`1029-1034`, `1104-1109`); that is the defensive reading behind the ingress.

**5. CONFIRMED.** Attack: `MCPClient.call` still places `requestState` when `input.state` is absent. Failed: the key is spread only when `input.state !== undefined` (`src/core/MCPClient.ts:428-432`; `tests/src/core/MCPClient.test.ts:1291-1311`). Attack: this server issues a round with no carrier, then `#retry` refuses the echo. Failed: `#required` always stamps `requestState` and refuses an empty seal (`src/core/MCPServer.ts:1313-1339`); `#retry` requires the pair together (`1093-1098`; `tests/src/core/MCPServer.test.ts:4331-4353`). Attack: a foreign stateless round cannot be answered through the public client. Failed: `MCPCallOptions.input.state` is optional (`src/core/types.ts:2515-2517`); `tests/conformanceClient.ts:183-185` calls `call` with `{ responses }` when the peer sealed no state.

**6. CONFIRMED.** Attack: a `round`-keyed `MCPInputOptions` reference remains in `src/`, `tests/`, or `guides/`. Failed: the option is `selector` (`src/core/types.ts:772`; `src/core/MCPServer.ts:992`, `1163`; `guides/mcp.md:1277`; `tests/setup.ts:405`). Attack: `{ round: … }` application state was renamed. Failed: those literals are still consumer state (`tests/src/core/MCPServer.test.ts:3993`, `4369`, `4387`, `4407`, `4631`). The fixture prompt name `'round'` is a resource/prompt name, not the option key.

**7. CONFIRMED.** Attack: a reconciliation row is absent from the diff and unnamed in departures. Failed. Gate-the-port-doors, sampling widening plus `isMCPRoot`/`isAbsoluteURI`, URL refusal payload, unified unparsable reading, stale-reference sites (anchors, suite name, `'selector provider detail'` / `['selector', 'principal']`, escaped `MCPInputResponse` pipes, consumer-keyed cells, `MCPElicitURL` no longer “not produced here”), conformance narrative, `selector` rename, F3 `limit.state` sentence, F4 URL-mode paragraph, hoisted `buildInputContext`, wording plus minimal `MCPLegacy` fixture — all in the commit. The mixed-round composition and the unified ingress wording are the named departures in `tmp/audit/mf2-report.md`. Attack: row 11’s “value-identical-through-canonical-serialization” phrase is a silent drop. Failed as a drop: the sealed-state sentence is now “the `requestState` the server issued, returned unchanged” (`guides/mcp.md:1380`), which is the opaque carrier the retry echoes.

**8. BROKEN.** The conformance baseline paragraph is false on the `server-stateless` cause. Guide (`guides/mcp.md:4012-4017`): those 2 failed are a probing call that declared no capabilities and was refused `-32021` over HTTP 400. Recorded table (`tests/conformance.test.ts:51-56`): those 2 failed are SEP-2575 `_meta` checks — omit `_meta`, or omit `protocolVersion` — answered `-32022` where the scenario expects `-32602`, and the undeclared-capability pair is green. Failing input: that guide sentence read against that table comment. Smallest fix: replace the `server-stateless` cause with the table’s SEP-2575 `-32022` vs `-32602` cause. The rest of the claim held: the `102` / `8` totals match `EXPECTED`; `http-custom-header-server-validation` `3` / `6` and its SEP-2243 cause match; URL-mode (`guides/mcp.md:1369-1377`) matches `helpers.ts:150-158`; round size spends `limit.state` default `16384` (`src/core/constants.ts:120`, `src/core/types.ts:2022`); `MCPInputRequest` / `isMCPInputRequest` are embedded-arm cells, not deprecated-carrier cells (`types.ts:590-609`, `guides/mcp.md:2119`, `2254`); the `MCPInputResponse` row renders three columns with escaped union pipes (`guides/mcp.md:2262`).

```text
VERDICT: FAIL — 1 broken, 0 unresolved, 0 not-evidenced, 0 findings outside the claims
```
