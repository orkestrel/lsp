1. **UNRESOLVED** — this lane could not invoke `node`/`npm`/`vitest` (the harness rejected those tools), and this worktree has no `dist/`. Settle by `npm run build:src:server` then `npm run test:src:bin -- -t "carries the verdict record beside the rendered text"` plus a probe that, in one `#execute`, deep-equals `structuredContent` to `result.value` and `content` to `[{ type: 'text', text: formatVerdict(result.value) }]`. `ProbeServer.ts:228-239` is written to do that; snapshotJSON still replaces the object with a canonical clone, which the claim's deep-equal wording admits.

2. **UNRESOLVED** — same missing live drive. Settle by the owned legacy raw-wire call and the pinned `createMCPLegacyClient` row, asserting the legacy `result.structuredContent` deep-equals the modern one. Installed `modernResultToLegacy` copies every key except `resultType`/`ttlMs`/`cacheScope` (`node_modules/@orkestrel/mcp/dist/src/core/index.js:1101-1121`), so a run that still dropped the field would refute it.

3. **UNRESOLVED** — no wire capture against `019c18a^`. Settle by sending the same `tools/call` refusal (escape path, malformed claim) and a `success: false` tool result on `bd4bb70` and `89f7bd7` and diffing the JSON-RPC. Source attack that did not finish the claim: P1's diff leaves `if (!result.success) return result` and the `#prove` `ProbeError` throws untouched; `@orkestrel/tool` still maps a throw to `{ success: false, error }` (`index.js:146-153`); MCP `#normalize` still answers that as `isError: true`, while a throw from `#execute` itself still becomes `-32603` `"Server error"`.

4. **CONFIRMED** — applied the installed `serializeJSON` enumerable-key algorithm (`index.js:697-750`) to the documented `Verdict` example in `src/core/types.ts:307-330` (reason and receipt present, three case checks, three control checks). Empty: 38 keys; each ranged issue adds 11; whole `MCPCallResult` adds 6 (`resultType`, `content`, `structuredContent`, array length 1, `type`, `text`). Empty whole 44 clears 64; two-issue whole 66 does not. Installed `MCPServer.#execute` (`index.js:4501-4507`, `4735-4741`) turns that miss into `-32603` `"Server execution returned an invalid tool result"`. A 30-issue record (the bin `WIDE` control) is 38+330=368, under `PROBE_KEYS` 4096. Seeding out `limit: { keys }` leaves fallback judging 4096 and the dispatcher judging 64, so a two-issue carried result still dies as `-32603`. Failed to refute: the documented cutoff. Separate observation, not a refutation: `buildRecord` in `ProbeServer.test.ts` is a one-check-per-phase slice (empty 21 keys), so `buildRecord(2)` is still under 64; that row uses 8 issues and does not pin the documented 0/2 cut.

5. **UNRESOLVED** — construction that would settle it, not run: control text `Array.from({ length: 400 }, (_, i) => \`export const VALUE_${i}: number = 'bad'\`).join('\\n')` through the shipped bin, or the same 400-issue documented record through `#execute`'s `isBoundedJSON` pair (`ProbeServer.ts:237-239`) then `createMCPServer({ limit: { keys: 4096 } })`. On paper 38+11×400 exceeds 4096, so `#execute` returns the text block alone (`formatVerdict` closing line `receipt …` / `no receipt`); that five-key result clears the published bound. A cheaper pin of the same branch: seed `PROBE_KEYS` to 64, rebuild, drive a two-issue claim, restore. This lane could not write a probe or mutate.

6. **CONFIRMED** — `git diff --name-only 019c18a 89f7bd7` is only the P2-owned files; the hunks replace `30_000` with `PROBE_DEADLINE`, `2_000` with `LINT_DEADLINE`, and `4096` with `PROBE_KEYS` at the same three sites (`Probe.ts:88`, `LintStage.ts:60`, `ProbeServer.ts:69`). `grep` of `src/` for those literals as runtime defaults now hits only `constants.ts`. Failed to find a moved branch, added call, or changed literal.

7. **UNRESOLVED** — the bound numbers and Constants rows held under the same key-count and file reads as claims 4 and 6 (`PROBE_DEADLINE` 30_000, `LINT_DEADLINE` 2_000, `PROBE_KEYS` 4096). `formatVerdict` (`src/core/helpers.ts:86-101`) emits `probe ${id}…` prose, not `JSON.stringify` of the record, so the departure sentence is true as a description of this package. The record-beside-text sentence is the live `tools/call` claim in (1) and was not driven. Settle with the same bin drive as (1).

8. **CONFIRMED** — `git diff --name-only 019c18a^ 89f7bd7` is exactly `guides/probe.md`, `src/core/constants.ts`, `src/server/Probe.ts`, `src/server/ProbeServer.ts`, `src/server/stages/LintStage.ts`, `tests/src/bin/main.test.ts`, `tests/src/server/ProbeServer.test.ts`. Grep of those implementation files for `any`, `as Type`, `@ts-`, `eslint-disable`, and nested `function` declarations in the P1/P2 hunks found none. New data lives in `constants.ts` or in `#limits` / test fixtures. Failed to find an off-lists path or a banned form in owned source.

9. **UNRESOLVED** — installed `buildCallOutcome` prefers `structuredContent` when the key is present (`index.js:936`). The owned client drives now assert `isVerdict(outcome.value)`; the guide's third-party-client paragraph says the client hands back the record (`guides/probe.md:529-532`). Settle by running those two client rows after `build:src:server`. A remaining consumer sentence that still said the client receives rendered text would refute it; grep of `guides/probe.md` did not find one.

10. **BROKEN** — attacked five writer verdicts. Unsound: “the text-only fallback needs no test” (`p1-report.md` “Claims I could not close”). Failing state: they already compose `createMCPServer`+`createMCPLegacy` on a synthetic record (`ProbeServer.test.ts:49-78`); wrapping that record with the four lines at `ProbeServer.ts:228-239` and a 400-issue (or `keys: 64`) input is the same cost class as `buildRecord(8)`, and a real control of 400 `export const VALUE_n: number = 'bad'` lines is a cheap claim. Smallest fix: add that row (text block last line is the receipt line; `structuredContent` absent; no `-32603`) and retract the “needs no test” verdict. Held: no off-limits file was touched (claim 8’s name-only list). Also unsound as stated: the bound-test comment that the default “stops carrying the next” declaration — on their thin `buildRecord` fixture two issues stay under 64 keys. Held at source: P2 “no behavioral edit”. Held on the documented example: 38/11.

**Findings outside the claims**

- `tests/src/server/ProbeServer.test.ts` `readCall` never constructs `ProbeServer` and never runs `#execute`. It pins `@orkestrel/mcp`’s default 64-key leaf on a foreign server, not this package’s `limit: { keys: PROBE_KEYS }` wiring or the text-only fallback. Failing input: that test file as it stands. Smallest fix: drive the 0/2/wide/fallback cases through `ProbeServer`’s dispatcher (or the shipped bin) rather than a hand-built `createMCPServer`.

```text
VERDICT: FAIL — 1 broken, 6 unresolved, 0 not-evidenced, 1 findings outside the claims
```
