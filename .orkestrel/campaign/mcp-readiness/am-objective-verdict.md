1. **CONFIRMED.** Failed attack: reach `#required` without `#gate` measuring that round. `#input` (`src/core/MCPServer.ts:1018`) and `#retry` (`:1168`) both call `#gate` on the `#round` snapshot before seal; `#required` is the only issuer. Failed attack: a completing retry still needs the answered round’s capabilities. `#retry` returns `undefined` when the selector does (`:1159`), `#call` then runs the tool (`:791–792`); the recorded row `gates the next round rather than the answered one on a retry` (`tests/src/core/MCPServer.test.ts:4387–4408`) drives that door with `_meta` capabilities `{}`. Failed attack: an unparsed first-round context skips the gate — `#gate` measures `capabilities ?? {}` (`:1037`) and refuses every admitted kind. No other issuer exists.

2. **BROKEN.** Wrong-kind refusal holds: `isMCPInputResponse` dispatches on `request.method` (`src/core/validators.ts:1966–1973`); a roots listing under a sampling key, a form accept under sampling, and a sampling completion under roots are refused (`tests/src/core/validators.test.ts:1029–1032`). Form accept is checked against that key’s issued schema (`:1972–1973`). The dated sampling shape does not. In-tree `CreateMessageResult.content` (`tests/mirrors/ext-tasks-2026-07-28-schema.json:1858–1881`) is Text, Image, Audio, ToolUse, ToolResult, or an array of `SamplingMessageContentBlock`. `isMCPSampleResult` admits only a single `text` | `image` | `audio` block (`src/core/validators.ts:1933–1936`). Failing input, sampling key `capital_question`: `{ role: 'assistant', model: 'test-model', content: [{ type: 'text', text: 'Paris' }] }` — schema-valid `CreateMessageResult`, guard false, `#answers` refuses the retry (`MCPServer.ts:1123–1128`). Same refusal for a `tool_use` / `tool_result` block. Roots envelope matches `ListRootsResult`; `Root.uri` format is the looseness the writer named, not this break. Smallest fix: widen `MCPSampleResult` / `isMCPSampleResult` to that `anyOf`, or document the subset on the type and stop calling it the dated shape.

3. **CONFIRMED.** Failed attack: a pre-reshape payload with `key` + `schema` and no `requests` still parses. `parseMCPInputState` requires a nonempty `isMCPInputRequestMap` (`src/core/parsers.ts:185`); that payload returns `undefined`. Principal, expiry, id, version, method, name, and digest remain required (`:174–184`) and are still compared on retry (`MCPServer.ts:1105–1111`). Empty `requests: {}` is refused at parse (`parsers.ts:185`) and at `#round` (`MCPServer.ts:1208`). `#answers` iterates `state.requests` (`:1123, :1186–1188`). Extra `key` / `schema` members on a valid `requests` payload are unused leftovers, not a binding the retry still consults.

4. **CONFIRMED.** Failed attack: omit `state` on the round and still see it on retry. `#required` spreads `state` only when defined (`MCPServer.ts:1264`); `#retry` forwards it only when defined (`:1145`). Failed attack: the server reads consumer state for policy — the only check is `isJSONValue` at own-time (`:1209`). The member rides inside the serialized protected payload; an identity continuation returns those canonical bytes unchanged. Recorded row `returns the consumer's keyed round…` pins a defined string through seal/open (`MCPServer.test.ts:1930`).

5. **BROKEN.** `MCPElicitation` and `elicit:` are gone from `src/` and `tests/`. Stale references remain in `guides/`. Failing input: the heading is `Ask the client for input during the call in hand` (`guides/mcp.md:1216`) while these links still target `#produce-a-form-elicitation-for-the-call-in-hand`: `guides/mcp.md:532`, `:796`, `:3178`. Surface / Types also disagree with the shipped shapes: `isMCPInputRequest` and `MCPInputRequest` still call sampling and roots deprecated (`:2072`, `:2206`) after this server issues them; `MCPInputRequestMap` / `MCPInputResponseMap` stay “server-keyed” (`:2073`, `:2207`, `:2212`) while `MCPInputRound` is consumer-keyed (`:2216`); `MCPElicitURL` still says “not produced here” (`:2202`) while a consumer round with `mode: 'url'` is a legal issued request. `test:guides` 144 passed does not check those anchors (writer). Smallest fix: retarget those links to `#ask-the-client-for-input-during-the-call-in-hand`; rewrite those Surface / Types cells to the shipped shapes.

6. **CONFIRMED.** Failed attack: the MCPLegacy assertion changed or now proves a different refusal. `refuses input-required at the legacy revision boundary` still expects `Legacy protocol 2025-11-25 cannot represent an input-required result` (`tests/src/core/MCPLegacy.test.ts:368–411`). Path: `legacyInvocationToModern` stamps `{}` capabilities; `#gate` answers `-32021` with `{ elicitation: {} }`; `MCPLegacy.#capability` sees no tasks extension and returns `'input-required'` (`src/core/MCPLegacy.ts:192–200`). Failed attack: a parsers rejection row mutates more than one binding — `protectedPayload` applies one override (`tests/src/core/parsers.test.ts:28–47`); `JSON.stringify` drops `undefined` for a missing member. Failed attack: `requests: {}` is the same as missing `requests` — it is a present empty map, a binding the old `key` + `schema` pair could not express.

7. **CONFIRMED.** Failed attack: omitting `mode` is invalid on the wire, so dropping the stamp ships an illegal `elicitation/create`. Dated `ElicitRequestFormParams.required` is `message` and `requestedSchema` only (`tests/mirrors/ext-tasks-2026-07-28-schema.json:1961–1964`); `mode` is optional on the form arm. `isMCPElicitForm` admits a missing `mode` (`src/core/validators.ts:1576–1578`); `isMCPElicitRequest` therefore admits it (`:1626`). `#required` ships `round.requests` verbatim (`MCPServer.ts:1311–1314`); the old `{ ...form.request, mode: 'form' }` stamp is gone. URL params still require `mode` (`schema.json:1996–1999`; `isMCPElicitURL` at `validators.ts:1600`). `CONFORMANCE_ROUNDS` for the elicitation tools omit `mode` (`tests/setupConformance.ts:936–945`); writer-recorded `input-required-result-basic-elicitation` is 2/0.

8. **CONFIRMED.** Failed attack: a kind maps to the wrong capability key, or the `-32021` payload names a kind that is present. `computeMissingCapabilities` maps `sampling/createMessage` → `sampling`, `roots/list` → `roots`, and every remaining admitted request (form or URL elicitation) → `elicitation` (`src/core/helpers.ts:133–147`). A mixed round against `{}` yields `{ elicitation: {}, sampling: {}, roots: {} }`; a client short of one kind names only that key (`tests/src/core/validators.test.ts:784–798`). URL-only vs form still keys the payload `elicitation` (`:800–808`). `#gate` puts that record on `data.requiredCapabilities` (`MCPServer.ts:1039–1043`).

9. **CONFIRMED.** Failed attack: the commit touches a file outside the owned set plus the named patches. `minput.diff` files: `guides/mcp.md`, `src/core/MCPServer.ts`, `src/core/helpers.ts`, `src/core/parsers.ts`, `src/core/types.ts`, `src/core/validators.ts`, `tests/conformance.test.ts`, `tests/setup.ts`, `tests/setupConformance.test.ts`, `tests/setupConformance.ts`, `tests/src/core/MCPLegacy.test.ts`, `tests/src/core/MCPServer.test.ts`, `tests/src/core/parsers.test.ts`, `tests/src/core/validators.test.ts`. Failed attack: a banned construct in those hunks — no `any`, no `as` assertion, no suppression, no nested function declaration in the new `#gate` / `#answers` / `#round` / `computeMissingCapabilities` / `isMCPInputResponse` bodies. `MCPServer.#register` and the `handle` ingress were not given a second route.

10. **CONFIRMED.** Attacked the two named verdicts and the reshape-avoidance claim. Failed attack: `MCPMethodManagerInterface` moved — no hunk on `src/core/types.ts:1818–1833`; `add` / `method` unchanged. Failed attack: the dispatch spine moved — `#register` still binds `tools/call` to `#call` (`MCPServer.ts:318`); `#call` still runs `#input` then execute (`:791–813`); comments in `handle` only. Failed attack: keep the option key `elicit` and the three test files stay green — `setup.ts` and `MCPLegacy.test.ts` still break on `request` / `response`; `parsers.test.ts` still breaks on `key` / `schema`. Failed attack: keep `key` + `schema` in sealed state beside `requests` — that cannot represent a mixed round, so it is not this widening.

VERDICT: FAIL — 2 broken, 0 unresolved, 0 not-evidenced, 0 findings outside the claims
