# M5 closure — the deprecated-surface row, 2026-08-26

M5 closes with no code change, at mcp `11c879c`.

- **Producers and registrations: retained as already absent**, proved by the Grok
  enumeration sweep (`m5-sweep-distillate.md`): `buildDiscoverResult` stamps no `roots`,
  `sampling`, or `logging` capability, and the server's `methods.add` inventory carries
  none of the families' methods.
- **Receipt parsing: retained as already correct** — `isMCPInputRequest` admits the
  `sampling/createMessage` and `roots/list` arms the vendored 2026-07-28 schema defines,
  and logging is inbound metadata validation only.
- **The `@deprecated` markers: intentionally excluded by the user's ruling** — no
  deprecation of our surfaces exists in this greenfield fleet; the vendor protocol's era
  facts stay documented as prose, which the tree already does. The m5-deprecated unit's
  uncommitted tag edits were discarded on that instruction; its brief and report are
  retained beside this file as the record of the superseded attempt.
- A fleet-wide word-boundary sweep for `@deprecated` over every repository's `src/` and
  `app/` trees reads zero hits after the discard, 2026-08-26.
