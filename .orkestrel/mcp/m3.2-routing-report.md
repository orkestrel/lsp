1. What changed

- [MCPClient.ts](/home/user/mcp/src/core/MCPClient.ts): snapshots caller options at entry, preserves lazy subscription validation, and claims active progress before subscription routing.
- [MCPClient.test.ts](/home/user/mcp/tests/src/core/MCPClient.test.ts): adds accessor-abort, claimed-progress, and surviving-discard regression rows. Existing pre-aborted and unstamped-progress controls remain.
- [mcp.md](/home/user/mcp/guides/mcp.md): clarifies progress-before-stale-discard ordering in the `listen` Methods row.
- [guides.test.ts](/home/user/mcp/tests/guides.test.ts): executes that guide claim through the loopback seam.
- `tmp/probe/m3-verify.test.ts`: deleted last. Absence check exited 0.

2. Red-first records

- Accessor snapshot:
  - Command: `npm run test:src:core -- tests/src/core/MCPClient.test.ts -t 'snapshots an accessor signal before a later read aborts it'`
  - Red: exit 1; 1 failed. The first read fulfilled with the acknowledgement instead of rejecting.
  - Green: exit 0; 1 passed.

- Claimed progress:
  - Command: `npm run test:src:core -- tests/src/core/MCPClient.test.ts -t 'claims stale-stamped progress for the active call before subscription routing'`
  - Red: exit 1; 1 failed. The handler received `[]`.
  - Green: exit 0; 1 passed.

- Surviving discard pin:
  - Command: `npm run test:src:core -- tests/src/core/MCPClient.test.ts -t 'discards stale-stamped frames that no active call or subscription claims'`
  - Before and after: exit 0; 1 passed.

- Controls:
  - Plain pre-aborted signal: exit 0.
  - Unstamped active progress: exit 0.

3. Snapshot-once derived set

Search scope: every textual `options.member` and `options?.member` access in `src/core/MCPClient.ts`.

- Constructor: `on` and `error` were repeated; each is snapshotted. Other constructor members were already single-read.
- `listen`: `signal` was repeated; `signal` and `capacity` are captured when `listen` is called. The generator uses those snapshots for validation, abort binding, pending registration, and cleanup.
- `call`: `input` was repeated; it is snapshotted before reading `state` and `responses`.
- `#request`: `signal` and `progress` were already read once. Retained.
- Task requests reach `#request` without `MCPCallOptions`; no repeated caller-options read exists on that path.
- The later `options.description` and `options.parameters` matches are writes to an internal tool-options object, not caller-option reads.

The post-change search reports each caller-options member once per entry point.

4. Guide sentence ruling

The subscription section remains true without change:

> “Every frame the server stamps with this subscription's id arrives as an owned snapshot…”

The Methods row now states:

> “active progress is claimed before stale frames are dropped.”

Its transcription, `claims active progress before dropping other stale-stamped frames`, exited 0.

5. Scoped gate readings

- `npm run test:src:core -- tests/src/core/MCPClient.test.ts -t 'subscriptions/listen'`: exit 0; 15 passed.
- Unfiltered client file: exit 1; 138 passed, with the known sandbox `listen EPERM` affecting the real HTTP rows.
- Unfiltered guides project: exit 1; 138 passed, with spawned-child stdio rows timing out in this bench.
- Targeted guide transcription: exit 0.
- Scoped `oxfmt --check` over all owned tracked files: exit 0.
- Scoped `oxlint --deny-warnings` over all owned tracked files: exit 0.
- `npm run check`: exit 0.
- `git diff --check`: exit 0.
- Diff scope contains only `guides/mcp.md`, `src/core/MCPClient.ts`, `tests/guides.test.ts`, and `tests/src/core/MCPClient.test.ts`.
- Probe absence check: exit 0.

6. Observations outside scope

- MCP HTTP fixture hosting: the sandbox rejects `listen(127.0.0.1)` with `EPERM`, causing the unfiltered client run’s HTTP timeouts.
- Stdio child-process transport: the bench’s child stdio limitation caused the unfiltered guide run’s spawned-child timeouts; the added guide transcription passed.
- Probe mutation instrument: approval policy `never` prevents the `prove` instrument here. The Orchestrator retains the host-side mutation checks.