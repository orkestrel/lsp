# Unit M3.2 — snapshot the caller's signal and claim progress before the stamp discard

Role and engine: Sol `implementer`, GPT-5.6 Sol, reached through `codex exec`, sandbox
`workspace-write`, working directory `/home/user/mcp`. You perform this assignment
directly and spawn nothing beyond the shell commands your work needs. Baseline: mcp
commit `f0ad416`, tree clean apart from the Orchestrator's probe file at
`tmp/probe/m3-verify.test.ts` — read it, adopt its rows, and delete it as the last step.

Findings carried, the M3 analyst verdict's broken claims
(`/home/user/lsp/.orkestrel/mcp/m3-analyst-audit-verdict.md`), each verified by the
Orchestrator's probes of 2026-08-26 (`npm run test:probe -- --run
tmp/probe/m3-verify.test.ts`, 4 passed — the defect rows and their discriminating
controls):

1. **The accessor signal.** `listen` reads `options.signal` as a property at
   `src/core/MCPClient.ts:406`, `:442`, `:443`, and `:446`. A conforming accessor that
   aborts between reads passes `throwIfAborted` on the first read, attaches the abort
   listener AFTER the abort (an already-aborted signal never fires it), and the
   subscription still opens — measured: 4 reads, `aborted: true`, the acknowledgement
   delivered, `subscriptions/listen` on the wire. A consumer that trusts its own abort
   drops the stream and leaks the pending entry and listener. The foreign-contract law
   is explicit: read the foreign object exactly once; no result may depend on read
   count.
2. **The swallowed progress frame.** `#routeSubscription` runs before `#reportProgress`
   (`src/core/MCPClient.ts:702-703`) and claims-and-discards ANY stamped notification
   whose id names no active subscription — including a `notifications/progress` frame
   carrying a valid `progressToken` for an active call. Measured through the published
   loopback seam: active call id `2`, injected progress with the stale stamp → the
   progress handler received nothing and the `notification` event carried nothing; the
   unstamped control reached the handler. `src/core/types.ts:2316` promises each
   progress frame for the request to its handler. The comment at `:700-701` already
   states the correct order — "A progress frame a caller is waiting on is claimed by
   that caller" — the code has it backwards.

## The work — the analyst's prescriptions, adopted verbatim

1. In `listen`, read `options.signal` ONCE into a local, call `throwIfAborted()` on the
   snapshot, and use the snapshot for the abort binding, the pending registration, and
   every cleanup path. Then re-ask the repaired claim at every entry point that reaches
   the same rule: derive with a search every `options.signal` (and any other
   caller-options member) property read in `src/core/MCPClient.ts` that occurs more
   than once per invocation — `call` and the task paths included — and snapshot-once
   each such entry point. Report the derived set with the search's scope.
2. Reorder the dispatch seam so `#reportProgress` runs before `#routeSubscription`:
   valid active progress is claimed first, and an UNCLAIMED stale-stamped frame —
   progress with no active token, or any other stamped method — stays subject to the
   ruled late-subscription discard.
3. Promote the Orchestrator's probe rows into `tests/src/core/MCPClient.test.ts`, named
   for what each proves, red-first against the current tree:
   - the accessor-abort row: an options object whose `signal` accessor aborts on a
     later read → the first read REJECTS unsent once the snapshot lands (red today: it
     resolves the acknowledgement);
   - the claimed-progress row: a stale-stamped progress frame with a valid active token
     reaches the call's progress handler and never surfaces on `notification` (red
     today: swallowed);
   - the surviving-discard row: a stale-stamped non-progress notification, and a
     stale-stamped progress frame with NO active token, are each still discarded
     silently (green today and after — the pin that the reorder narrows nothing else).
   Keep the plain pre-aborted control and the unstamped progress control where
   equivalent rows exist; add them where they do not.
4. Re-read the guide's subscription section discard sentences
   (`guides/mcp.md` § Consume a subscription from a client, and the Methods row's
   discard clause) against the reordered code: where a sentence now understates that a
   progress frame a caller waits on is claimed before the discard, correct exactly that
   sentence and its transcription row; touch nothing else in the guide.
5. Delete `tmp/probe/m3-verify.test.ts` after the promotions land.

## Host environment and bench limits

Linux container, Node and npm on PATH, network DENIED in your sandbox — no installs.
Dependencies are installed. Nested `git` invocations from a spawned tool can report
"not a git repository" while your own `git status` succeeds; that is the sandbox. The
`probe` MCP instrument refuses in this sandbox (approval policy `never`) — record any
claim needing it as a host observation. Never make a whole-suite gate a criterion for
yourself; the Orchestrator runs the authoritative chain after you exit.

## Scope

Owned files: `src/core/MCPClient.ts` (the `listen` signal snapshot, the derived
snapshot-once entry points, the `:702-703` reorder), `tests/src/core/MCPClient.test.ts`
(the promoted and added rows), `guides/mcp.md` (only a discard sentence the reorder
makes false), `tests/guides.test.ts` (only that sentence's transcription),
`tmp/probe/m3-verify.test.ts` (deleted last).

Report-only: `src/core/types.ts`, `src/core/helpers.ts`, `tests/setup.ts`,
`tests/guides.test.ts` beyond the named row.

Off-limits: everything else.

Allowed tools: read, edit, and scoped shell commands in `/home/user/mcp`. No commit, no
push, no `git checkout`/`restore`/`stash`/`reset`/`clean`, no tree-wide `format` or
`lint --fix`, no installs.

## Execution

You are the bench engine reading this brief inside your own CLI: do the work yourself,
directly, and spawn nothing beyond the shell commands your work needs.

## Output

Your final message is the unit report, in this shape and nothing else:

1. What changed: each owned file with the exact behavioral delta.
2. The red-first records: each promoted row's exact red and green commands with
   readings, the surviving-discard pin's stable green included.
3. The snapshot-once derived set with the search scope, and each entry point's ruling.
4. The guide sentence ruling: corrected with its transcription, or read as still true
   with the reading quoted.
5. Scoped gate readings with exit codes: the client test file's project run
   (`-t 'subscriptions/listen'` scoped and the file unfiltered), the guides project
   unfiltered, scoped `oxfmt --check` and `oxlint --deny-warnings` over the owned
   files, `npm run check` tree-wide, `git diff --check`.
6. Observations outside scope, each named against the capability that owns it.

No process diary.

## Deviation contract

A conflict with the primary objective stops the unit: report expected, found, exact
evidence, done or not done, and at most one short hypothesis. The named stop
conditions: the reorder reddens any existing row beyond the promoted set; the snapshot
change alters any pinned behavior other than the accessor case; a discard row cannot be
pinned without touching `tests/setup.ts`. Ancillary conflicts — row titles, the local's
name, the guide sentence's wording — are yours to decide, record, and carry on from.

## Acceptance criteria

Ordered cheap-first.

1. `git diff --check` exits 0; the diff touches only owned files; the probe file is
   gone.
2. Scoped `oxfmt --check` and `oxlint --deny-warnings` over the owned files exit 0.
3. The accessor-abort and claimed-progress rows are green red-first; the
   surviving-discard rows are green throughout; the controls stand.
4. The client file's subscription rows and the unfiltered file exit 0 (the known
   sandbox HTTP `listen EPERM` rows excepted, where they appear in your sandbox); the
   guides project exits 0 unfiltered.
5. `npm run check` exits 0 tree-wide; no banned construct in the diff.

## Review evidence

The Orchestrator captures the actual diff and the actual `git status` output after you
exit, runs the mutation probes that close a verbatim adoption — disabling the snapshot
and the reorder each in turn and watching the adopted pins fail — and re-reads the diff
against the prescriptions. The pins commit as the regression guards.
