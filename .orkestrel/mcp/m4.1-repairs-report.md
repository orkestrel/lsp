# Unit report: m4.1-repairs

Every finding the brief carries is closed and the scoped gates read green. No deviation.
Written 2026-08-26 by `implementer` (Opus 5) in `/home/user/mcp`, from the clean baseline
`c2a35d4` on `claude/lsp-spec-audit-est33d`. Owned files only: `tests/setupConformance.ts`,
`tests/conformance.test.ts`, `src/core/helpers.ts`, `guides/mcp.md`.

## Deliverable: reproject the metadata row

`readTaskNotificationMetadata` (`tests/setupConformance.ts:357-365`) and the
`TaskStatusNotificationParams metadata` row (`:552-568` before, `:558-566` after).

Before, the projection took `properties` wholesale and the row's expected side carried the
escaped upstream prose:

```ts
			properties: readSchemaPath(TASK_SCHEMA, ['$defs', 'NotificationMetaObject', 'properties']),
```

```ts
			metadata: {
				type: 'object',
				properties: {
					'io.modelcontextprotocol/subscriptionId': {
						$ref: '#/$defs/RequestId',
						description:
							'Identifies the subscription stream a notification was delivered on. The\nserver MUST include this key ... opened the stream.',
					},
				},
			},
```

After, the projection reads the property's reference by path and the row expects that
reference alone:

```ts
			subscription: readSchemaPath(TASK_SCHEMA, [
				'$defs',
				'NotificationMetaObject',
				'properties',
				'io.modelcontextprotocol/subscriptionId',
				'$ref',
			]),
```

```ts
			metadata: { type: 'object', subscription: '#/$defs/RequestId' },
```

The definition-level `type` read is unchanged. The row's `symbol` is unchanged; the member
name moved from `properties` to `subscription`, which the reprojection requires because the
member no longer carries a properties map, and which matches the package's own term for that
reserved key (`MCP_META_SUBSCRIPTION`).

The executed reading is the row itself: `readConformanceDrift` returns `undefined` only when
`Object.is(expected, model)` holds (`tests/setupConformance.ts:160`), and the expected side
serializes to `{"params":{"$ref":"#/$defs/NotificationMetaObject"},"metadata":{"type":"object","subscription":"#/$defs/RequestId"}}`.
The conformance project reports that row passing, so the reprojected read returned
`'#/$defs/RequestId'`. The deviation trigger for any other value did not fire.

## Deliverable: rename the boolean parameter

`src/core/helpers.ts:880-887`, `:899`.

Before:

```ts
 * @param tasks - If `true`, includes requested task identifiers; if `false`, omits them
```

```ts
	tasks = false,
```

```ts
	const taskIds = tasks ? requested.taskIds : undefined
```

After:

```ts
 * @param enabled - If `true`, carries the requested task identifiers into the filter; if `false`,
 *   omits them. Default: `false`
```

```ts
	enabled = false,
```

```ts
	const taskIds = enabled ? requested.taskIds : undefined
```

### Call-site search population

Command: `grep -rn "buildSubscriptionFilter" src/ tests/ guides/ app/`. Scope: the `src/`,
`tests/`, `guides/`, and `app/` trees; `app/` matched nothing. The hits are the declaration
(`src/core/helpers.ts:883`), one import and one call in `src/core/MCPServer.ts` (`:69`,
`:1335`), one import and the calls in `tests/src/core/helpers.test.ts` (`:25`, `:533`, `:594`,
`:595`, `:603`, `:1685`), and the guide's imports, fences, and Surface row
(`guides/mcp.md:840`, `:853`, `:913`, `:919`, `:927`, `:928`, `:2085`). Every call passes the
argument positionally, so the rename forced no edit outside the owned set. The local variable
named `tasks` at `src/core/MCPServer.ts:1335` is that file's own binding and is untouched.

## Deliverable: retitle the digest row

`tests/conformance.test.ts:58`.

Before: `it('pins the schema raw-byte digest before parsing', () => {`

After: `it("pins the vendored schema's raw-byte digest", () => {`

The formatter keeps the double quotes because the title carries an apostrophe;
`oxfmt --check` accepts the line as written.

## Deliverable: restate the conformance project

`guides/mcp.md` § Declared conformance gaps, `:3917-3930` before and `:3917-3943` after.

Before, the section defined the project as the foreign-runner run alone: "A reproducible run
is `npm run test:conformance`: it starts the real Streamable HTTP server … It is a project of
its own — `tests/conformance.test.ts` over the fixture in `tests/setupConformance.ts`".

After, an opening paragraph names both halves — "The conformance project is a foreign-runner
exchange and a schema-authority comparison, and each measures something the other cannot" —
the runner paragraph is unchanged apart from dropping `current` from "the current recorded
result", a new paragraph describes the comparison half, and the closing paragraph opens "They
share one project" instead of "It is a project of its own".

The added paragraph names the mirror path and the digest constant as the brief requires:

> The comparison starts no server and drives no client. It reads the stable Tasks
> extension's published schema from the vendored mirror
> `tests/mirrors/ext-tasks-2026-07-28-schema.json`, and the `TASK_SCHEMA_DIGEST` constant in
> `tests/setupConformance.ts` pins that file's raw bytes: a changed byte throws at module
> load, before a comparison row runs, so no row reads a mirror this package has not pinned.
> Each comparison row projects one schema coordinate — a task variant, a result composition,
> a notification shape, a subscription fragment — and compares it with the shape this
> package ships, so a contract that drifts from the authority reddens the row naming the
> coordinate.

The recorded foreign-runner reading stays as stated: **23 passed / 0 failed**, the
`dns-rebinding-protection` guard included, with no remaining failing scenario. The load-time
claim rests on `TASK_SCHEMA = readTaskSchema(TASK_SCHEMA_PATH)` at
`tests/setupConformance.ts:195` and the throw at `:186-188`, and matches the R1 probe reading
the brief supplied.

Ancillary decisions recorded: `current` is dropped from the runner paragraph because the
restatement rewrites the sentence and `.claude/rules/writing.md` § Substitutions bans the word;
"Each comparison row" replaced an initial "Each row" so the sentence excludes the digest pin,
which projects no coordinate.

## Deliverable: close the verified prose hits

| Site | Before | After |
| --- | --- | --- |
| `guides/mcp.md:925` | `// The third argument is the derived support fact.` | `` // The `enabled` parameter is the derived support fact. `` |
| `guides/mcp.md:2085` | ``a `true` third argument carries the requested task identifiers`` | ``` `enabled` set to `true` carries the requested task identifiers ``` |
| `guides/mcp.md:913` | `The helpers behind that path are the same two the other families use, with the tasks argument supplied:` | ``The helpers behind that path are `buildSubscriptionFilter` and `matchesSubscriptionNotification`, the ones the other families use, with `enabled` supplied:`` |
| `guides/mcp.md:885` | `no third flag records it` | `no stored flag records it` |

The `:913` rewrite names the helpers and drops the count, which the reconciliation offered as
either alternative; it names both because the fence imports a third helper
(`isMCPTaskNotification`) that the other families do not use, so "the ones the other families
use" alone would have been ambiguous about which. The Surface-row cell keeps its exact
character width, so the table's column alignment is unchanged.

## Deliverable: delete the carried `simply`

`guides/mcp.md:325`: "It is simply the next registration" became "It is the next registration".

A second `simply` sits at `:1335` — "A carrier the port simply cannot recover" — which the
finding list does not name but the brief's acceptance criterion reaches, because
`grep -c "simply" guides/mcp.md` must report 0. The file is owned and the substitution table
bans the word outright, so the word is deleted there too: "A carrier the port cannot recover".
Recorded here as an ancillary decision rather than a deviation.

## Gate readings

Every reading below post-dates every edit. Commands were run bare in `/home/user/mcp`.

| Gate | Reading | Exit |
| --- | --- | --- |
| `npx oxfmt --config .oxfmtrc.json --check tests/setupConformance.ts tests/conformance.test.ts src/core/helpers.ts guides/mcp.md` | `All matched files use the correct format.` | 0 |
| `npx --no-install oxlint --config .oxlintrc.json --deny-warnings tests/setupConformance.ts tests/conformance.test.ts src/core/helpers.ts` | no output | 0 |
| `npm run check` | `tsc --noEmit` over the root project plus `check:src:core`, `check:src:browser`, `check:src:server`, no diagnostic | 0 |
| `npx vitest run --config vite.config.ts --no-cache --reporter=dot --project conformance` | `Test Files 1 passed (1)`, `Tests 42 passed (42)` | 0 |
| `npm run test:guides` | `Test Files 1 passed (1)`, `Tests 144 passed (144)` | 0 |
| `npx vitest run --config vite.config.ts --no-cache --reporter=dot --project src:core tests/src/core/helpers.test.ts tests/src/core/MCPServer.test.ts tests/src/core/MCPClient.test.ts tests/src/core/validators.test.ts` | `Test Files 4 passed (4)`, `Tests 596 passed (596)` | 0 |

Each count matches the baseline the brief states. `npm run check` was read through
`tail -20` with `${PIPESTATUS[0]}` carrying the gate's own status; every other gate was read
bare.

### Mechanical acceptance searches

- `grep -c "third argument" guides/mcp.md` → `0`.
- `grep -c "simply" guides/mcp.md` → `0`.
- `grep -rn '{@link SubscriptionsListenRequest' tests/ --exclude-dir=mirrors` → no hit, exit 1.

## Writing-rules sweep

Population: every added line in `git diff -U0` over `guides/mcp.md`, `src/core/helpers.ts`,
and `tests/conformance.test.ts` — 30 added lines. Nothing in `tests/setupConformance.ts` is
prose, so its added lines are outside the sweep.

Patterns, run case-insensitively with `grep -Ein` over that population:

- Substitution table:
  `should|simpl|easy|easier|just|currently|\bnow\b|\bnew\b|latest|utiliz|leverag|\bvia\b|in order to|e\.g\.|i\.e\.|etc\.|performant|robust|allows you to|and/or|\bsince\b|\bonce\b|please|sanity check|dummy|blacklist|whitelist|master|slave|ensure|guarantee`
  → no hit.
- Reference and voice: `\babove\b|\bbelow\b|\bwe\b|\bour\b|let's|\bhere\b` → no hit.
- Counts and ordinals: `\bfirst\b|\bsecond\b|\bthird\b|\bboth\b|\btwo\b|\bthree\b|\bfour\b` →
  one hit, `guides/mcp.md:885`, "the two options at the moment the listen request is
  answered". Ruled permitted, not dropped: the reconciliation
  (`m4-round-audit-verdict.md`, § Source confirmation) rules that this sentence names its
  members (`task` and `subscription`, at `:882`) and that the pair cannot grow, so the count
  rule admits it; the line enters the population only because the same line carries the
  "no stored flag" repair.

## Tree state

`git status --short`:

```text
 M guides/mcp.md
 M src/core/helpers.ts
 M tests/conformance.test.ts
 M tests/setupConformance.ts
```

`git diff --stat`:

```text
 guides/mcp.md             | 37 +++++++++++++++++++++++++------------
 src/core/helpers.ts       |  7 ++++---
 tests/conformance.test.ts |  2 +-
 tests/setupConformance.ts | 19 ++++++++-----------
 4 files changed, 38 insertions(+), 27 deletions(-)
```

The vendored `tests/mirrors/ext-tasks-2026-07-28-schema.json` file is untouched, and no file
outside the owned set changed.

## Deviation state

None. No site's text differed from its finding's quote, the reprojected row read
`'#/$defs/RequestId'` when executed, the rename forced no edit outside the owned set, and no
gate reddened.
