# Unit brief: m4.1-repairs — the M4 round audit's fix unit

## Role and engine

`implementer` — Opus 5, native subagent. You write in `/home/user/mcp`, the sole writer
in that checkout, from the clean committed baseline `c2a35d4` on branch
`claude/lsp-spec-audit-est33d`. You perform the assignment directly yourself and spawn
nothing.

## Objective

Close every finding the M4 round audit reconciled into this unit: the metadata row's
reprojection, the boolean parameter rename with its documented default, the digest row's
title, the guide's conformance-project restatement, the verified prose hits, and the
carried `simply`. The scoped gates read green.

## Context

- Read before editing: `/home/user/mcp/AGENTS.md`; `/home/user/mcp/.claude/rules/` —
  `names.md`, `typescript.md`, `writing.md`, `tests.md`, `documentation.md`; no skill
  (explicit none).
- The findings and their evidence are in
  `/home/user/lsp/.orkestrel/mcp/m4-round-audit-verdict.md` (the reconciliation — your
  finding list), `m4-audit-reviewer-verdict.md` (the full reviewer reasoning), and
  `m4-audit-objective-verdict.md`. Read the reconciliation first; where it and a lane
  verdict differ, the reconciliation wins.
- The R1 probe reading, already taken by the Orchestrator: a drifted mirror fails
  collection with the named digest error before any row runs, so the digest row is the
  readable pin and its title must state what it proves, nothing more.
- Verify each site's current text before editing; a mismatch with a finding's quote is a
  deviation.

## Scope

- Owned: `tests/setupConformance.ts`, `tests/conformance.test.ts`,
  `src/core/helpers.ts`, `guides/mcp.md`.
- Off-limits: everything else, the vendored `tests/mirrors/` file above all. A rename
  consequence outside the owned set is a deviation, never an edit.

## Deliverables

1. **Reproject the metadata row.** In `readTaskNotificationMetadata`
   (`tests/setupConformance.ts:436-444`), replace the wholesale
   `['$defs','NotificationMetaObject','properties']` read with the path read
   `['$defs','NotificationMetaObject','properties','io.modelcontextprotocol/subscriptionId','$ref']`,
   and set the row's expected side to `'#/$defs/RequestId'` — the escaped upstream prose
   at `:561-562` leaves the source entirely. Keep the definition-level `type` read.
   Adjust the row's `symbol` or helper naming only as far as the reprojection requires.
2. **Rename the boolean parameter.** In `src/core/helpers.ts`, `buildSubscriptionFilter`
   renames its third parameter `tasks` → `enabled` (declaration, internal reads, and the
   `@param` line), rewords the `@param` around the adjective form, and appends
   `Default: \`false\``. Call sites are positional, so no file outside your set changes;
   confirm with a search and report the population.
3. **Retitle the digest row.** `tests/conformance.test.ts:58`: the title becomes
   `pins the vendored schema's raw-byte digest`.
4. **Restate the conformance project.** `guides/mcp.md:3917-3930` (§ the conformance
   project's definition): the project is the foreign-runner run PLUS the digest-pinned
   schema mirror — name `tests/mirrors/ext-tasks-2026-07-28-schema.json` and the
   `TASK_SCHEMA_DIGEST` constant, and keep the recorded foreign-runner reading true as
   stated.
5. **Close the verified prose hits.** `guides/mcp.md:925` and `:2085`: name the
   parameter (`enabled`, after deliverable 2) instead of "third argument". `:913`: drop
   "the same two" — name the helpers or write "the ones the other families use". `:885`:
   "no third flag records it" → "no stored flag records it".
6. **Delete the carried `simply`.** `guides/mcp.md:325`: "It is simply the next
   registration" → "It is the next registration".

## Execution

You perform the assignment directly and spawn nothing. Validate scoped, in this order:

```text
npx oxfmt --config .oxfmtrc.json --check tests/setupConformance.ts tests/conformance.test.ts src/core/helpers.ts guides/mcp.md
npx --no-install oxlint --config .oxlintrc.json --deny-warnings tests/setupConformance.ts tests/conformance.test.ts src/core/helpers.ts
npm run check
npx vitest run --config vite.config.ts --no-cache --reporter=dot --project conformance
npm run test:guides
npx vitest run --config vite.config.ts --no-cache --reporter=dot --project src:core tests/src/core/helpers.test.ts tests/src/core/MCPServer.test.ts tests/src/core/MCPClient.test.ts tests/src/core/validators.test.ts
```

Baselines: conformance `42 passed (42)`, guides `144 passed (144)`, the four-file scoped
suite `596 passed (596)`.

## Deviation contract

Stop and report when a site's text differs from the finding's quote, when the
reprojected row reads a value other than `'#/$defs/RequestId'` when executed, when the
rename forces an edit outside the owned set, or when a gate reds outside your edits.
Ancillary wording inside a deliverable's stated shape is yours to decide and record.

## Acceptance criteria, cheap-first

1. Scoped format and lint exit 0; `npm run check` exit 0.
2. `grep -c "third argument" guides/mcp.md` reports 0; `grep -c "simply" guides/mcp.md`
   reports 0; a search for the escaped upstream prose fragment
   `{@link SubscriptionsListenRequest` over `tests/` excluding `tests/mirrors/` reports
   no hit.
3. Conformance `42 passed (42)` exit 0; guides `144 passed (144)` exit 0; the four-file
   scoped suite `596 passed (596)` exit 0.
4. A writing-rules sweep over your added and changed prose lines passes, pattern and
   population named.

## Output

Write the report to `/home/user/lsp/tmp/units/m4.1-repairs-report.md`: each deliverable
with its before and after, the rename's call-site search population, the gate readings
with exit codes, the sweep, and the actual `git status --short` and `git diff --stat`
output. Your final message is a short summary naming the report path.
