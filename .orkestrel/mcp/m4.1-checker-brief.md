# Unit brief: m4.1-checker — mechanical closure of the m4.1-repairs fix round

## Role and engine

`checker` — Sonnet, native subagent, read-only (Read, Grep, Glob; no shell, no Write). You
perform the assignment directly and spawn nothing.

## Objective

Rule on each numbered claim about the m4.1-repairs commit (mcp `11c879c`) with evidence,
attempting refutation rather than confirmation.

## Context

- Subject tree: `/home/user/mcp`, committed at `11c879c` on `claude/lsp-spec-audit-est33d`.
- The unit's brief and report: `/home/user/lsp/.orkestrel/mcp/m4.1-repairs-brief.md` and
  `m4.1-repairs-report.md`. The findings it closes:
  `/home/user/lsp/.orkestrel/mcp/m4-round-audit-verdict.md`.
- Supplied executed evidence (label readings from it as supplied): the commit's full diff at
  `/home/user/lsp/tmp/units/m4.1-checker-evidence-diff.txt`. The Orchestrator's gate run
  after the writer exited, 2026-08-26: scoped format and lint exit 0, `npm run check` exit
  0, conformance `42 passed (42)`, guides `144 passed (144)`, scoped four-file core suite
  `596 passed (596)`.
- Rules: `/home/user/mcp/AGENTS.md` § Writing; `/home/user/mcp/.claude/rules/writing.md`.

## Claims

1. `readTaskNotificationMetadata` in `tests/setupConformance.ts` reads the schema by the
   path `['$defs','NotificationMetaObject','properties','io.modelcontextprotocol/subscriptionId','$ref']`
   and its row expects `'#/$defs/RequestId'`, keeping the definition-level `type` read; and
   the text `{@link SubscriptionsListenRequest` appears nowhere under `tests/` outside
   `tests/mirrors/`.
2. `buildSubscriptionFilter` in `src/core/helpers.ts` names its third parameter `enabled`
   in the declaration, every internal read, and the `@param` line; the `@param` prose reads
   as an adjective-form boolean description and ends with a documented `Default: \`false\``;
   and the diff touches no file outside `tests/setupConformance.ts`,
   `tests/conformance.test.ts`, `src/core/helpers.ts`, and `guides/mcp.md`.
3. The digest row's title in `tests/conformance.test.ts` reads exactly
   `pins the vendored schema's raw-byte digest`.
4. The conformance-project section of `guides/mcp.md` states the project as the
   foreign-runner run plus the digest-pinned schema mirror, naming
   `tests/mirrors/ext-tasks-2026-07-28-schema.json` and `TASK_SCHEMA_DIGEST`.
5. `guides/mcp.md` contains no hit for `third argument`, no hit for `simply`
   (case-insensitive), no hit for `the same two`, and the phrase `no stored flag records`
   at the site that read `no third flag`.
6. Every added or changed prose line in the diff obeys the writing rules — no counts, no
   `should`, no banned substitution-table term in a banned sense. Name the pattern and the
   population you swept.

## Execution

Attempt refutation of each claim against the current tree and the supplied diff. A claim
you cannot break is CONFIRMED with the evidence that convinced you; a broken claim is
BROKEN with the exact site.

## Output

Return the verdict as your final message — per-claim rulings with `file:line` evidence,
then one terminal line in the form `VERDICT: PASS` or `VERDICT: FAIL — <broken claims>`.
The Orchestrator persists it; write no file.
